# service.env — TEMPLATE. KEY NAMES ONLY. NO VALUES, EVER, IN GIT.
#
# ⛔ Copy to C:\ProgramData\AromaXiangXiang\config\service.env at INSTALL time, fill it there,
#    and give it a restrictive ACL (LocalService: Read; Administrators: Full). That file is not
#    tracked and must never be added.
#
# ⛔ The service refuses to start if any of these is empty — see serviceEnvPreflight.js. A
#    missing credential must stop the process, not produce a healthy assistant answering from
#    nothing.
#
# ⛔ CLAUDE_CHAT_MODEL is here on purpose. It is the Opus chat-lane canary and currently lives
#    as an intentional uncommitted edit in the running launcher. Writing its value into a
#    tracked file would promote a live experiment into committed truth; supplying it at install
#    time keeps that decision where it belongs — with the Owner, per install.

ANTHROPIC_API_KEY=
HUB_TOKEN=
CLAUDE_CHAT_MODEL=
