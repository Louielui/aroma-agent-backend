${page}: ambiguity is IDENTICAL in both arms
${page}: the arms show the SAME nodes and differ only by group lines
(a) confirmed, NO DISPATCH_CLAIMED → PENDING
(a) fallback turn records BOTH providers separately, once each
(a) no prompt / user content / model output / secret appears in any recorded field
(a) usage IS recorded when the parse FAILS (the paid-but-unaccounted bug)
(a) usage recorded EXACTLY ONCE on the success path
(a2) a provider omitting those fields yields null, without throwing
(a2) an OpenAI failure carries http status + provider type/code/param, never the body
(a2) the router log line carries the allowlisted fields and no content
(b) ACCEPTED: clean JSON, ```json fence, bare ``` fence
(b) ACCEPTED: fence whose JSON string value CONTAINS ``` (the real failing case)
(b) ACCEPTED: multi-line code block inside the reply string
(b) DISPATCH_CLAIMED present, NO execution → INTERRUPTED (fail-closed)
(b) STILL REJECTED: language tag neither empty nor json -> fence_malformed
(b) STILL REJECTED: no closing fence (genuine truncation) -> fence_malformed
(b) STILL REJECTED: payload that is not valid JSON -> invalid_json
(b) STILL REJECTED: prose before the fence, and trailing prose after it
(b) WORKER_CLAIMED present, NO execution → INTERRUPTED (B2-14 worker track, mirrors DISPATCH_CLAIMED)
(b) empty response still rejected
(b) schema handling is UNCHANGED by the fence rule (documents the real contract)
(c) execution present, NO result → INTERRUPTED
(d) result present + ok → SUCCEEDED (from disk)
(e) result present + not ok → FAILED (from disk)
(f) result present but timeline not updated → SUCCEEDED/FAILED from the artifact
*** "Aroma System" survives verbatim through the render path ***
*** "這段不要記錄" means not stored — not stored-and-redacted ***
*** ${label.trim()} + initial ASK → ${decision} → ASK suppressed, world read ***
*** ${label.trim()} → ${decision} ***
*** --avatar-size is the ONLY place the avatar size is written ***
*** 1 — an uncited lead before the FIRST marker never becomes that source\
*** 1 — no beforeRead hook: behaviour is unchanged ***
*** 1 — two operations, same raw id: both render, neither borrows the other ***
*** 1. FINAL on step 1 → exactly one model call, no read ***
*** 1. every turn is written to the Lab store, with full verbatim text ***
*** 1/2 — the PLANNER query is what reaches the provider, verbatim ***
*** 10 — a completed search with no usable rows is LIVE-ZERO, not a failure ***
*** 10 — the shaped schema with two colliding-id operations is strict-valid ***
*** 10. the loop contains no provider-specific branch ***
*** 10b — withRowRefs never emits an empty enum ***
*** 11 — sourcesRead stays de-duplicated source kinds, not inflated ***
*** 11. every model invocation is reported to the accounting hook ***
*** 11/12/13/14 — every transport failure is UNAVAILABLE, with its own enum ***
*** 12. a direct answer still costs exactly ONE model call ***
*** 15 — ⛔ a claim with no attributable URL is never promoted to evidence ***
*** 18 ea matches 18.000 — a unit the evidence carries does not invalidate the number ***
*** 18 matches 18.000 — a correct value is not deleted by a string comparison ***
*** 2 — beforeRead allow: executeRead runs exactly once ***
*** 2 — the ref the model is SHOWN is byte-identical to the schema enum ***
*** 2 — uncited lead → good → good: each source keeps only its own sentence ***
*** 2+3. the record outlives the process that wrote it ***
*** 2+9. READ → observation → FINAL: two model calls, and call 2 SEES the data ***
*** 21/22/23/25 — questions that need no outside world make ZERO web calls ***
*** 24 — a mixed turn reads BOTH worlds, and nothing internal leaves ***
*** 25 — the public source is GOVERNED now, and still off by default ***
*** 26/27/28 — ⛔ the semantic layer never learns the vendor, and cannot write ***
*** 29/30 — ⛔ the accounting line carries counts, never the query, URLs or the key ***
*** 2b — and the validator indexes those same canonical refs ***
*** 3 — a blank / newline lead is not content, and does not corrupt the claim ***
*** 3 — beforeRead final: zero reads, and the loop returns that result ***
*** 3 — with two owners, bare "7" and "aroma_system#7" resolve to NOTHING ***
*** 3. READ → READ → FINAL: three decisions, two reads, both observations carried ***
*** 3/26/27 — the planner remains the SOLE outbound query constructor ***
*** 3/4/6 — ⛔ nothing but the query can reach the vendor ***
*** 3b — with ONE owner, the legacy aliases still resolve ***
*** 4 — a hook that THROWS stops the read (fail closed) ***
*** 4 — replenishment rows + purchasing zero rows: both truths, distinguishable ***
*** 4. no extra call after FINAL ***
*** 4. turn order, timestamp, role, model and provider are all preserved ***
*** 401 -> READ_FAILED: unauthorized, NEVER "no results" ***
*** 5 — a hook returning nonsense stops the read (fail closed) ***
*** 5 — an Owner-supplied value MAY travel, because he typed it ***
*** 5 — row_local verifies on canonical refs, and refuses ambiguous aliases ***
*** 5. flag OFF writes NOTHING and loads nothing ***
*** 5. reads are stopped at MAX_REASONING_STEPS, then ONE reserved call to compose ***
*** 5b — a canonical ref from a read the claim did not declare is refused ***
*** 5b. the ONLY live-path change is one guarded block ***
*** 6 — a substantive uncited sentence BETWEEN two citations is inherited by neither ***
*** 6 — set_scoped resolves the EXACT EvidenceSet by readKey ***
*** 6 — the allowlist is checked BEFORE the hook ***
*** 6. an invented capability is NEVER executed ***
*** 6. nothing in the Lab claims the archive is clean ***
*** 6. ordinary text is NOT redacted — NEGATIVE control ***
*** 6. secrets are redacted — POSITIVE control ***
*** 6. the redaction runs BEFORE the write, and the archive records only KINDS ***
*** 6b — a bare source matching TWO live evidence sets is ambiguous ***
*** 6c — a bare source with exactly ONE live evidence set still works ***
*** 7 — WRITE_SHAPED is checked BEFORE the hook ***
*** 7 — an automatic single Aroma read keeps ref=aroma_system#<id> ***
*** 7. a write/action request inside the loop is never executed ***
*** 7. delete: one turn, one conversation, a date range, and everything ***
*** 7. export carries every turn, grouped and ordered ***
*** 7/8 — location is admitted when present and OMITTED when absent ***
*** 8 — invented rows and wrong quantities are still refused ***
*** 8 — the step bound is unchanged ***
*** 8. a failed read becomes a controlled observation, and is not retried ***
*** 8. deleting the whole Lab archive leaves production untouched ***
*** 9 — READ → READ → FINAL: both reads survive and both get the plan schema ***
*** 9 — reasoningLoop remains domain- and provider-neutral ***
*** 9. a write failure NEVER throws, and is reported ***
*** 9. fail-OPEN is documented AS THE OPPOSITE of the Computer Operator audit ***
*** 9. the conversation completes and the failure is VISIBLE ***
*** A / B / C — the gate is silent unless BOTH flags and the chat lane agree ***
*** A · 9/16/17 — the documented shape yields NON-EMPTY, LABELLED, sourced content ***
*** A — A4 OFF + chat: the mode enum is EXACTLY production, commit included ***
*** A — A4 OFF, public OFF: the legacy call shape is untouched ***
*** A — A4 off: the automatic keyword read happens exactly as it does today ***
*** A — A4 off: the nextRead schema is the UNCHANGED object, not a copy ***
*** A — an UNAVAILABLE read produces NO EvidenceSet and NO scope line ***
*** A — an explicit role pin WINS over the environment ***
*** A — an ordinary source is still bound by the shared cap ***
*** A — an ordinary turn never touches the worker ***
*** A — explicit mixed → mixed, and it declares BOTH worlds required ***
*** A — production offers public_knowledge only when the Owner switches it on ***
*** A — replenishment + purchasing: the validator keeps supported facts from BOTH ***
*** A — 你好: no read, no Answer Plan forced, one call ***
*** A — 幫我睇 Aroma System 最近啲發票: the model names the view, and it RUNS ***
*** A/B — the role -> provider -> model map is FIXED, whatever the main adapter is ***
*** A/B/D — good→good keeps both; good→broken keeps the good; broken→good→good keeps none ***
*** A2 — the invoice question reads INVOICES, not the inventory default ***
*** A4 on + CHAT lane: guidance appears ONCE, BEFORE a verbatim, LAST SYSTEM_PROMPT ***
*** A4-3B/A — a public read SLOWER than the shared cap survives, end to end ***
*** A4-3B/B — a recovery-shaped public read (args=null) never sends an empty query ***
*** A4-3B/B — no planner means no public read, even with a valid raw query ***
*** A4-3B/B — the PLANNER owns the outbound query on a pure-public route turn ***
*** ACCEPTANCE 1 — Costco is ANSWERABLE, in ONE read and with NO join ***
*** ACCEPTANCE 2 — 「上次盤點同存量對唔對得上」 is NOT ANSWERABLE, and that is the PASS ***
*** ACTION and BUSINESS_QUERY are out of scope ***
*** ANY two of the FOUR on ⇒ configuration_conflict ⇒ zero execution ***
*** APPROXIMATE values, sampled from a screenshot — flagged as such in the source ***
*** ASK lane projection is frozen ***
*** AUTO-READ PROOF — BUSINESS_QUERY still classifies, but owns NO read initiation ***
*** A′: an omitted assistant turn puts NO part of the reply on disk ***
*** A′: passing the reply ANYWAY still writes nothing — omission is structural ***
*** A′: read context used → user kept, assistant body omitted ***
*** A′: the omission record keeps place, order and provenance — and nothing else ***
*** B — A4 ON, public flag OFF: the public world is honestly unavailable ***
*** B — A4 off: the parser output is unchanged and drops args ***
*** B — READ 1 rows + READ 2 live-but-empty: BOTH truths survive, unpaired ***
*** B — a LIVE read matching zero rows keeps its EvidenceSet and SCOPE ***
*** B — a declared per-adapter timeout overrides the shared cap ***
*** B — an injected public source yields exactly one operation ***
*** B — no pin falls to CLAUDE_MODEL, and to nothing else ***
*** B — rows on a NON-business route STILL cannot force a plan ***
*** B — sources carry NO snippet, and evidence is still factual ***
*** B — supplied facts: 「採購價」 no longer reads Aroma System ***
*** B — ⛔ obligation exists but the MAIN model reads: worker = 0 ***
*** B — 你能看到 Aroma System 嗎: ask ONE question, read NOTHING, write NOTHING ***
*** B/C/D — ${name} → not_mixed, and NO worlds are required ***
*** C / J — no model at all FAILS CLOSED, before any transport ***
*** C · ⛔ BLOCKER — a broken citation must not hand its text to the next source ***
*** C — A4 ON, public ON: a clear public question reaches public_knowledge ***
*** C — A4 SHADOW: no narrowing ***
*** C — A4 off: an ordinary chat turn is unchanged (one call, no schema drift) ***
*** C — Aroma asked which view; 「庫存」 resolves it and the read runs ***
*** C — CONVERSATION + model-directed LIVE read → next call gets the Answer Plan ***
*** C — a SECOND terminal invokes the worker exactly once ***
*** C — a failed second read cannot become evidence, and does not cancel the first ***
*** C — clear internal: the model names the operation and Truth Closure activates ***
*** C — explicit mixed + initial ASK → require_mixed → both worlds ***
*** C — invented public operations are refused, with no fuzzy matching ***
*** C — the SAME source at the SAME speed dies without the declaration ***
*** C — uncited model prose is discarded, however confident it sounds ***
*** C1 — under id collision, BOTH operations\
*** C1. ⛔ THE REAL LOG DISTINGUISHES EVERY VERDICT — not one constant ***
*** C1b. ⛔ THE LOGGED DROP CARRIES ENUM AND COUNT ONLY — no content ***
*** C2 — the view just read is declared READ, not left looking unavailable ***
*** C2 — two operations sharing a sourceId ARE citable as distinct entities ***
*** C2. ⛔ A SHORTAGE SUPERLATIVE MAY NOT USE A suggested_order_qty PROOF ***
*** C2b. AND A BARE 「排序」 MAY STILL USE THAT PROOF — it names no measure ***
*** C2c. and the matching proof still passes ***
*** C3 — FORMERLY A LIMITATION PIN: a shared sourceId no longer mis-attributes ***
*** C3. ⛔ DUPLICATE TITLES, DIFFERENT RAW IDS — A CORRECT TOP-N STILL PASSES ***
*** C3. ⛔ THE DEFAULT COMPANION CONSTRUCTS NO EXECUTOR ***
*** C3b. ⛔ AN ITEM FROM ANOTHER SOURCE FAILS THE CLAIM CLOSED ***
*** C3c. ⛔ ORDERING NO LONGER SHIPS ON ZERO MATCHES — the borrowed-proof bypass ***
*** C3d. and an ORDERING subsequence of its OWN rows still passes ***
*** C4. ⛔ A TOP-12 CLAIM SHOWING 2 ITEMS IS REFUSED — the declared count is checked ***
*** C4. ⛔ AND THE REFUSAL PATH NAMES THE MISSING EXECUTOR RATHER THAN PROCEEDING ***
*** C4b. ⛔ A COUNT LARGER THAN THE PROVEN SET IS REFUSED ***
*** C4c. ⛔ AND A HEADING THAT NAMES A COUNT WITHOUT DECLARING ONE FAILS CLOSED ***
*** C7. ⛔ NOTHING IN A REQUEST CAN ENABLE A CAPABILITY ***
*** CHAT lane projection is frozen ***
*** CLASS 3 — an explicitly PUBLIC question takes the public path, untouched ***
*** COMMIT / proposal lane projection is frozen ***
*** COMPUTER_OPERATOR alone authorizes NOTHING without a configured supervisor ***
*** CONCURRENCY: parallel writers all survive ***
*** CONTROL — the three POST draft routes are unreachable by ANY input ***
*** Chinese numerals ***
*** Chinese operators ***
*** D — A4 ON + chat, zero-read schema: commit is STRUCTURALLY IMPOSSIBLE ***
*** D — A4 on: the strict schema carries args ***
*** D — a clear INTERNAL question never reaches the public provider ***
*** D — a zero-read Gmail question still initiates its existing generic read ***
*** D — ambiguous beef: ASK, zero reads, zero evidence, zero unavailable ***
*** D — an automatic BUSINESS_QUERY read is unaffected ***
*** D — exactly the closed args reach the fake public executor ***
*** D — model-directed LIVE read matching ZERO rows still demands a plan ***
*** D — only ONE world available ⇒ no verifier call, no cost ***
*** D — the only read failing leaves Truth Closure shut ***
*** D — the retired model exists nowhere in adapter CODE ***
*** D — ⛔ true ambiguity + initial ASK → clarify → ASK allowed, ZERO reads ***
*** D(pin) — no OpenAI key means NO verifier, and never a fallback to Claude ***
*** D/E — 35s outer, 30s inner, 10s for everyone else ***
*** D/E — sources but no attributable content is UNAVAILABLE, NEVER live and NEVER zero ***
*** D/E — the worker\
*** D2 — the args shape is CLOSED: no url, provider, endpoint, headers, count ***
*** D5. ⛔ not_detected AND evaluated_allowed ARE DISTINGUISHABLE IN THE LOG ***
*** D5b. ⛔ AND A REJECTION CARRIES STATUS, REASON AND COUNT ***
*** D5c. ⛔ THE SUMMARY CARRIES NO CONTENT — enum and count only ***
*** D5d. LIVE: THE GATE SUMMARY REACHES THE REAL ANSWER_PLAN LINE ***
*** D6. ⛔ AN INVENTORY PROOF MAY NOT VALIDATE A SECTION OF INVOICE ROWS ***
*** D6b. ⛔ AND THE INVENTORY SECTION IN THE SAME TURN STILL PASSES ***
*** D6c. ⛔ NO GROUP FOR THE PROOF MEANS NO USABLE PROOF ***
*** D7. ⛔ A HEADING NAMING A COUNT IT DID NOT DECLARE FAILS CLOSED ***
*** D7b. ⛔ AND A DECLARED COUNT LARGER THAN THE SECTION IS REFUSED ***
*** D7c. and an honest declared count still passes ***
*** DEAD_LOCK_FAILS_CLOSED — a crashed holder does NOT get cleaned up automatically ***
*** DEAD_OLD_LOCK_FAILS_CLOSED — dead AND ancient is still not an invitation ***
*** DESCRIPTOR — a refused observation is COUNTED, never silently dropped ***
*** DESCRIPTOR — matchingTotal exists ONLY when the page literally printed it ***
*** DESCRIPTOR — rowShape.hasLocation reports THE READ, not the site ***
*** DESCRIPTOR — the three readStates are A1\
*** DESCRIPTOR — ⛔ a results page is a SAMPLE, and says so in four fields ***
*** DESCRIPTOR — ⛔ an observation is admitted by the SEALED ORDER, not by a field it carries ***
*** DESCRIPTOR — ⛔ filtersApplied is null, NOT [] ***
*** DPI drifting since the gate invalidates the coordinate arithmetic ***
*** DST is honoured, not assumed ***
*** E / F — a main model that FINALs or ASKs never reaches the gate ***
*** E · an uncited lead before a broken citation reaches no source ***
*** E — A4 ON + chat, post-read Answer Plan schema: commit is impossible there too ***
*** E — a mixed turn completes BOTH required worlds through the real composition ***
*** E — an AUTOMATIC read that fails also produces no EvidenceSet ***
*** E — continuation 「兩邊都睇」 carries prior OWNER context to the verifier ***
*** E — internal adapter params are byte-identical with and without args ***
*** E — model-directed UNAVAILABLE read does NOT activate truth closure ***
*** E — the launcher still pins a CURRENT model explicitly ***
*** E — unknown properties are not admitted, and cannot ride along ***
*** E — ⛔ allow_final + initial ASK → the ASK is PRESERVED, not forced into an answer ***
*** E — 「我哋供應商。」 continues the original task as an INTERNAL read ***
*** E — 你好: the model may answer directly — ONE call, ZERO reads ***
*** E2 — a LIVE read is not cancelled by a later UNAVAILABLE one ***
*** E2 — an invalid freshness is nulled, never passed through ***
*** E2E 1 — AMBIGUOUS → ASK, and ZERO reads of either world ***
*** E2E 2 — CLEAR INTERNAL → internal only, public untouched ***
*** E2E 3 — CLEAR PUBLIC → public only, and the PLANNER\
*** E2E 4 — ⛔ MIXED → BOTH worlds, public query re-authored from Owner words ***
*** E2E 5 — SUPPLIED FACTS → ZERO reads; he already told her ***
*** E2E — the five outcomes are genuinely different, not one behaviour ***
*** E8. ⛔ A DIRECTED aroma_system.replenishment TURN FINDS ITS OWN PROOF ***
*** E8b. ⛔ AND THE SAME TURN STILL REFUSES A WRONG ORDER ***
*** E8c. ⛔ THE PROOF STILL MAY NOT REACH A DIFFERENT OPERATION\
*** E8d. ⛔ A PROOF WHOSE OPERATION HAS NO GROUP REPORTS no_ranking_proof ***
*** E8e. and LEGACY evidence with no readKey still resolves its single group ***
*** END TO END: a Gmail turn whose reply CITES the mail stores the question and omits the answer ***
*** END TO END: flag OFF still writes nothing at all ***
*** END TO END: read happened, reply CITES NOTHING → the answer is kept, mail still absent ***
*** END TO END: with no read access the reply IS stored, and provenance still real ***
*** EXACT values, read from Manus computed styles ***
*** English phrasing works the same way ***
*** English words in the question do not turn the whole answer English ***
*** F — A4 ON + proposal lane: the mode enum is NOT narrowed ***
*** F — READ → READ → FINAL: the loop is not cut short by grounding ***
*** F — READ_ACCESS off → no read operation exposed at all ***
*** F — ambiguous → clarify, with a MEANING question and zero obligation ***
*** F — an ambiguous question ASKS, and reads nothing at all ***
*** F — args null survives as null, and an empty declaration IS null ***
*** F — invalid citation indices FAIL CLOSED, with no whole-text fallback ***
*** F — invalid timeout metadata falls back to the shared cap, never to none ***
*** F — the A4 Recovery Worker pin is untouched by this repair ***
*** F — the same canonical args produce the same readKey ***
*** F — two LIVE operations still keep both EvidenceSets and both readKeys ***
*** F — ⛔ a WRONG-WORLD choice is refused and nothing is read ***
*** F — ⛔ assistant history cannot cause mixed, and never reaches the verifier ***
*** F — ⛔ the model ASK text is never sent to the verifier ***
*** F — 「市場。」 reads nothing internal and invents nothing ***
*** F(live) — an adapter declaring garbage is still timed out ***
*** F(pin) — the module cannot even name a conversational adapter ***
*** F2 — a clarify question carrying implementation vocabulary is replaced ***
*** F2 — a source not authorised this turn is never exposed ***
*** FAIL-SAFE: anything that is not an explicit false omits the body ***
*** FREE TEXT CAN NEVER BECOME AN ACTION NAME ***
*** G — EVERY timer the connector creates is cleared, on success and on timeout ***
*** G — SYSTEM_PROMPT is byte-identical and still the FINAL segment ***
*** G — a fast success leaves no pending timer behind ***
*** G — absence from evidenceSets IS the representation; the claim fails closed ***
*** G — an automatic BUSINESS_QUERY read behaves exactly as before ***
*** G — an invented row is still refused across two operations ***
*** G — aroma_system withheld from OpenAI → not ONE child operation is exposed ***
*** G — query, freshness or location each change the readKey ***
*** G — structured output still reaches the provider unchanged ***
*** G — with internal evidence live, only the PLANNER\
*** G — ⛔ EXACT VALUES arrive at executeRead: schema → parser → pending → loop → read ***
*** G — ⛔ every unusable verifier result fails to NOT_MIXED, never to mixed ***
*** G — ⛔ verifier ASK: zero reads, zero evidence, and it is not a read failure ***
*** G — 「兩邊都睇。」 is mixed, not ambiguous: internal may run, nothing public invented ***
*** G/H · 19/20 — URL, TITLE and CONTENT all reach GPT, and GPT still writes the answer ***
*** G/H — assistant history excluded; Owner-only bound preserved ***
*** G/H — continuation carries prior OWNER messages, oldest first ***
*** G/H — unknown and write-shaped capabilities are refused ***
*** G/H/I — overlap stops, order does not matter, one page may carry two claims ***
*** G2 — with A4 off the same envelope delivers args = null (channel closed) ***
*** GET /api/v1/context/health is refused without auth ***
*** GET /api/v1/context/recent — the Gmail/Drive leak — is refused without auth ***
*** GET /demo is refused, and a browser is sent to the login form ***
*** GET /settings serves one self-contained page ***
*** GET returns values, caps and effective switches ***
*** GMAIL can be withheld from GPT on its own, with no code change ***
*** H / I — verifier ALLOW lets the proposed world execute ***
*** H — NO sampling parameter is sent, in either direction ***
*** H — a missing API key leaves the source unavailable and startup intact ***
*** H — a second model-directed read carries ITS OWN args, not the first read\
*** H — an explicitly public question triggers no Aroma read ***
*** H — an operation outside the vocabulary never reaches the reader ***
*** H — the A4-1R semantic guidance is FROZEN for this slice ***
*** H — the mixed verifier runs at most ONCE per turn ***
*** H — the raw query never appears in the readKey ***
*** H — the rendered block never tells two stories about one read ***
*** H — ⛔ no_plan_returned is GONE from the model-directed path ***
*** H-E9. ⛔ EVERY SHAPE THAT DEFEATED THE PARSER IS STILL RECOGNISED ***
*** H-E9b. ⛔ AND EACH FAILS CLOSED WITH TWO CORRECT ITEMS PRESENT ***
*** H-G11. ⛔ A TWENTY-CLAIM SHOWING ONE ROW IS REFUSED — Blocker 11, structurally ***
*** H-G11b. ⛔ A FOUR-CLAIM SHOWING THREE IS REFUSED — the production heading ***
*** H-G11c. A GENUINE NO-N SUPERLATIVE IS STILL LEGITIMATE ***
*** H-G11d. AND THE HONEST DECLARED COUNTS ALL PASS ***
*** H1. ⛔ rankingClaim IS REQUIRED AND NULLABLE, and its enums are closed ***
*** H10. ⛔ THE PROOF MUST OWN THESE ROWS — by readKey, not by source name ***
*** H11. ⛔ THE DECLARED METRIC MUST BE THE ONE ACTUALLY SORTED ON ***
*** H12. ⛔ AN INCOMPLETE ORDERING ENTITLES NOTHING, however neat the sequence ***
*** H13. ⛔ top_n = EXACTLY N ITEMS. Three under a four-claim is refused ***
*** H14. ⛔ AND ONE ITEM UNDER A TWENTY-CLAIM IS REFUSED — Blocker 11, structurally ***
*** H15. ⛔ A CLAIM LARGER THAN THE PROVEN SET IS REFUSED ***
*** H16. top_n = EXACTLY proven[0:N], IN ORDER ***
*** H17. superlative = AN EXACT PREFIX OF WHAT IS DISPLAYED ***
*** H18. ordering KEEPS A–G LEGAL-SUBSEQUENCE SEMANTICS ***
*** H19. ⛔ AN ITEM DROPPED BEFORE THE GATE REJECTS THE WHOLE SECTION ***
*** H19b. AND AN ORDINARY SECTION KEEPS PER-ITEM DROPPING, UNCHANGED ***
*** H19c. ⛔ 頭 / 前 / 第 OPEN A RANKING, AND WITHOUT A DECLARATION IT FAILS CLOSED ***
*** H19d. ⛔ AND 「目前庫存」 IS STILL NOT A RANKING — the anchor is what protects it ***
*** H2 — a raw adapter method is not an operation ***
*** H2 — end to end, ONE authority call per turn, and it is the resolver ***
*** H2. THE LEAK-GUARD ANSWERS ONE QUESTION AND RETURNS A BOOLEAN ***
*** H20. ⛔ ON SUCCESS THE MODEL HEADING IS REPLACED, NOT KEPT ***
*** H21. ⛔ THE SERVER HEADING NAMES THE VERIFIED CLAIM, and only that ***
*** H22. ⛔ SENTINEL SURVIVAL — the real seam, the real log, the real reply ***
*** H23. ⛔ A REJECTED RANKING SECTION TAKES ITS HEADING WITH IT ***
*** H24. ⛔ ONCE ON THE STRUCTURED PATH, FAILURE NEVER FALLS BACK TO AN ORDINARY SECTION ***
*** H25. ⛔ THE COUNTERS RECORD SHAPE, NEVER CONTENT ***
*** H26. THE COUNTERS ARE ZERO ON AN ORDINARY TURN, not absent ***
*** H27. ⛔ GATE ORDER: the FIRST failing check is the one reported ***
*** H28. rankingSectionViolations still returns the REJECTED INDICES ***
*** H29. ⛔ LIVE: A FOUR-CLAIM SHOWING THREE ROWS DOES NOT REACH THE OWNER ***
*** H3 — a bare aroma_system is no longer a valid request ***
*** H3. ⛔ A RANKING HEADING WITH NO DECLARATION FAILS CLOSED ***
*** H30. ⛔ LIVE: THE PROVEN SECTION SHIPS UNDER THE SERVER\
*** H31. ⛔ A GROUNDED MODEL HEADING IS REPLACED TOO — the blanking is load-bearing ***
*** H32. ⛔ LIVE: A RANKING HEADING WITH NO DECLARATION SHIPS NO ROWS ***
*** H33. ⛔ LIVE: A DECLARED METRIC THE TURN DID NOT PROVE SHIPS NOTHING ***
*** H34. ⛔ LIVE: THE RIGHT COUNT OF THE WRONG ROWS SHIPS NOTHING ***
*** H35. ⛔ LIVE: THE COUNTERS REACH THE REAL ANSWER_PLAN LINE ***
*** H36. ⛔ LIVE: A FOREIGN ROW SINKS THE WHOLE RANKING SECTION, IN EVERY KIND ***
*** H37. ⛔ LIVE: 「頭四項缺貨」 WITH NO DECLARATION SHIPS NOTHING ***
*** H4. ⛔ AND EVERY SHAPE THAT DEFEATED THE PARSER NOW FAILS CLOSED ***
*** H5. AN ORDINARY SECTION IS UNTOUCHED — heading and all ***
*** H6. ⛔ AN ILL-SHAPED DECLARATION IS REFUSED, NOT REPAIRED ***
*** H7. normaliseRankingClaim reports presence and validity SEPARATELY ***
*** H8. ⛔ NO RANKED SOURCE — a declaration does not create a proof ***
*** H9. ⛔ TWO ORDERINGS IN ONE TURN — AMBIGUITY IS PER SECTION, NOT PER TURN ***
*** HOOK — an ABSENT replyCitesContext still omits (fail-safe) ***
*** HOOK — no read context at all → kept, as before ***
*** HOOK — read happened, reply cites nothing → the text is written ***
*** HOOK — reply cites → omission record, and the text never reaches the writer ***
*** HOOK — the user turn is ALWAYS kept, both ways ***
*** I — ONE planned query per turn, so a second public read IS the same instance ***
*** I — a mixed question is not an ambiguity ASK ***
*** I — a provider error is unavailable, never a fabricated empty world ***
*** I — an UNSUPPORTED model claim is filtered by the EXISTING validator ***
*** I — an explicit edit request still routes to ACTION under A4 ON ***
*** I — args do NOT reach any current adapter: params are identical with and without ***
*** I — the credential check is unchanged and still comes first ***
*** I — the verifier runs at most once per turn, ASK path included ***
*** I — ⛔ mixed SKIPS the ambiguity verifier entirely ***
*** I/J — malformed output and a throwing worker both fail closed ***
*** I/J — ⛔ assistant turns and unknown roles never reach the verifier ***
*** I1. ⛔ app.js HAS ZERO REACH INTO ANY W1 CAPABILITY ***
*** I2 — the INSTANCE identity mechanism itself is intact ***
*** I2. ⛔ COMPUTER_OPERATOR IS STILL ZERO-EFFECT AT RUNTIME ***
*** I3. ⛔ NO OBSERVATION CAPABILITY IS ENABLED, IN EITHER REGISTER ***
*** I4. ⛔ NOTHING IN src/computer CAN RUN A TASK, A PROCESS OR POWERSHELL ***
*** I5. THE rootUntouched HELPER AND ITS INVARIANT SURVIVE THE PORT ***
*** INVALID, not pass —
*** INVERTED 2026-07-28: all three bindings demonstrated under the operator account ***
*** INVERTED in Phase 3a: it now stops a real process — all three bindings live ***
*** INVERTED: Gmail is NOT shown for an invoice question, even when it matched by search ***
*** INVERTED: an invalid value is ON, not off ***
*** INVERTED: an unattributed 讀唔到 is now left alone ***
*** INVERTED: the gate is now FOUR flags, mutually exclusive (Owner ruling) ***
*** INVERTED: the negations and the isolation scope ARE on the visible face ***
*** J — A4 OFF constructs NO web provider and makes NO outbound search ***
*** J — a planning question containing 「安排」 does not read the calendar ***
*** J — a poisoned content part does not poison a separate, sound one ***
*** J — an invented capability is still refused before the connector ***
*** J — egress still blocks AFTER the ambiguity gate allows ***
*** J — not_mixed hands the meaning to the source-intent resolver ***
*** J — public_knowledge.search stays offerable after one search ***
*** J — the accounting line stays content-free now that rows carry content ***
*** J — the schema reached by the model-directed path passes strict-mode rules ***
*** J — ⛔ after require_public, a SECOND ASK cannot end the turn ***
*** J — 「幫我查下最近發票」: zero auto-read, and the contract cannot say commit ***
*** J1. ⛔ AN ITEM LOST TO THE SECTION CAP NARROWS THE CLAIM TOO ***
*** J1b. AND A FIVE-ITEM DECLARATION — exactly at the cap — still passes ***
*** J1c. AN ORDINARY SECTION OVER THE CAP KEEPS TODAY\
*** J2. ⛔ THE SELECTION GUARD WAS FAIL-OPEN ON LEADING WHITESPACE ***
*** J2b. AND THE OTHER PADDING-SENSITIVE GUARD IN THIS FILE IS ALREADY TOLERANT ***
*** J3. ⛔ LIVE: A RANKING BIGGER THAN THE TURN CAN RENDER SHIPS NOTHING ***
*** J3b. ⛔ THE CAP ROUTE, AT THE RENDER SEAM, WITH SIX RETRIEVABLE ROWS ***
*** J4. ⛔ LIVE: A PADDED RANKING HEADING WITH NO DECLARATION SHIPS NOTHING ***
*** K — UTILITY still routes server-first under A4 ON ***
*** K — WRITE_SHAPED refusal is untouched ***
*** K — after require_internal, a second ASK is refused too ***
*** K — an exact duplicate public read does not execute twice ***
*** K — an invented capability is still refused before the connector ***
*** K — cannot_route touches no connector ***
*** K — explicit mixed, INTERNAL first → allowed ***
*** K — the verifier is called at most ONCE per turn ***
*** K — the verifier runs at most once per turn ***
*** K1. ⛔ A ROW FROM ANOTHER OPERATION MAY NOT RIDE IN ON ITS TITLE ***
*** K1. ⛔ SUCCESS REQUIRES ALIVE-BEFORE AND ABSENT-AFTER ***
*** K10. ⛔ THE MODULE IS DORMANT — no side effect at require time ***
*** K11. NO PRODUCTION CALLER IN THIS TRANCHE ***
*** K1b. AND THE HONEST CITATION OF THE SAME ROWS STILL PASSES ***
*** K2 — end to end, one turn spends exactly one final-verifier call ***
*** K2. ⛔ AT THE RENDER SEAM: the foreign row does not reach the Owner ***
*** K2. ⛔ NOTHING TO KILL IS A NAMED NON-SUCCESS, NOT A KILL CLAIM ***
*** K3. THE FALLBACK SURVIVES FOR SHAPES WITH NO SERVER-RESOLVED IDENTITY ***
*** K3. ⛔ MORE THAN ONE MATCH KILLS NOTHING ***
*** K3b. ⛔ AND IT NEVER KILLS AN ARBITRARY powershell.exe ***
*** K3c. ⛔ NO CALLER-SUPPLIED PID, PATH OR PROCESS NAME IS HONOURED ***
*** K4. ⛔ A FAILED TASK STOP CANNOT BECOME A SUCCESS ***
*** K5. ⛔ A FAILED TERMINATE CANNOT BECOME A SUCCESS ***
*** K5b. STOPPING THE TASK IS ENOUGH WHEN IT REALLY STOPS THE PROCESS ***
*** K6. ⛔ aliveAfter TRUE IS ALWAYS A FAILURE, WHATEVER ELSE SUCCEEDED ***
*** K7. implemented IS TRUE ONLY BECAUSE THE CODE EXISTS ***
*** K8. ⛔ observerKillDemonstrated STAYS FALSE ***
*** K9. ⛔ killingCompanionStopsObserver STAYS FALSE ***
*** KILL 1 — the SERVICE GATE stops it before anything is sent ***
*** KILL 2 — the COMPANION ABORT stops it after it is running ***
*** KILL 3 — the OS FALLBACK: closing the channel leaves it nothing to answer on ***
*** L / O — an explicit mixed need is ALLOWED, and both reads proceed ***
*** L — ACTION still routes before any retrieved content, under A4 ON ***
*** L — a write-shaped capability is still refused ***
*** L — explicit mixed, PUBLIC first → allowed, no forced ordering ***
*** L — the reasoning step bound is unchanged ***
*** L — ⛔ internal evidence may not ride out inside a public query ***
*** L — ⛔ under require_mixed, an ASK after ONE world is still refused ***
*** L/M — Owner-only input: assistant turns and unknown roles excluded ***
*** L/M — ⛔ every unusable verifier result WITHHOLDS the final ***
*** L1. ⛔ A RANKING SECTION BEYOND THE RENDER CAP IS STILL COUNTED ***
*** L2 — end to end, an unusable verdict does not publish the model answer ***
*** L2 — the SECOND FENCE still fires if the PLANNER itself returns a contaminated query ***
*** L2. ⛔ citesEvidence:false STILL COUNTS WHAT THE MODEL WROTE ***
*** L3. AN ORDINARY FIFTH SECTION MOVES NOTHING ***
*** L4. AND A DECLARED RANKING BEYOND THE CAP COUNTS AS DECLARED, NOT MISSING ***
*** L5. NO SECTIONS AT ALL IS 0/0/0, and an absent array is too ***
*** L6. ⛔ AT THE RENDER SEAM: the real ANSWER_PLAN line carries the true rate ***
*** LEGACY envelope — no reply field — is projected the same way ***
*** LIVE-ZERO reaches the caller as zero rows, and that is a true answer ***
*** LIVE_LOCK_BUSY — a living holder makes the store busy, and nothing is touched ***
*** LIVE_OLD_LOCK_BUSY — age never overrules a living holder ***
*** LOCK 1 — a result can only carry declared, non-raw fields ***
*** LOCK 1a — observation.js imports one inert sibling, and nothing else ***
*** LOCK 1b — the observation return path never calls prompt assembly ***
*** LOCK 3 — the 7-day sweep actually deletes an aged file from disk ***
*** LOCK 3 — the sweep deletes the names that actually hold raw content ***
*** LOCK 3 — titles are EPHEMERAL ONLY; a durable record refuses them ***
*** M / N — the verifier sees the bounded history for a continuation ***
*** M — a LIVE model-directed read still activates Truth Closure ***
*** M — a value the OWNER typed himself is not a leak ***
*** M — an invented capability is still refused before the connector ***
*** M — no chain-of-thought field exists anywhere in the shaped schemas ***
*** M — ⛔ premature FINAL with PUBLIC missing is REFUSED, and the model recovers ***
*** M/N — once the required world is live, FINAL and ASK both behave normally ***
*** MALFORMED_LOCK_FAILS_CLOSED — an unreadable lock is busy at any age ***
*** N real processes appending M records each lose nothing ***
*** N — a LIVE public result becomes ordinary A3 evidence ***
*** N — a LIVE zero-row read is still evidence ***
*** N — a write-shaped capability is still refused ***
*** N — premature FINAL with INTERNAL missing is REFUSED ***
*** N — the egress invariant is written down, and honestly marked UNENFORCED ***
*** N — ⛔ CLEAR PUBLIC: the initial FINAL is refused ***
*** N/O/P — ⛔ end to end, a mixed turn sends the worker NO internal evidence ***
*** N2 — the detector catches the exact forbidden composition ***
*** N2 — ⛔ the refusal is VISIBLE TO THE MODEL, not only to the loop ***
*** N3 — A4-0A performs no egress at all: args reach no connector ***
*** NO order unlocks a NEVER action — however perfect the seal ***
*** NOTHING in the application requires the Computer Operator modules ***
*** NO_AUTOMATIC_RECLAIM_PATH — the reclamation machinery is gone, not merely unused ***
*** NULL IS NOT FALSE — an absent title is unknown, not unchanged ***
*** NULL IS NOT FALSE — the two states must not collapse ***
*** O — a LIVE public search matching nothing is evidence, not a failure ***
*** O — an UNAVAILABLE read still produces no EvidenceSet ***
*** O — both worlds live → FINAL is allowed immediately ***
*** O — with NO obligation, an ordinary ASK is untouched ***
*** O/Q — a live read in the required world releases the FINAL ***
*** OFF is the default, and OFF means byte-identical ***
*** ONE step in flight — the Companion never holds the plan ***
*** ONLY BUSINESS_QUERY may name a source ***
*** OWNER-DIRECTED layout — deliberate departures from the Manus reference ***
*** OpenAI: a malformed responseFormat throws BEFORE any network call ***
*** OpenAI: no responseFormat → nothing is sent "just in case" ***
*** OpenAI: responseFormat becomes text.format with strict:true ***
*** P / Q / R end-to-end — a broken verifier reads NOTHING ***
*** P / Q — malformed and throwing verifiers both fail closed ***
*** P — a FAILED public search produces no EvidenceSet ***
*** P — a LIVE zero-row model-directed read is still evidence ***
*** P — no chain-of-thought field was added ***
*** P — the pre-read ASK still happens, now owned by the source-intent resolver ***
*** P — ⛔ an UNAVAILABLE public read does not satisfy the public world ***
*** P — ⛔ an UNAVAILABLE read does not satisfy the required world ***
*** P1 — ⛔ the planner never receives the raw query or any internal evidence ***
*** P1. AN EPHEMERAL RESULT MAY CARRY titles — ONCE THE SESSION IS PROVEN ***
*** P10 — two PUBLIC requests in one turn spend ONE planner call ***
*** P11 — the cache also remembers a REFUSAL, so a broken planner is asked once ***
*** P12 — ⛔ assistant turns are excluded, and so is every unknown role ***
*** P13 — at most 4 preceding Owner messages, oldest dropped first ***
*** P14 — the total character bound holds against a pasted document ***
*** P15 — no Owner context means no query leaves ***
*** P16 — the planner output is admitted through the CLOSED arg shape ***
*** P17 — the module is provider-neutral, proven statically ***
*** P18 — ⛔ the ROOT is a plain object, and the fields are DERIVED not retyped ***
*** P19 — the log line is counts and enums only ***
*** P1b. ⛔ BUT A FOREIGN SESSION\
*** P2 — a recovery-shaped public read with args=null still reaches the provider ***
*** P2 — ⛔ the public executor receives none of the four internal values ***
*** P2. ⛔ THE AUDIT RECORD REFUSES titles OUTRIGHT ***
*** P20 — the planner system text forbids inventing specifics and naming the restaurant ***
*** P2b. AND THE COUNTS IT REPLACED ARE STILL AUDITABLE ***
*** P3 — a pure-public turn with NO planner performs NO read, valid raw query or not ***
*** P3 — ⛔ the public read still executes: MIXED gets BOTH worlds ***
*** P3. ⛔ THE SERIALIZED RECORD CARRIES NEITHER THE KEY NOR THE VALUE ***
*** P4 — nothing leaks into telemetry either ***
*** P4. windowChanged IS DERIVED SERVER-SIDE AND NEITHER TITLE IS KEPT ***
*** P4b. AND null STAYS DISTINCT FROM false ***
*** P5 — fail closed: a planner that is ${name} produces NO public read ***
*** P5. ⛔ observer-result.json IS NOT A PERMANENT RECORD ***
*** P5b. THE RETENTION TAXONOMY COVERS THE OBSERVER ARTEFACTS ***
*** P5c. AND THE SWEEP DELETES RAW CONTENT WHILE KEEPING RECORDS ***
*** P6 — ⛔ a failed plan NEVER falls back to the main model query ***
*** P6. NO OBSERVATION CAPABILITY IS ENABLED BY THIS TRANCHE ***
*** P7 — a failed plan creates NO EvidenceSet and no trust state ***
*** P7/P8 — with NO internal evidence the planner STILL owns the outbound words ***
*** P8b — one plan per turn, even across two public attempts ***
*** P9 — a value the OWNER typed himself may still travel ***
*** PARALLEL_WRITERS_REAL_OVERLAP — writers that are genuinely running at once lose nothing ***
*** PERSONA_IDENTITY was not touched, and did not need to be ***
*** POSITIVE CONTROL — a blank rendering would be caught ***
*** POSITIVE CONTROL — keying the rule to expiresAt would have refused ***
*** POSITIVE CONTROL — ordinary settings that MENTION limits still save ***
*** POSITIVE CONTROL — the old rule would have omitted the non-citing reply ***
*** POST /api/v1/demo/intake is refused without auth (it spends money) ***
*** POST /api/v1/intake is refused without auth (it spends money too) ***
*** POST saves, and the answer says when ***
*** Phase 3a cannot create the approved test folder ***
*** Q — an UNAVAILABLE model-directed read does NOT activate Truth Closure ***
*** Q — the public operation is read-shaped and exposes no action ***
*** Q — ⛔ A4 OFF: an ASK behaves exactly as it always did ***
*** Q — ⛔ a REFUSED internal read does not satisfy the internal world ***
*** Q/R — ⛔ a worker-selected public read still goes through the Owner-only planner ***
*** R — A4 off: no A4 wording reaches the system prompt ***
*** R — no injected verifier while enabled ⇒ ASK, never a silent skip ***
*** R — ⛔ an unusable verifier on an initial ASK invents nothing and reads nothing ***
*** R — ⛔ the default READ bound is still exactly 3, plus one reserved compose ***
*** R/S/T — require_mixed: refused until BOTH worlds are live ***
*** R1. ⛔ AN APPROVAL ID CANNOT BE REUSED AFTER close() ***
*** R2. ⛔ NOR AFTER IT EXPIRES ***
*** R3. ⛔ A DIFFERENT ORDER REUSING AN ID IS DISTINGUISHED, AND STILL REFUSED ***
*** R3b. THE DRY-RUN SEAM IS PRESERVED — singleUse:false is deliberate ***
*** REACH (browseOrder) — the ORDER cannot be supplied or widened by a caller ***
*** REACH (session) — ⛔ R3.1: the credential path is UNREACHABLE, not merely unused ***
*** REACH — free text yields an OFFER, never an action ***
*** REACH — near-misses do not trigger, and a purchase is REFUSED not downgraded ***
*** REACH — the acceptance utterance is detected deterministically, with no model ***
*** REACH — ⛔ a GENERIC shop word resolves to no site, and never picks one ***
*** READ_ACCESS off → readContextUsed is FALSE, so the reply is storable ***
*** RENAMED: she is 香香. Every retired name must stay retired ***
*** RENDERER — a blocked read says we never got there, and claims nothing ***
*** RENDERER — an unpriced product is reported as unpriced, not as a price ***
*** RENDERER — ⛔ ANY LINE CARRYING A PRICE CARRIES ITS PROVENANCE IN THE SAME LINE ***
*** RENDERER — ⛔ THE CEILING: mayAssertClaim is false, and it is false ALWAYS ***
*** RENDERER — ⛔ every qualifier comes from a DESCRIPTOR FIELD, not a hand-written caveat ***
*** RENDERER — ⛔ 「搵唔到」 is said, and 「冇貨」 is refused, IN THE WORDS ***
*** S — an ASK question carrying implementation vocabulary is replaced ***
*** S — one invocation per obligation, never a loop ***
*** S — shadow does not touch routing, and adds no model call ***
*** S — the caller may widen READS to 4, and no caller may exceed the ceiling ***
*** S — zero automatic read before the model decision, unchanged ***
*** S — ⛔ a rejected ASK never reaches the Owner through the fallback render ***
*** S-A — a successful operation is withdrawn from OPEN and described as READ ***
*** S-B — a successful ZERO-RESULT read is LIVE, not unavailable ***
*** S-C — a model-directed read whose connector FAILS is never called retrieved ***
*** S-D — the same semantics for an AUTOMATIC Aroma read that fails ***
*** S-E — a source nobody asked stays OPEN: notAsked is neither read nor failed ***
*** S1. ⛔ TWO PROOFS IN THE TURN, AN INVENTORY-ONLY SECTION IS STILL ENTITLED ***
*** S10. LEGACY ROWS WITH NO SERVER readKey KEEP THE OLD FALLBACK ***
*** S11. SINGLE-PROOF TURNS BEHAVE EXACTLY AS BEFORE ***
*** S12. ⛔ A PROOF WHOSE OPERATION OWNS NO SINGLE GROUP ENTITLES NOTHING ***
*** S13. ⛔ A SECTION WHOSE METRIC IS NOT ITS OWN PROOF\
*** S14. ⛔ IDENTITY CONTINUITY: retrieved row → validated item → binding → the reply ***
*** S15. THE LOG STILL CARRIES ENUM AND COUNT ONLY ON A REFUSAL ***
*** S16. ⛔ THE SENTENCE GATE IS BYTE-IDENTICAL — not stricter either ***
*** S2 — end to end: ONLY a mixed turn gets the fourth decision ***
*** S2 — one world alone never releases a mixed obligation ***
*** S2. ⛔ AND THE REPLENISHMENT SECTION BINDS TO ITS OWN PROOF, NOT THE FIRST ONE ***
*** S3. ⛔ METRIC CANNOT IDENTIFY A PROOF — two reads can share one ***
*** S4. ⛔ A SECTION MIXING TWO OPERATIONS IS REFUSED WHOLE ***
*** S5. ⛔ NO PRUNING TO THE MAJORITY, EVEN WHEN ONE OPERATION DOMINATES ***
*** S6. THE MODEL-FACING SECTION SCHEMA EXPOSES NO readKey ***
*** S7. ⛔ A FORGED readKey ON THE MODEL ITEM HAS ZERO EFFECT ***
*** S8. ⛔ AND A FORGED readKey CANNOT RESCUE A MIXED SECTION EITHER ***
*** S9. ⛔ TWO PROOFS: THE SECTION IS ALLOWED, THE SENTENCE IS STILL REFUSED ***
*** SIR3 — every first-READ path resolves source intent first ***
*** SIR3 — mixed + first INTERNAL read: internal executes, public stays outstanding ***
*** SIR3 — mixed + first PUBLIC read: public executes, internal stays outstanding ***
*** SIR3 — the mixed verifier can no longer establish a world, even when wired ***
*** SIR3 — the resolver is not re-asked when FinalKnowledge later demands more ***
*** STATIC — no write surface was added by this repair ***
*** STATIC — only public_knowledge gets an extended read bound ***
*** STATIC — the planner still knows nothing about connectors or evidence ***
*** STATIC — the public planner is NOT gated by internal evidence ***
*** STRUCTURAL_CORRUPTION_LOUD — a valid-JSON but wrong-shape store is never read as empty ***
*** T — A4 ON + chat still cannot output commit ***
*** T — no beforeFinal: behaviour is identical ***
*** T — no chain-of-thought field, and no knowledgeRoute label, was added ***
*** T — the gate telemetry is content-free ***
*** T — ⛔ a rejected FINAL never reaches the Owner either ***
*** T/U — after the worker\
*** THE CARVE-OUT: a title the Owner himself typed is not laundering ***
*** THE COST OF CLAUSE-SCOPING, recorded rather than hidden ***
*** THE FLAG IS NOT SUFFICIENT — on, with no order, unlocks nothing ***
*** THE LANE GUARANTEE: only the proposal shape can ever carry promoteToProposal ***
*** THE LIVE FAILURE: a capability question renders no rows and no omission note ***
*** THE LIVE FAILURE: a metric value the model mistyped is corrected, not deleted ***
*** THE LIVE FAILURE: a recalled item name never reaches the Owner ***
*** THE LIVE FAILURE: a source name as sourceId is still dropped — the check is NOT relaxed ***
*** THIS process — the real one running these tests — is not pointed at production ***
*** TURN_ROUTER defaults to on ***
*** TURN_ROUTER=off keeps pre-router behaviour — a live rollback target ***
*** TURN_ROUTER=off → not a single shadow line ***
*** TURN_ROUTER=shadow keeps Step 2 behaviour — reads are NOT governed ***
*** U — the A4 semantic guidance is byte-identical to the A4-1R checkpoint ***
*** U — ⛔ a hook that throws or returns nonsense does NOT release the answer ***
*** U — ⛔ no hook at all: legacy loop behaviour is identical ***
*** U — ⛔ the DEFAULT read bound is still 3, and the ceiling is explicit ***
*** U1 ask: a clarifying question with a null draft is byte-identical ***
*** U1 draft with a null recipient is byte-identical ***
*** U1 draft: the parsed envelope is byte-identical to today ***
*** U2 — a refusal becomes an ordinary observation the model can act on ***
*** U2 — an ordinary A4 turn with no obligation still gets 3 ***
*** UNDER ROUTING the hostile content never ARRIVES — the stronger guarantee ***
*** UNREADABLE_CLAIM keeps BOTH spellings — this one goes blind silently ***
*** UNREADABLE_EXISTING_FILE_FAILS_LOUD — every existing-but-unreadable file uses ONE contract ***
*** UTILITY is exempt — its numbers are computed, not claimed ***
*** V — A4 OFF offers no public capability even when injected ***
*** V — a SINGLE-world obligation keeps the bound at 3 ***
*** V — the semantic guidance is byte-identical (no fifth calibration) ***
*** V — ⛔ beforeFinal and beforeTerminal are the SAME seam ***
*** V — ⛔ no write path, and no capability vocabulary at all ***
*** V — ⛔ with A4 OFF the worker never runs ***
*** V2 — beforeTerminal takes precedence when BOTH are supplied ***
*** W — a MIXED obligation from an initial FINAL may use 5, and no more ***
*** W — the default read bound is still 3 ***
*** W — ⛔ no chain-of-thought field, in the schema or anywhere near it ***
*** W — ⛔ no write path, and no provider anywhere in the module ***
*** WORLDS_FOR is total over the enum and frozen ***
*** WRITE_RE accepts these names, and would refuse a write-shaped one ***
*** WRITTEN WHERE IT WILL BE SEEN: what this half cannot catch ***
*** X — MIX1 first-READ turns keep their existing max-4 behaviour ***
*** X — a single-world obligation from a suppressed ASK keeps 3 ***
*** X — ⛔ no keyword classifier, no domain rule, no holdout string ***
*** X — ⛔ the schema is closed, portable, and carries no reasoning or query field ***
*** Y — Owner-only egress provenance is untouched by mixed turns ***
*** Y — a mixed obligation from a suppressed ASK may use 5, and no more ***
*** Y — no keyword routing, no domain noun, no holdout sentence ***
*** Y — the OLD gate flag no longer governs anything ***
*** Y — ⛔ A4 OFF: the gate does not exist, byte for byte ***
*** Z — semantic guidance SHA is unchanged ***
*** Z — the wiring pins the exact measured model, and does not touch the global default ***
*** Z — with no mixed verifier wired, the resolver still governs the world ***
*** Z — ⛔ no keyword or phrase matcher decides any of this ***
*** a 401 disables Save and says NOT SIGNED IN, not 「read failed」 ***
*** a Cantonese request still has its politeness prefix stripped ***
*** a DIFFERENT date is still rejected — this is normalisation, not relaxation ***
*** a DIFFERENT model id produces a different record — the value is read, not assumed ***
*** a DIFFERENT time or date is still refused ***
*** a Drive doc and a Decision saying "approved, execute now" reach NO dispatch — GPT served ***
*** a FALLBACK never reaches the main result — and Gmail is now wholly out of scope ***
*** a HYPOTHETICAL never fires ***
*** a NEGATED instruction never fires ***
*** a NON-Aroma intent asks Aroma System for NOTHING ***
*** a NON-commit turn records WHY, instead of leaving a bare sentence ***
*** a PARTIAL number still fails — exact equality is not relaxed ***
*** a QUESTION never fires ***
*** a REFUSED dry-run is audited too — an attempt is never silent ***
*** a REPORT of a change already made never fires ***
*** a ROUTED email takes the identical U1 path as an explicitly chosen one ***
*** a STRIPPED-ENV node child DOES reach production — the honest limit ***
*** a SUBSTRING of prose still does not match — only the digit run is extracted ***
*** a THROWING audit sink fails the dry-run — no record, no result ***
*** a TITLE is never an identifier ***
*** a bare aroma_system is NOT itself an operation ***
*** a bare verb is not an instruction ***
*** a binding whose target is already dead must NOT count as demonstrated ***
*** a broken or absent source produces NO block, not a half one ***
*** a broken settings file cannot break a conversation ***
*** a business question is NOT captured by UTILITY ***
*** a business question is untouched — it still reads, with the right method ***
*** a business question with rows DOES get the plan schema ***
*** a business route with NO rows still demands no plan ***
*** a calendar entry title counts too ***
*** a candidate is created as a candidate, and is readable back ***
*** a capability question performs ZERO connector calls ***
*** a chat turn that READ items sends responseFormat on the outbound request ***
*** a child that inherits the environment is still redirected ***
*** a citation with NO url still fences the region it governs ***
*** a claim naming NO evidence source is unverified ***
*** a claimed outcome that is not a real outcome becomes null, not the claim ***
*** a clean dry-run never claims the file exists or that it would succeed ***
*** a click confirms, then reverts ***
*** a client-supplied file is IGNORED, not honoured ***
*** a coerced mode is marked; behaviour is UNCHANGED ***
*** a complete YYYY-MM-DD, from ISO and from an RFC 5322 mail header ***
*** a completed chat turn is appended to its conversation ***
*** a completed turn is appended and survives a reload ***
*** a concept the EvidenceSet does not assert is never suppressed ***
*** a contaminated baseline makes later hits unattributable ***
*** a conversation that SPANS the change still matches across spellings ***
*** a correct SUBSEQUENCE is a legitimate ranking — a top-2 need not carry the tail ***
*** a corrupt line is skipped; the rest of the memory survives ***
*** a date is not a subtraction ***
*** a decimal is NOT treated as a separated-digit sequence ***
*** a decomposer that fails is a refusal, never a guessed plan ***
*** a demo router built with NO store does not write to real data ***
*** a derivation is kept even when four ordinary facts already fill the item ***
*** a derivation whose inputs are missing on the row is dropped, not guessed ***
*** a different model on the same provider is visibly different ***
*** a dropped fact records a REASON, and now the value too ***
*** a dry-run consumes its own step nonces, so it cannot be replayed ***
*** a dry-run step is NEVER recorded as ok — nothing happened ***
*** a fact whose value is NOT in the row is still dropped — the check is unweakened ***
*** a failed append emits exactly one allowlisted line ***
*** a failed append never costs the Owner his reply ***
*** a failed audit write never breaks the approval itself ***
*** a failed persist never lets the model\
*** a failed read disables Save ***
*** a failed reject says so rather than claiming success ***
*** a failed turn does not leave it spinning ***
*** a fallback CANNOT occur without a log line ***
*** a fallback to Claude with context reports TRUE — the case a flag guess gets wrong ***
*** a false read claim in the SHOWN answer is still corrected ***
*** a file with no instruction asks ONLY about the change ***
*** a filtered limitation is COUNTED and RECORDED ***
*** a finished run reports correctly AFTER its sealed order has expired ***
*** a forbidden file is named, with the reason ***
*** a fresh login is ok, even though the ACCESS token expires in hours ***
*** a garbage return value is unavailable, not chat ***
*** a gated action reaching the OBSERVER is refused, order or no order ***
*** a genuine chat is NOT marked — the two cases must not look alike ***
*** a genuinely unavailable source may be reported as unavailable ***
*** a good read ENABLES Save — the guard must not lock the page permanently ***
*** a judgement needs the Owner AND a bounded, redacted reason ***
*** a lesson with nothing to say is not a lesson ***
*** a limitation carrying a count the evidence does not own is per-turn, and stays ***
*** a live calendar + a reply claiming 讀唔到 is corrected before it leaves the service ***
*** a lock held by a DEAD process is broken, not waited on forever ***
*** a lock lost mid-critical-section is refused at commit, never written through ***
*** a long limitation is shaped, never reproduced ***
*** a long or spaced value is SHAPED, never reproduced ***
*** a long or spaced value is still shaped, never reproduced ***
*** a long turn is truncated with a marker, not silently cut ***
*** a lookup he asked for that legitimately found nothing to say still gets the record ***
*** a malformed timezone DECLINES, through the router, rather than answering wrongly ***
*** a message matching one intent counts one — the common case is not inflated ***
*** a message naming BOTH is treated as the system ***
*** a message that DOES carry intent plans a read, so the planner itself works ***
*** a message that is not a request creates NOTHING ***
*** a metering failure is fail-open; a Decision failure is NOT ***
*** a metric value the model got WRONG is replaced by the row\
*** a mixed list yields ONLY the validated ones ***
*** a mutation on a corrupt store refuses too, and still writes nothing ***
*** a name that is NOT in the description is still rejected ***
*** a network failure disables Save ***
*** a no-intent message still reads NOTHING automatically — notAsked survives ***
*** a no_target_window result is INVALID, not "the owner window is absent" ***
*** a non-business route with a named internal source does not qualify ***
*** a non-clarify decision may not smuggle a question ***
*** a non-metric fact still goes through valueMatches, unrelaxed ***
*** a nonce is bound to its POSITION — step 2\
*** a normal append leaves no temp debris ***
*** a number that came from a READ row is sourced ***
*** a number the OWNER supplied is sourced, and is not a finding ***
*** a one-character paraphrase is scored as one ***
*** a paraphrase reusing a distinctive noun still counts as citation ***
*** a patch is written, and the bytes are the patch ***
*** a phone number INSIDE a segment is now indexed ***
*** a phone number from the event DESCRIPTION is now verifiable ***
*** a phone number matches across separator styles, and only across separators ***
*** a plan echoing the ref validates ***
*** a plan naming one operation reads only that operation\
*** a plan over the fact bound is REFUSED, never quietly truncated ***
*** a plan whose every item is unprovable does NOT report success ***
*** a plan with no answerClaims yields NO bindings — UNBOUND, not guessed ***
*** a polite request phrased as a question is REFUSED — and that is the safe side ***
*** a present-but-not-a-string timezone THROWS ***
*** a protected path creates nothing ***
*** a protected path never offers ***
*** a provider UNAVAILABLE does not become an answer with evidence ***
*** a read request still creates NOTHING — the safe direction is unchanged ***
*** a read that throws does not leak its message into the observation ***
*** a reader never observes an empty or partial store while a writer runs ***
*** a reader never sees "no such conversation" while a writer is appending ***
*** a real number with an invented unit is still an invention ***
*** a real reply passes through byte-identical ***
*** a real requestId survives, because correlation is the point ***
*** a recall failure is fail-soft but never silent ***
*** a refresh token inside the warning window runs, but says so ***
*** a refused save is 422 with a readable reason, not 500 ***
*** a rejected value is now ON the drop record ***
*** a relative date carries the timezone exactly as today does ***
*** a result carrying raw content is REFUSED, not silently trimmed ***
*** a result with NO provider field is still attributed correctly ***
*** a row_local claim naming a retrieved row VERIFIES ***
*** a row_local claim with NO sourceIds is unverified — it is not row-local at all ***
*** a run that changed nothing says so, and writes no empty file ***
*** a runaway patch is refused rather than written ***
*** a saved switch takes effect on the next turn, not the next restart ***
*** a second order is REFUSED while one is live — no queue ***
*** a second order is refused while one is live — there is no queue ***
*** a section that EXISTS must carry an item, and the choice is declared ***
*** a section that loses SOME items states the omission rather than shrinking silently ***
*** a sentence carrying a number is dropped — the server owns the figures ***
*** a sentinel below the detection floor makes its absence meaningless ***
*** a set_scoped claim MATCHING the evidence scope verifies ***
*** a set_scoped claim without a structural scope is unverified ***
*** a settings router built with NO save does not write the real settings file ***
*** a short reply NEVER escalates — not into proposal, never into execution ***
*** a short reply continues the previous turn instead of arriving as fresh input ***
*** a short reply still creates NOTHING — no decision, task, proposal or dispatch ***
*** a single-item ranking section cannot be out of order ***
*** a source named in a DIFFERENT clause does not anchor the claim ***
*** a source read but NOT live cannot carry a binding ***
*** a source that was NOT ASKED is distinguishable from one that is empty ***
*** a source that was not authorised produces no operation ***
*** a source with no audited limit is limitKnown:false, and stays unknown throughout ***
*** a source with no status concept gets NO status segment ***
*** a status claim with no number at all is still caught ***
*** a step nonce burns on FIRST use — replay and double-submit both refused ***
*** a step outside the approved root never reaches step resolution at all ***
*** a stock claim the evidence cannot support is removed ***
*** a stop is FIRST-WINS — the record says what actually stopped it ***
*** a stopped run unlocks nothing — the 3a binding is not weakened ***
*** a store failure never takes the answer away ***
*** a superlative question is recognised by SHAPE, not by a topic vocabulary ***
*** a test process with no AROMA_DATA_DIR does NOT get the production dir ***
*** a three-character term is NOT separated — the phenomenon is specific to pairs ***
*** a timeout and a network error are READ_FAILED, never a throw ***
*** a title supplied by a caller does NOT survive into the record ***
*** a to-do from the description is verifiable, whole or in parts ***
*** a traversing id can never escape the conversations directory ***
*** a traversing id is refused by the route, not just by the store ***
*** a true claim about a FIELD does not draw a correction ***
*** a turn that DID read is left alone — this guard owns only the empty case ***
*** a turn that DID read is out of scope, even with one source ***
*** a turn that retrieved items MUST emit an [AROMA-ANSWER-PLAN] line ***
*** a utility turn is stored in conversation history like any other ***
*** a utility turn reads nothing and never reaches the model ***
*** a valid IANA name is used as written ***
*** a valid candidate carries the six canonical fields and starts as candidate ***
*** a valid development request is untouched ***
*** a valid service token IS admitted even when no Owner password is set ***
*** a valid timezone saves and is then what localTime reads ***
*** a value the describer refused never appears in the log either ***
*** a well-formed store still round-trips through the validator untouched ***
*** a write that cannot be serialised THROWS rather than clobbering ***
*** a written-Chinese scope note is suppressed on repeat, same as the Cantonese one ***
*** a zero-node UIA read is INVALID, and once was not ***
*** absent evidence is not entitlement — fails closed ***
*** active → 啟用中, and a raw enum never reaches the screen ***
*** after a fallback, provenance names the provider that ACTUALLY answered ***
*** all EIGHT lifecycle types are accepted ***
*** all five conditions together, and then it unlocks ***
*** allIntentsFor keeps what intentFor discards ***
*** allowedPath must be EXACTLY the one directory ***
*** ambiguity is RESIDUAL: the guidance orders the check and says so ***
*** ambiguous → ASK, and ZERO reads ***
*** an ABSENT field is left alone — the accept is specific to empty-string, not to omission ***
*** an ACTION still authors, and the task passes through verbatim ***
*** an AUDITED-UNBOUNDED endpoint is establishable — it is not the unknown case ***
*** an EMAIL-DRAFT-shaped envelope is projected the same way ***
*** an EXPIRED access token with a live refresh token still runs ***
*** an ORDINARY section is untouched by entitlement ***
*** an Owner note on an outcome is bounded and redacted before write ***
*** an UNAVAILABLE operation is never described as retrieved ***
*** an UNDECLARED derivation is still dropped ***
*** an UNMAPPED status is never dropped — 狀態未確認 plus the raw value ***
*** an UNREADABLE conversation throws — it is not "not found" ***
*** an UNREADABLE settings file THROWS — it is not "no settings yet" ***
*** an UNWIRED store is reported, not mistaken for a save ***
*** an abbreviation that RESOLVES is still rejected ***
*** an absent necessity is REQUIRED — a plan that forgets to say must not shrink ***
*** an absent usage block stays null — it is not a zero ***
*** an action with no Chinese label is REPORTED, never dropped ***
*** an address, a URL or a path is never carried, however short ***
*** an already-read operation is stated as READ, never left to look missing ***
*** an approval event records who, when, and the work order hash ***
*** an archive containing ONLY the current conversation yields nothing ***
*** an automatic read can be recorded as the operation it was ***
*** an empty UIA artefact is INVALID even if a node count is reported ***
*** an empty {} DOES clear the persisted record of what he chose ***
*** an empty {} does NOT wipe the running config — applyFlags only ever writes ***
*** an endpoint with no window says so explicitly, rather than omitting queryScope ***
*** an engine that reports no calls yields [] — never a fabricated entry ***
*** an expired refresh token REFUSES, and says exactly how to fix it ***
*** an explicitly injected store DOES write — the inversion is the default, not the feature ***
*** an id or a link is citation, and cannot occur by chance ***
*** an image carrying ONLY the own signature passes ***
*** an image carrying the OWNER signature adjudicates CONTAINMENT-FAILURE ***
*** an inert implementation that is not exported cannot be guarded, and fails ***
*** an instruction with no file asks ONLY about the file ***
*** an invented capability executes zero reads through the real path ***
*** an invented or write-shaped operation resolves to NOTHING ***
*** an inventory question still renders real data ***
*** an invoice question reads aroma_system and NOT Gmail ***
*** an invoice question shows ONLY invoice evidence — five sources in, one out ***
*** an obligation is not claimed for a world that cannot be reached ***
*** an obligation settles the meaning: neither other gate is re-asked ***
*** an omitted assistant turn is rendered as an explicit absence ***
*** an omitted record\
*** an operational number with nothing read is withheld, and said so ***
*** an order that does not contain the action does not authorise it ***
*** an order that omits ANY required prohibition is refused ***
*** an ordinary conversational turn reads nothing ***
*** an ordinary text response is unchanged ***
*** an ordinary turn is UNCHANGED — A′ did not quietly stop storing replies ***
*** an ordinary turn that finishes early is unchanged — no reserved call ***
*** an orphan lock is left alone rather than deleted by a non-owner ***
*** an outcome is recorded once, bounded, and changes NO confidence ***
*** an unattributed claim is no longer "generic" — it stays silent ***
*** an unchanged rate is not news ***
*** an unconfigured provider is unavailable, not empty ***
*** an unconfigured server cannot be logged into at all ***
*** an unknown IANA name THROWS — it does not become UTC or the OS zone ***
*** an unknown future state is excluded BY DEFAULT ***
*** an unknown key is still rejected, with reason unknown_key ***
*** an unknown switch or a bad value is refused ***
*** an unknown total is stated as unknown — never as the shown count ***
*** an unknown type is REFUSED, not silently written ***
*** an unmatched message calls the connector ZERO times ***
*** an unmatched message yields notAsked — NOT a plan and NOT unavailable ***
*** an unparseable calculation falls to CONVERSATION, it does not answer ***
*** an unreadable response is unavailable, and says so distinctly from a timeout ***
*** an unrecognised claimKind is UNVERIFIED, not coerced to a known kind ***
*** an unrecognised decision TYPE is refused, not guessed ***
*** an unrecognised value resolves toward READING LESS, not more ***
*** an unrelated proper noun is NOT hers ***
*** an unsafe approvalId cannot steer the filename ***
*** an unsealed, unapproved or unhashed order unlocks nothing ***
*** an unsupported sentence is dropped; the verified row is KEPT ***
*** an untitled source keeps a usable label, and the FIELD still tells the truth ***
*** an unwritable directory is reported, never thrown ***
*** and NOT when the totals are known but differ ***
*** and \
*** and a business question with rows DOES get the plan schema ***
*** and a superlative over an UNRANKED source is not refused either ***
*** and an UNRESTRAINED clarify still reports verdict_clarify ***
*** and an ordinary raw sentence still passes with no detail ***
*** and demands NO Answer Plan — no rows, and not a business route either ***
*** and end to end, 「5磅是多少公斤？」 reads nothing ***
*** and it is REPLACED by a positive default, not merely deleted ***
*** and it keeps the BROADER approval claim — the placeholder\
*** and it says explicitly that this is NOT a read failure ***
*** and it still contains NO read-failure phrase for the guard to contradict ***
*** and neither do its neighbours — the arbitrary line is gone ***
*** and no hash of it either — a low-entropy string hashes to a crackable value ***
*** and no switch exists for a source that is not registered ***
*** and she does NOT claim the system was unreadable ***
*** and so does the phrasing that already worked — one behaviour ***
*** and the correct top-four in the proven order is ALLOWED ***
*** and the durable approval trail records the entry point ***
*** and the false-positive class STAYS closed ***
*** and the heading goes with it — no empty 資料限制 block ***
*** and the rule still means what it meant ***
*** and to say so when memory has nothing ***
*** answerClaims is REQUIRED + NULLABLE — optional semantics, strict-mode safe ***
*** app.css gained exactly the approved rules ***
*** applyFlags touches ONLY the keys the Owner set ***
*** aroma_system expands to exactly its six views ***
*** at shadow a real turn emits a line that actually populated ***
*** below the cap IS establishable — the rule must not be uselessly conservative ***
*** below the cap, the count IS the matching total ***
*** both files carry the exception and its reasoning at the top ***
*** both metrics survive — this is the six-drop turn ***
*** buildReadContext skips the source before connector.read() is called ***
*** but a set_scoped claim on a truncated source is NOT verified ***
*** but ☰ SURVIVES — it is the only way back to a collapsed sidebar ***
*** chatter yields no file and no intent ***
*** citesEvidence:false may not name a retrieved row in prose ***
*** citesEvidence:false with sections supplied — the sections are dropped and logged ***
*** claimKind is a CLOSED enum — a model cannot invent a fourth kind ***
*** clarify on a NON-established route returns mode=ask with zero reads and no tool talk ***
*** clicking one loads the FULL transcript ***
*** coercion also warns, naming the field ***
*** completeness travels, and the model is told which number is the total ***
*** confidence range is checked, and a value without a basis is malformed ***
*** consulted sources ENRICH a cited row; they never manufacture one ***
*** continuation 「兩邊都睇。」 → both, no repeated clarification ***
*** continuation 「市場。」 → public only ***
*** continuation 「我哋供應商。」 → internal only ***
*** corrected now (was missed):
*** crash debris is swept by the next successful write ***
*** dark mode overrides the same names, so every rule follows it ***
*** decomposeGoal: one call, schema-forced, usage carried verbatim ***
*** defaults and coercions are frozen ***
*** delete asks first ***
*** delete is hover-revealed, keyboard-reachable, and still asks ***
*** delete removes it, and deleting twice is not an error the Owner sees ***
*** delete removes the file and the listing ***
*** delete still removes an omission record ***
*** demoRouter records the decision — it is not computed and forgotten ***
*** deploy-companion.ps1 stages the DERIVED list, not a hand-written one ***
*** derivations have their OWN cap, and it is declared ***
*** droppedItems is counted separately from droppedFacts ***
*** droppedLimitations reaches the log line ***
*** each business question reads exactly its own one source ***
*** each operation resolves to its one frozen method ***
*** each source keeps its own paragraph, and the order is fixed ***
*** each switch carries the Owner-facing name from the one label table ***
*** either check refusing is enough ***
*** empty -> NO_RELEVANT_RESULTS, and that is NOT a failure ***
*** enabled: only commit is removed, and the canonical ORDER is preserved ***
*** end to end, a broken resolver reads nothing and asks ***
*** every Aroma routing intent has exactly one operation, and vice versa ***
*** every arithmetic refusal survives ***
*** every assistant message carries a copy control ***
*** every boundary-removing phrase is refused, and the phrase is quoted back ***
*** every business intent still returns exactly the method it always did ***
*** every constraint the descriptions expressed is still expressed ***
*** every corpus case gets the verdict it should ***
*** every dry-run writes a computer-audit record, marked as a dry-run ***
*** every endpoint is present in ROW_SHAPE, QUERY_SCOPE and SERVER_LIMITS ***
*** every escape from the root is refused ***
*** every execution action is on the VISIBLE FACE, not behind 詳細 ***
*** every existing counter is unchanged by a claim declaration ***
*** every forbidden action in the SEALED order reaches the card ***
*** every forbidden action is covered by one of the two kinds ***
*** every line carries a DATE, so any claim can be sourced ***
*** every negation is refused — the class that matters most ***
*** every other A4 semantic prompt is unchanged ***
*** every other failure is READ_FAILED with a short reason ***
*** every phrasing that worked before still routes exactly where it did ***
*** every registered source has a settings switch — including aroma_system ***
*** every registered source has aliases and a label — none may be missing ***
*** every registry source has a builder — including the new one ***
*** every rendered row carries ref=<source>#<id> in the outbound prompt ***
*** every request carries a timeout signal ***
*** every request is a GET, and the method is a constant ***
*** every source that returned rows gets a line — order does not decide survival ***
*** every unreadable state refuses — the Owner\
*** everything read and nothing left still says what is held ***
*** evidence is metadata ABOUT content, never content ***
*** evidence older than 7 days is actually deleted — demonstrated, not promised ***
*** exactly ONE question, and never 「A 定 B」 ***
*** exactly at the boundary is still a warning, and outside it is silence ***
*** exactly at the cap is fine ***
*** facts[] never consult claimBindings ***
*** field tiers after the capture — the four states 「CANDIDATE」 was hiding ***
*** field-set changes ALARM, because no threshold exists to be chosen ***
*** first run is legitimately empty ***
*** focus is still VISIBLE — removing the colour must not remove the indicator ***
*** generic sources keep their own names and are unchanged ***
*** greeting and composer, nothing else ***
*** hardcoded colour: nothing can repaint or invert it ***
*** her section lands AFTER the data and BEFORE 下一步 ***
*** her words never carry item detail ***
*** history is used only when the message names nothing ***
*** historyTextOf reads the field the client actually sends ***
*** hostile retrieved content that ARRIVES reaches no dispatch — both providers ***
*** icon only — the name and every string are unchanged ***
*** ids and short enums only — never a goal, a file or a reply ***
*** if the promotion fails, ok is false and no proposalId is invented ***
*** image bytes, screen text and credentials are REFUSED, not stripped ***
*** installing fetches nothing new: the icon is inline, not a URL ***
*** internal telemetry is stripped from every field it can appear in ***
*** internal → internal only ***
*** invalid lifecycle transitions fail closed ***
*** it DOES pass when coverage is structurally established ***
*** it adds TYPES to the existing events stream, not a collection ***
*** it cancels only the proposal bound to THIS sealed order ***
*** it cancels the PROPOSAL, not just the sealed order ***
*** it catches a refusal the first check would miss ***
*** it disappears the moment the first message is sent ***
*** it does not read her phrasing — an invented denial is caught too ***
*** it forwards app.agentAuditConfigured — no second computation ***
*** it goes through withLock — the same critical section as everything else ***
*** it is DETERMINISTIC — no model, no network ***
*** it is a pure function of the Owner\
*** it is audited — a governance action that leaves no trace is the same fault ***
*** it is cleared in the terminal step that runs after success AND failure ***
*** it is loopback-gated — and the WRITE surface still refuses anything but POST ***
*** it is published as SERVER-COMPUTED, not as something she may write ***
*** it never evaluates anything but arithmetic ***
*** it never rewrites a line and never adds one ***
*** it shares the producer\
*** it sits in the same footer row as the attribution ***
*** lastClaimIn segments on STRUCTURE, and never inside a number ***
*** live and unavailable are described as the two different things they are ***
*** load() on an unreadable file THROWS — it never reports "empty" ***
*** loaded is set after a transcript is in the thread, not before the fetch ***
*** malformed input is still rejected on this path ***
*** matchingTotal and sourceTotal are never conflated ***
*** memory and evidence are marked distinctly in the outbound prompt ***
*** midnight is exactly midnight — no millisecond remainder ***
*** mixed → both worlds, and either may be read first ***
*** mixed: each missing world gets its OWN single invocation ***
*** mode, clarificationReason and modeCoerced reach the outcome line ***
*** mode=commit + one task reaches a PROPOSAL — steps 3 to 5 ***
*** model comes from the ADAPTER RESULT, not from null and not from a guess ***
*** model_estimate is a permitted CANDIDATE basis and nothing more ***
*** naming a SOURCE is not citing its CONTENT ***
*** naming an item THIS turn actually retrieved is still allowed ***
*** neither signature may sit on the Windows console palette ***
*** never guess gender ***
*** newest validated first, ties broken deterministically by id ***
*** no 500-entry cap on the durable trail ***
*** no ASCII punctuation between two Chinese characters — the card ***
*** no ASCII punctuation between two Chinese characters — the refusals ***
*** no Owner context → not_mixed without a call ***
*** no Owner text at all → ask, and no call is spent ***
*** no Phase 1 module can touch a disk, a process, a network or a device ***
*** no Supervisor exists in the running application — it is constructed by nobody ***
*** no Windows account was created, and the definition says so as data ***
*** no adapter may silently ignore responseFormat ***
*** no archive file at all → no block, no throw ***
*** no authorised capabilities at all → cannot route, no call spent ***
*** no block, no reply, empty, or a broken block → CITES (omit) ***
*** no capability in the register is unconditionally enabled ***
*** no capability is UNCONDITIONALLY enabled ***
*** no disc behind the dot — the dot IS the avatar ***
*** no followUp means the answer ends without a question ***
*** no generated operation is write-shaped — the loop would refuse it ***
*** no inert WRITE returns a success-shaped value — it throws ***
*** no intent match returns NULL, not listInventory ***
*** no intent names more than one source ***
*** no link is invented ***
*** no match is an empty array, never null ***
*** no match reads NOTHING, and latin terms match whole words only ***
*** no message, reply, or raw error text reaches the log ***
*** no method or path from outside the table can ever be named ***
*** no module still computes the data dir for itself ***
*** no new CSS was needed — the control reuses existing styles ***
*** no new dependency was introduced ***
*** no password configured ⇒ everything is refused, nothing is served openly ***
*** no read context → the reply IS stored, exactly as before ***
*** no retired name appears on any user-facing surface ***
*** no retired name is left in anything the Owner READS ***
*** no route factory defaults a WRITER to a process-wide real instance ***
*** no rule outside :root restates a font size, except icon glyphs ***
*** no same-round baseline makes every zero result meaningless ***
*** no schema description is written in Cantonese ***
*** no settings file at all → the default, not an error ***
*** no stale correction: the guard judges what is SHOWN, not discarded prose ***
*** no test may call the answerer without routing first ***
*** no test spawns a node child with an env that ERASES the detection ***
*** no token value appears anywhere in the result, in any state ***
*** no two rows may share an id ***
*** node --test is detected by the variable NODE ITSELF sets ***
*** none of the false triggers produce an offer ***
*** nothing disappears silently — the hidden count is real ***
*** nothing in the application imports the Companion or the channel ***
*** nothing in the application imports the observation boundary ***
*** nothing is ever dropped — the Owner ruled no cap ***
*** nothing spawns, writes, connects, clicks, types or captures ***
*** nothing still tells the Owner to press a button that no longer exists ***
*** nothing the store logs contains message content ***
*** nothing written yet is empty settings, not an error ***
*** null confidence is PRESERVED as null — never defaulted to 0.5 ***
*** null confidence renders as "not established", never as a number ***
*** null is CLEARED, not malformed — it is how the schema spells "not set" ***
*** observation.js cannot synthesise input, touch disk, or start a process ***
*** off and shadow still require an exact spelling ***
*** offerFor returns null, never an empty object — no field appears for nothing ***
*** ok:true and auditWritten:false is now an IMPOSSIBLE pair ***
*** on a GPT turn, provenance is GPT\
*** once she has said it, a REWORDED restatement is dropped ***
*** one table per concept — the router iterates what the answerer publishes ***
*** one transient retry at most is permitted, and none is implemented here ***
*** one turn spends exactly one resolver call ***
*** only HER prior turns count — the Owner saying it is not her having said it ***
*** only ITEM lines become needles — our own header never does ***
*** only a VALIDATED lesson may be applied ***
*** only exact values are honoured — three states, no near misses ***
*** only the OFFENDING section is dropped, not its neighbour ***
*** only the parts the Owner filled in appear ***
*** operation names are unique ***
*** optionality is expressed as a NULL UNION, never by omission ***
*** ordinary conversation is NOT annotated — the control must stay quiet to stay trusted ***
*** ordinary prose containing a numeral character is NOT a count claim ***
*** other separable forms of the same compounds ***
*** over the cap is REFUSED with the numbers, and nothing is saved ***
*** over-bound text is REJECTED, never silently truncated ***
*** own signature unrecognised makes the capture unable to support an absence claim ***
*** owner content becoming visible is CONTAINMENT-FAILURE, not a failed row ***
*** precondition: a message that does not name her system supplies nothing ***
*** preferences are marked as outranking recalled conversation ***
*** presentedOrder reads FIRST MENTION, and ignores rows never named ***
*** previous conversations become a readable block, newest conversation first ***
*** priority is UTILITY → ACTION → BUSINESS_QUERY → CONVERSATION ***
*** proper nouns keep their original spelling ***
*** provider-neutral and network-free, proven statically ***
*** public → public only ***
*** put() returns a hash and a size — never the image ***
*** queryScope inferred from audited source is marked as DEBT ***
*** read context actually injected → readContextUsed true, with source KINDS ***
*** read happened, reply cites NOTHING → body is KEPT ***
*** read-failure detection is unchanged — this widened the guard, it did not move it ***
*** readMatchesIntent: mixed accepts either side, single worlds do not ***
*** reasonForStatus maps every documented failure class ***
*** records survive a completely new store object over the same file ***
*** records written before A′ (schemaVersion 1) are still readable ***
*** refs are {kind, id} and CANNOT smuggle evidence text ***
*** rejecting is a server action, not a screen state ***
*** rejection behaviour is frozen: malformed input still throws ***
*** rejection is recorded, and the lesson is not deleted ***
*** render() stops dropping the conversation it is handed ***
*** replacement is what makes UN-setting possible — the reason merge would be wrong ***
*** reply quotes an email SUBJECT → body is OMITTED ***
*** requiresEvidence must be exactly true — an unverifiable action is refused ***
*** resolved at most once per stable Owner context ***
*** retrieved content cannot influence the route ***
*** retrieved content cannot route a turn anywhere — the router never sees it ***
*** rows -> RESULTS_FOUND ***
*** rows carry a time and a turn count — the only things that separate them ***
*** rows on a NON-business route can never force a plan ***
*** rows returned but none in the asked-for period has a name and words ***
*** rubbish input measures nothing and never throws ***
*** rubbish input never throws and defaults to ASKING ***
*** rubbish input never throws and never claims a correction ***
*** rubbish input never throws and supplies nothing ***
*** running text is never bold: only two weights exist, 400 and 500 ***
*** running this very file did not write to the production store ***
*** saving a bad timezone is refused, all-or-nothing ***
*** sealed, rejected, approved and refused are all emitted ***
*** secrets are redacted BEFORE the record exists, and only the redacted text survives ***
*** semantic guidance is unchanged ***
*** shadow changes NOTHING about the turn ***
*** shadow still changes nothing for a NON-utility turn ***
*** she may write either label, and the rendered one is the Owner-facing one ***
*** she may write either the code or the Owner-facing word ***
*** she must still UNDERSTAND Cantonese; only her output changes ***
*** shownCount may never be presented as matchingTotal ***
*** signed governance records were not rewritten by the rename ***
*** something the evidence never held is scored as unrelated ***
*** source kinds only — a source payload cannot ride in on readContextSources ***
*** source order is preserved and duplicates collapse ***
*** sourceClass is a class, never the source list ***
*** sourceTotal=null can never support an unqualified source-wide universal ***
*** source_wide VERIFIES only when matchingTotal === sourceTotal ***
*** source_wide is structurally distinct from row_local ***
*** stage 1 — every observation capability is declared and OFF ***
*** startOfLocalDay follows the SETTING, not the operating system ***
*** start_url points at a page that actually exists ***
*** stats counts omissions, so an archive of placeholders cannot look full ***
*** still three sections, and the face stays short ***
*** stripCitationMarkers removes links and leaves the sentence intact ***
*** substring matching is NOT the mechanism — 8.0 must be rejected ***
*** substring matching is still not the mechanism ***
*** supersession replaces a belief and points at its replacement ***
*** swallowed per-node failures make the node count meaningless ***
*** telemetry carries no business content and no model prose ***
*** text is read from EVERY block, not just the first ***
*** the 0-byte UIA artefact that started this is swept like any other ***
*** the A4 shaped schema obeys strict mode at every node ***
*** the AUTOMATIC read is still vetoed by notAsked, end to end ***
*** the Aroma System rewrite table is WITHDRAWN ***
*** the CURRENT conversation is excluded ***
*** the Companion account has NOT been created by this code ***
*** the Companion account holds no credential and no bank session ***
*** the Companion cannot capture a screen or read a UI tree ***
*** the Companion cannot move a mouse, send a key, launch an app or write a file ***
*** the Companion delegates observation and performs none of it ***
*** the Companion imports NOTHING that could reach a desktop or a disk ***
*** the Companion refuses EVERY request — no capability is enabled ***
*** the HOW-TO half was not deleted — it is in the ＋ menu ***
*** the Lab store is NOT any production store ***
*** the Observer is a SECOND entry point and the 3a bindings do not reach it ***
*** the Owner is never asked to switch language ***
*** the Owner message carries no Aroma business intent — so aromaMethodFor is null ***
*** the Owner session cookie is NOT the approval router\
*** the Owner validates; nobody else can ***
*** the Owner writes style and preferences, and they come back ***
*** the Owner's own number, this turn, is his — not a claim of hers ***
*** the Owner\
*** the PRODUCTION composition has a real audit store — asserted, not assumed ***
*** the RAW model string never reaches the log — only the boolean ***
*** the REAL read path emits one line per source, with the right verdicts ***
*** the SHAPED schema — what is actually sent — is compatible too ***
*** the SOURCE is copied, not the rendered DOM text ***
*** the Service and Companion talk over a real named pipe ***
*** the Supervisor cannot spawn, connect, click, type or capture ***
*** the Supervisor has NO execute path — only a dry-run ***
*** the U1 path does not share the distill envelope projection ***
*** the U1 schema keys and name are frozen ***
*** the adapter registers read-shaped and produces sourced, CONTENTFUL evidence ***
*** the admission filter accepts only the four words ***
*** the admission filter accepts only the two words ***
*** the allowedPath folder was NOT created, and nothing here would create it ***
*** the approval half is in the composer placeholder ***
*** the approved root is exactly the Owner-decided folder ***
*** the architecture leak is gone: no designer vocabulary in clause 3 ***
*** the archive is NOT claimed to be durable storage ***
*** the assistant reply has NO bubble — plain text on the page background ***
*** the attribution line is never part of what is copied ***
*** the audit sink is the durable store, not only the in-memory array ***
*** the avatar dot is #FFA02E in BOTH themes — no token reaches it ***
*** the avatar orange became a token instead of a second literal ***
*** the backup doc no longer says this is unbuilt ***
*** the band follows the SETTING, not the machine ***
*** the base URL reports which source it came from ***
*** the block is bounded, and stops at a whole conversation ***
*** the block says what it is, and what it cannot do ***
*** the block states EXACTLY what wisdom is not ***
*** the browser cannot invent a field or a switch ***
*** the calendar window starts at TODAY 00:00, not at the current instant ***
*** the canned assistant bubble is GONE ***
*** the card tells the Owner that no PR will be opened ***
*** the catalogue is assembled from the frozen tables, and every operation resolves ***
*** the chat surface still knows nothing about the bridge ***
*** the check is available on its own, so the refusal has one implementation ***
*** the check rejects an entry that a human would call spoken ***
*** the clarification asks about MEANING, never about a tool ***
*** the clarify exit is recorded as the exit it is ***
*** the classifier-unavailable contract survives the split ***
*** the client calls the route instead of only greying out ***
*** the client never computes the greeting from the browser clock ***
*** the clipboard write has a graceful failure path ***
*** the composer carries NO accent colour, resting or focused ***
*** the composer is the ONE place A4 dependencies are built ***
*** the composition log is names and reasons — never a prompt or a key ***
*** the connector survey is recorded, because the shape is what recurs ***
*** the contract no longer authorises Cantonese output ***
*** the cookie is Secure — it must never cross a plaintext connection ***
*** the correction states counts and states only — never content ***
*** the current message wins over an older one in history ***
*** the date is a field the API returned, or null — never inferred ***
*** the date phrasings, and the anchor is no longer only 今日/今天 ***
*** the decision still carries the marker downstream ***
*** the default location is OUTSIDE the repo ***
*** the deliberate cost: one marker over several sentences keeps only the last ***
*** the deploy runs THREE rounds, each with a fresh Companion ***
*** the description glosses each Aroma operation and lists nothing else ***
*** the detector never returns or logs a needle ***
*** the deterministic entrance stands down when the model path already worked ***
*** the double-check is a different implementation, not a copied regex ***
*** the draft body survives the parser EXACTLY — every newline and character ***
*** the face sentence is DERIVED — drop an action and it disappears ***
*** the fallback distinguishes a successful READ from a failed COMPOSITION ***
*** the fallback is written Chinese ***
*** the fallback path survives — a genuine no-plan still returns no result ***
*** the fallback text cannot itself trip the read-state guard ***
*** the fence can see the shape it is built for ***
*** the first time, the fixed scope property is shown ***
*** the fix instruction carries the absolute path and says why ***
*** the flag fails closed on anything but exactly "on" ***
*** the flag is set when the turn is sent, on the conversation itself ***
*** the flag is still NECESSARY — a perfect order with the flag off is refused ***
*** the flag is still off and the launcher still does not mention it ***
*** the flag is unread, so turning it on does nothing ***
*** the floor is 8 — the Owner\
*** the footer keeps the LOCAL-DEMO fact, which nothing else states ***
*** the four intents map to the worlds they oblige ***
*** the frozen identity is not reachable from settings ***
*** the gap is bounded by clause punctuation — a denial cannot reach into the next clause ***
*** the gap may not cross clause punctuation ***
*** the gate fails closed: unset, empty and nonsense all mean off ***
*** the gate is path-scoped: the approval routes are NOT behind it ***
*** the gloss carries STRUCTURE only — never an error, a body or a value ***
*** the greeting is large and quiet, and centred ***
*** the greeting route sits under a prefix the owner gate already covers ***
*** the guarantee is on the SHIPPED reply, not on what the model was offered ***
*** the guard is actually on the conversational reply path ***
*** the guard is pure and names a WORLD, never a capability ***
*** the guards that matter do not read settings — that is what actually holds ***
*** the guidance carries no test/holdout sentence — no teaching to the test ***
*** the half that DECIDES cannot act; the half that ACTS cannot decide ***
*** the hash covers everything the Owner would be shown ***
*** the header forbids treating a discussion as a decision ***
*** the header never asserts that a listed source appears below ***
*** the header tells the model it DOES NOT KNOW what it said ***
*** the hint gives the exact apply command ***
*** the hook has NO parameter that could carry retrieved content ***
*** the identity fact — 「係咪內部嘅」 — is also registry-backed ***
*** the indicator reuses the existing blink, and respects reduced motion ***
*** the indicator sits at the LEFT of the title ***
*** the inert default still satisfies the read routes — it is empty, not broken ***
*** the inventory derivation is DECLARED in the adapter, not invented here ***
*** the invoice source field is DECLARED with an Owner-facing label ***
*** the isolation statement is on the visible face ***
*** the key appears in the Authorization header and NOWHERE else ***
*** the key never appears in a timeout error ***
*** the keyword table still matches these sentences — that is not what changed ***
*** the language policy no longer spells the forms it forbids ***
*** the lantern assets and their assembler are removed, not orphaned ***
*** the largest FALL leads, because that is the direction that is believed ***
*** the launcher and the repo default now agree ***
*** the ledger is not empty, and not stale to the point of meaninglessness ***
*** the lesson cap is respected ***
*** the limits are carried IN the order, and bounded ***
*** the line answers the question that could not be answered ***
*** the line is emitted for a FALLBACK too, with its reason ***
*** the list is grouped 今日 / 尋日 / 更早 ***
*** the list is newest-first by updatedAt and carries no message bodies ***
*** the live server still gets production — this must not break the real process ***
*** the log carries NO message content — allowlisted fields only ***
*** the log line carries the reason, the shape, and a short value only ***
*** the log line is an enum and a count ***
*** the log line is an enum and two numbers ***
*** the log line is enums and identifiers only ***
*** the log line is enums and numbers only ***
*** the log records WHAT was dropped — identity, never values ***
*** the log records how many items the model offered and how many were real ***
*** the log records what the ROUTER decided AND what the pipeline actually did ***
*** the manifest meets Chrome\
*** the manifest must be revalidated, or an icon change looks permanent ***
*** the manifest resolves every dependency — nothing is MISSING ***
*** the mark is ONE filled circle — no ring, no glow, no inner shapes ***
*** the matching total reaches the model — and is NOT stated as a source total (A1) ***
*** the measured counts ARE stateable in Chinese numerals ***
*** the message is only shown after the server confirms it ***
*** the message text is never logged ***
*** the message vocabulary is CLOSED, like the action enum ***
*** the message-driven planner still governs the AUTOMATIC read ***
*** the minimal answer is a count and provenance — NEVER arbitrary rows ***
*** the model is TOLD 缺口 exists, and how it is computed ***
*** the model is shown ONLY unread authorised sources ***
*** the model may write NO audit field — the list is empty and enforced ***
*** the module cannot apply anything — v1 writes and stops ***
*** the module contains no non-GET verb and takes no method parameter ***
*** the module has no path that could log or return a token ***
*** the module never logs, and never returns a body or a row wholesale ***
*** the module never reads the OS zone as a fallback ***
*** the net is deliberately WIDE, and the direction of its error is stated ***
*** the note names WHY nothing was read when the reason is known ***
*** the note names the source that was not consulted ***
*** the observation action set is closed, and input actions are not in it ***
*** the observation block names a world and never a capability ***
*** the offer explains itself, firing or declining ***
*** the offer is built from inferWorkRequest — not a second reading ***
*** the offer is checked BEFORE the clarification branch ***
*** the offer records HOW it got there ***
*** the offer still yields to a turn that produced a real proposal ***
*** the offer telemetry is set BEFORE emit() writes the line ***
*** the omission note appears ONLY when something was actually shown ***
*** the omission notes are written Traditional Chinese ***
*** the one fact that mattered survives: did the focused window change ***
*** the one-file scope is STATED on the face, not just implied by a filename ***
*** the opt-out still wins over everything, including an omitted turn ***
*** the order is the table order — first match stays first ***
*** the outcome line carries whether the offer fired, and why not ***
*** the outer distill schema still closes additionalProperties ***
*** the page is told the truth about READ_ACCESS ***
*** the page states that the guards are code, not wording ***
*** the page title and tab title are the CURRENT name ***
*** the parameter is INERT, not merely deprioritised ***
*** the password is never in the login page, in either state ***
*** the path allowlist is exactly the six GETs ***
*** the per-turn omission notes are untouched by any of this ***
*** the picker note names every source, generated — not a stale four ***
*** the policy governs prose only — it may never touch source data ***
*** the policy is inside the block that crosses the adapter boundary ***
*** the precondition holds: no user mention of her system, no correction ***
*** the projected key set is exactly this, in this order ***
*** the projection stays CLOSED — answerPlan is the one named exception ***
*** the prompt only branches on roles the client actually sends ***
*** the proposal is marked as deterministically created ***
*** the proposal lane is unchanged — no decision schema there ***
*** the provider is neutral about MEANING: it decides no world and no query ***
*** the quantities reach the Owner, and the turn is clean ***
*** the question is ALWAYS one sentence ***
*** the real-sample count is visible, and the negation gap is stated ***
*** the reason is a closed enum, not the error text ***
*** the recovery worker keeps its PINNED model, not the session model ***
*** the refusals all survive the wider vocabulary ***
*** the refused phrasing now produces a proposal ***
*** the reject path passes a COMPUTED hash, not a field that does not exist ***
*** the rendered reply shows the row instead of the fallback ***
*** the renderer mutates nothing it is given ***
*** the request that produced nothing now produces an offer ***
*** the requirement block STATES the gap rather than hiding it ***
*** the response envelope is unchanged by persistence ***
*** the right password opens everything that was refused ***
*** the route exists on the approval surface, GET-only ***
*** the route has no path or query parameter at all ***
*** the router adds no paid model call ***
*** the router and the answerer share ONE unit vocabulary ***
*** the router holds NO utility vocabulary of its own ***
*** the row count is bounded ***
*** the row still lines up: the dot centres on the first line of the reply ***
*** the rule holds through the REAL pipeline, with the recall block present ***
*** the rule is concrete again, and still spells no banned form ***
*** the same date the row stores as a timestamp is accepted as a date ***
*** the same script yields the same trace whatever is behind callModel ***
*** the sampling arithmetic is stated in PHYSICAL pixels and still holds after DPI ***
*** the scan finds the inert implementations that exist ***
*** the schema no longer demands a section, and declares the choice instead ***
*** the schema restricts sourceId to THIS turn\
*** the scope meanings reach the model — no location, no as-of ***
*** the scope-note tables keep their Cantonese forms ***
*** the score travels even when the VALUE is withheld ***
*** the screen locking is a STOP condition — nothing runs when he is not there ***
*** the second time, a reworded restatement does not reach him ***
*** the sentence is DERIVED, not retyped — WYSIWYA property #1 ***
*** the server\
*** the service HANDS the classification to telemetry — not just to the response ***
*** the settings file lives OUTSIDE the repo ***
*** the shaper is inert when disabled — same object, not an equal one ***
*** the sidebar lists conversations newest-first, without transcripts ***
*** the sidebar loads history from the server, not just from the page ***
*** the signal carries no retrieved content — kinds only ***
*** the signal is a real BOOLEAN on every answered turn, never left undefined ***
*** the six Aroma routes are byte-identical after the table was extended ***
*** the six canonical concepts are all rendered ***
*** the socket-level spelling counts too ***
*** the source schema is never mutated ***
*** the square icon keeps the dot clear of the edges (checked at 32px) ***
*** the stack names no CJK face — a deliberate consequence of using it verbatim ***
*** the staged ENTRY connects and refuses, from the staged directory ***
*** the staged copy LOADS with no access to the repo ***
*** the step-scope check is UNREACHABLE today — dormant, not working, not broken ***
*** the stop vocabulary is CLOSED — no free text can become a reason ***
*** the store does NOT call itself durable ***
*** the sweep only ever touches its OWN files ***
*** the system text names no business noun and no implementation term ***
*** the system text names no product and tells her nothing about her powers ***
*** the three that named Gmail now name only the restaurant system ***
*** the time phrasings the Owner uses ***
*** the title and the dot are hidden while the conversation is empty ***
*** the title is the first user message, trimmed — and never changes after ***
*** the title was NOT touched — that question is separately deferred ***
*** the top-level check is KEPT — six of seven connectors rely on it ***
*** the transition table is closed, and every backwards move is refused ***
*** the two old implementations are gone ***
*** the unit codes the live rows actually carry are declared ***
*** the v0 action enum is EXACTLY the approved capability, nothing more ***
*** the validated plan reaches the Owner — no template wording survives ***
*** the verifier never receives the FINAL text the model wrote ***
*** the verifier output contract is closed and carries no reasoning field ***
*** the verifier receives MEANING only — never a capability or evidence ***
*** the web app manifest carries the current name ***
*** the whole-block cap stops on a record boundary ***
*** the work order hash travels with the decision ***
*** the worker cannot author an ASK either ***
*** the world rule is the same one A4 uses everywhere ***
*** the world vocabulary is exactly two words ***
*** the writer is untouched ***
*** the written forms that NOTHING else covers ***
*** the wrong-world refusal establishes the CORRECT obligation ***
*** the zero-result state stays DISTINCT from unavailable ***
*** there is no global working flag — two conversations can work at once ***
*** trust live + usedFallback true + a reply claiming 讀不到 is CAUGHT ***
*** turn() appends to the conversation it was given, not to whatever is on screen ***
*** two files named → it asks WHICH, and lists them ***
*** unanswerable is carried through, with no fabricated sections ***
*** unknown enum values are refused everywhere ***
*** unknown survives adapter → readContext, undefaulted ***
*** validated lessons are included ***
*** validation admits only the closed shape, and allow forces question null ***
*** validation holds with several sources present at once ***
*** wallpaper colour gamut must not trip either signature ***
*** what a dry-run CANNOT verify is reported as unknown, never as ok ***
*** what the picker CLAIMS about each provider must be TRUE, and stated on the option ***
*** when routing is uncertain it falls to CONVERSATION ***
*** when the calendar really IS down, 讀唔到 stands uncorrected ***
*** whichever she writes, the RENDERED value is the Owner-facing one ***
*** windowTitle is not an evidence field any more ***
*** with COMPUTER_OPERATOR off, all 32 combinations are byte-identical to the old gate ***
*** with TURN_ROUTER off, a clock question behaves exactly as before ***
*** with no choices left, nextRead collapses to null-only and takes args with it ***
*** with no key the adapter is not ready and refuses cleanly ***
*** with no ranked rows a ranking CLAIM fails closed, and an ordinary section does not ***
*** with nothing asked and nothing read, there is no block at all ***
*** with nothing left to read, nextRead is NULL-ONLY — never an empty enum ***
*** with nothing said before, the scope note is KEPT ***
*** with the demo flag off, every conversation route is 403 ***
*** x as a multiplication sign ***
*** zero rows → no ANSWER PLAN is forced (the decision surface is a different thing) ***
*** ⛔ ${c.name} IS REPORTED AS ITS OWN SHAPE ***
*** ⛔ ${c.name} SURVIVES INTO THE ACTUAL LOG LINE ***
*** ⛔ 1. A 「缺貨項目排序」 SECTION IN THE WRONG ORDER MUST NOT SHIP ***
*** ⛔ 1. ESTABLISHED INTERNAL + REACHABLE + clarify → NOT a terminal verifier ASK ***
*** ⛔ 1. SUPERLATIVE + PROVEN ABSOLUTE RANKING → SHIPS, naming the proven first ***
*** ⛔ 1. 「缺口 70」 IS ACCEPTED IN A SECTION *AND* IN directAnswer ***
*** ⛔ 1b. AND THE TRAFFIC WENT THROUGH THE SHARED AUTHORITY, not a private branch ***
*** ⛔ 2. AN ANSWER CONTRADICTING THE PROVEN ORDER DOES NOT SHIP ***
*** ⛔ 2. ESTABLISHED INTERNAL + UNREACHABLE → no invented obligation, no read ***
*** ⛔ 2. THE SAME SECTION IN THE PROVEN ORDER IS ALLOWED ***
*** ⛔ 2. 「缺口 69」 IS STILL REJECTED — the server computed 70 ***
*** ⛔ 3. A PROPORTIONAL SUPERLATIVE IS REFUSED — no proportional ordering exists ***
*** ⛔ 3. A 「缺貨狀況」 SECTION IS NOT A RANKING, WHATEVER ITS ORDER ***
*** ⛔ 3. ANOTHER RETRIEVED ROW CANNOT BORROW NAPA\
*** ⛔ 3. EXPLICIT public / mixed verdicts are unchanged ***
*** ⛔ 3/36 to 2/30 carries noise:true ON THE ROW, not in a footnote ***
*** ⛔ 3b. AND THE METRIC MAY NOT BE SWITCHED SILENTLY IN EITHER DIRECTION ***
*** ⛔ 4. AN UNDECLARED LABEL CANNOT VALIDATE A NUMBER ***
*** ⛔ 4. CONTEXT-FREE AMBIGUITY STILL CLARIFIES — the path is narrowed, not deleted ***
*** ⛔ 4. orderPlanning: the server cut precedes the sort → no global first ***
*** ⛔ 4b. AND A TURN THE ROUTER NEVER ESTABLISHED IS NOT RESTRAINED ***
*** ⛔ 4b. AND WITH NO DECLARED DERIVATIONS AT ALL, NOTHING IS CONSUMED ***
*** ⛔ 4c. A DERIVED NUMBER WITH NO BINDABLE RETRIEVED ROW STILL FAILS ***
*** ⛔ 5. A SENTENCE OF ORDINARY RAW EVIDENCE NUMBERS BEHAVES AS BEFORE ***
*** ⛔ 5. PROSE ALONE MAY NOT ASSERT A FIRST PLACE ***
*** ⛔ 5b. DIGIT BOUNDARIES — consuming 70 must not cut 700 into 0 ***
*** ⛔ 6. LIVE: number_not_in_evidence NO LONGER KILLS THE CONCLUSION ***
*** ⛔ 6. NON-SUPERLATIVE QUESTIONS ARE NOT THIS GATE\
*** ⛔ 6. [AROMA-ASK-FORK] SHOWS THE RESTRAINT AS ITS OWN BRANCH ***
*** ⛔ 7. THE 3-READ + RESERVED-COMPOSE ARCHITECTURE IS UNCHANGED ***
*** ⛔ A CORRECT reply produces the URL EXACTLY ONCE — no duplication ***
*** ⛔ A FAILED OR ABSENT PLAN NARROWS NOTHING — fail-safe, never fail-shut ***
*** ⛔ A LOGGER THAT THROWS DOES NOT FAIL THE TURN, AND RETURNS NOTHING ***
*** ⛔ A MEASURE NOTHING ORDERED IS STILL RECOGNISED — and cannot ship undeclared ***
*** ⛔ A QUESTION THE REGISTRY DOES NOT COVER KEEPS THE HONEST REFUSAL ***
*** ⛔ A SENTENCE THAT PASSES EMITS NO FAILURE DETAIL AT ALL ***
*** ⛔ A SENTENCE THAT SURVIVES PRODUCES NO DROP REASON ***
*** ⛔ A TURN THAT NEVER READ GETS NO RESERVED CALL — no free extra model call ***
*** ⛔ A URL IS NOT A FACT — both halves are required, and the kind must be known ***
*** ⛔ A VALID DERIVATION + AN UNRELATED BAD NUMBER IS raw_unsupported ***
*** ⛔ A. WITHHOLD 「${c.name}」 STILL SHIPS A NON-EMPTY REPLY ***
*** ⛔ A4 OFF: the resolver never runs ***
*** ⛔ A4 on + NON-chat lane: the guidance must NOT appear ***
*** ⛔ AN EMPTY REPLY IS A DEFECT STATE, NEVER SILENCE ***
*** ⛔ AN INCOMPLETE PROOF IS REFUSED EVEN IN THE RIGHT ORDER ***
*** ⛔ AN INCOMPLETE RANKING IS REFUSED EVEN WHEN THE ORDER IS RIGHT ***
*** ⛔ AN UNPROVEN METRIC IS REFUSED BEFORE ANY ORDER CHECK ***
*** ⛔ AN UNRETRIEVED NAME RECORDS name_not_in_evidence ***
*** ⛔ AN UNSUPPORTED NUMBER RECORDS number_not_in_evidence ***
*** ⛔ AND A NON-CLAIM SECTION IS STILL UNTOUCHED WITH ZERO SOURCES ***
*** ⛔ AND A REAL WORK REQUEST ON THE SAME FALLBACK IS STILL THE MODEL\
*** ⛔ AND A VALID DERIVATION WITH NO OTHER NUMBER STILL PASSES CLEAN ***
*** ⛔ AND THE DETAIL RIDES ON A FIELD THE SERIALIZER ALREADY SHIPPED ***
*** ⛔ AND THE NUMERAL-BEFORE-LABEL CASE IS STILL derived_unbound ***
*** ⛔ AND THE SYSTEM IS STILL RECOGNISED, by every spelling that denotes IT ***
*** ⛔ AND THE TRACE ITSELF NEVER PRINTS THE TURN ***
*** ⛔ AND droppedSentences COUNTING IS UNCHANGED ***
*** ⛔ ANY model output on a covered turn is discarded, however phrased ***
*** ⛔ B reaches production through ONE call site, and is not a second reasoning loop ***
*** ⛔ B1. A RANKING WITH THE CORRECT FULL SEQUENCE IS ALLOWED ***
*** ⛔ B1. A SENTENCE ATTRIBUTING NAPA\
*** ⛔ B1b. TWO NAMED ROWS FAIL CLOSED EVEN WHEN THE FIGURE IS CORRECT ***
*** ⛔ B1c. AND ONE NAMED ROW STILL WORKS — the rule narrows, it does not delete ***
*** ⛔ B2. NAPA FIRST BUT THE TAIL REORDERED MUST NOT SHIP AS A RANKING ***
*** ⛔ B2. 「缺口 70.5」 MUST BE REJECTED — 70 IS A PREFIX, NOT THE TOKEN ***
*** ⛔ B2b. WITH A RAW 5 ON THE ROW, 「70.5」 IS STILL REJECTED ***
*** ⛔ B2c. 「70,000」 CANNOT BE VALIDATED BY A DERIVED 70 ***
*** ⛔ B2d. AND A GENUINELY DECIMAL DERIVATION IS STILL ACCEPTED WHOLE ***
*** ⛔ B3. A BARE SUPERLATIVE NEEDS FIRST PLACE ONLY — NO TAIL VALIDATION ***
*** ⛔ B3. TWO CANONICAL ROWS SHARING A TITLE — NEITHER VALUE IS BORROWABLE ***
*** ⛔ B3b. ONE CANONICAL ROW STILL PASSES — identity narrows, it does not delete ***
*** ⛔ B4. AN ORDINARY LIST IS NOT A RANKING CLAIM ***
*** ⛔ B4. DERIVED 70 MUST NOT VALIDATE PROSE -70 ***
*** ⛔ B4b. DERIVED -5 VALIDATES PROSE -5 ***
*** ⛔ B4c. DERIVED -5 DOES NOT VALIDATE PROSE 5 ***
*** ⛔ B4d. AND AN INDEPENDENTLY SUPPORTED 5 IS STILL ALLOWED ***
*** ⛔ B4e. THE 70.5 / 70,000 / 700 PROTECTIONS SURVIVE THE SIGNED GRAMMAR ***
*** ⛔ B5. A RAW 69 ON THE ROW MUST NOT LAUNDER 「缺口 69」 ***
*** ⛔ B5. AND A WRONG FIRST PLACE STILL FAILS, RANKING OR NOT ***
*** ⛔ B5b. AND 「缺口 70」 ON THAT SAME ROW IS STILL ACCEPTED ***
*** ⛔ B5c. THE SAME ROW\
*** ⛔ B5d. A RAW 5 MUST NOT LAUNDER 「缺口 5」 WHEN THE DERIVATION IS -5 ***
*** ⛔ B5e. 「每箱 5」 ON THAT SAME ROW IS STILL ORDINARY RAW EVIDENCE ***
*** ⛔ B5f. AND 「缺口 -5」 IS STILL ACCEPTED ON THAT ROW ***
*** ⛔ B5g. A DUPLICATE-TITLE CLAIM CANNOT BE LAUNDERED EITHER ***
*** ⛔ BOTH observed phrasings are caught, and they differ from each other ***
*** ⛔ CANONICAL IDENTITY — DUPLICATE TITLES DO NOT COLLAPSE ***
*** ⛔ CLASS 1 — internal context + capability available → READ, no clarification ***
*** ⛔ CLASS 2 — internal context + capability UNAVAILABLE → no reads, no clarification ***
*** ⛔ CLASS 4 — genuine context-free ambiguity STILL ASKS ***
*** ⛔ DEFAULT OFF — an unset flag reads as off, and so does anything but "on" ***
*** ⛔ E0-B1 is not wired into the runtime, and browseResult.js is GONE ***
*** ⛔ EACH OUTCOME IS REPORTED AS AN ENUM PLUS A COUNT ***
*** ⛔ END TO END: nextRead → real connector read → observation in prompt → SAME provider → FINAL ***
*** ⛔ EVERY genuine work request still proposes a change — none may be lost ***
*** ⛔ EXHAUSTION AND GENUINE PLAN FAILURE ARE DIFFERENT STOP REASONS ***
*** ⛔ EXPLICIT TOP-N: exact membership, exact count, correct order ***
*** ⛔ GPT step 1 → READ → GPT step 2. Claude is NEVER called. ***
*** ⛔ INTEGRATION: THE PRODUCTION SECTION IS REFUSED AND THE REPLY IS NOT EMPTY ***
*** ⛔ IT CAN ONLY EVER NARROW — a named source that is not enabled stays unread ***
*** ⛔ KNOWN GAP, DELIBERATE: an unattributable denial stays silent ***
*** ⛔ LEGITIMATE clarification on another axis passes through UNTOUCHED ***
*** ⛔ LIVE: AN UNPROVEN RANKING SECTION IN CORRECT ORDER STILL MUST NOT SHIP ***
*** ⛔ LIVE: and an ORDINARY section over the same unproven source still ships ***
*** ⛔ M1. SOURCE A\
*** ⛔ M2. TWO COMPLETE PROOFS IS STILL AMBIGUOUS — FAIL CLOSED ***
*** ⛔ M3. AN ORDINARY SECTION IN A TWO-SOURCE TURN IS UNTOUCHED ***
*** ⛔ M4. THE SINGLE-SOURCE HAPPY PATH IS UNCHANGED ***
*** ⛔ M5. ZERO RANKED SOURCES — A CLAIM FAILS CLOSED, AN ORDINARY SECTION DOES NOT ***
*** ⛔ M6. LIVE: WITH TWO RANKED SOURCES A RANKING SECTION MUST NOT ESCAPE ***
*** ⛔ METRIC MISMATCH FAILS CLOSED, WHATEVER THE ORDER ***
*** ⛔ MULTIPLE RANKED SOURCES STAY FAIL CLOSED ***
*** ⛔ NO CALL SITE READS THE TRACER\
*** ⛔ NO FIELD CAN CARRY THE MESSAGE, THE REPLY, OR A SECRET ***
*** ⛔ NO HEADING, TITLE, VALUE OR MESSAGE REACHES THE VERDICT ***
*** ⛔ NO REPLY, SENTENCE, NAME OR ROW VALUE REACHES THE LOG ***
*** ⛔ NO SENTENCE, TITLE, NUMERAL, LABEL OR USER TEXT REACHES THE LOG ***
*** ⛔ NO TEXT DETECTOR: production never inspects her prose for a capability denial ***
*** ⛔ NOT-COVERED questions are untouched — composition is narrow ***
*** ⛔ ON DETECTION the disambiguation does not ship, and the FACT does ***
*** ⛔ ONE-ITEM SUPERLATIVE: only the proven first qualifies ***
*** ⛔ OPTIONAL facts do not authorise a read on their own ***
*** ⛔ ORDERING HEADINGS KEEP THE LEGITIMATE SUBSEQUENCE BEHAVIOUR ***
*** ⛔ ORDINARY SET HEADINGS ARE NOT RANKINGS ***
*** ⛔ ORDINARY TURNS PAY NOTHING EXTRA — the reserve is not a general raise ***
*** ⛔ PROHIBITION 1 — the BOUND port wins over the requested one ***
*** ⛔ PROHIBITION 2 — the sentence is derived, so a count cannot drift ***
*** ⛔ PROHIBITION 3 — reachable is ALWAYS null; a flag never answers a capability ***
*** ⛔ READ → READ → READ: a fourth call to COMPOSE, and pending prose is still NOT the answer ***
*** ⛔ READ_ACCESS off → nextRead executes ZERO connector reads ***
*** ⛔ REVIEW 1: a capped count is NOT a matching total ***
*** ⛔ REVIEW 2: order-planning is capped at 100 ***
*** ⛔ REVIEW 3: unknown filters are null, never [] ***
*** ⛔ REVIEW 4: limitKnown separates 「no limit」 from 「limit unknown」 ***
*** ⛔ RULE 1 — a need nothing carries is UNAVAILABLE, decided by the enum and not the model ***
*** ⛔ RULE 1 — costing answered from INVOICES is refused on the ENTITY, with nobody reading the need ***
*** ⛔ RULE 2 — an empty field and an unobserved one cap a fact at PARTIAL; an absent one kills it ***
*** ⛔ RULE 3 — a declared join is UNVERIFIED, and it costs sufficiency ***
*** ⛔ SEEN TO FAIL: an omitted property is detected ***
*** ⛔ SPARSE is ALWAYS_EMPTY\
*** ⛔ SUPERLATIVE WITH NO N: the items must be a proven PREFIX ***
*** ⛔ TELEMETRY PROSE RECORDS telemetry ***
*** ⛔ THE 17:06 TURN — the registry URL reaches the shipped reply ***
*** ⛔ THE ACCEPTANCE CASE — a plan naming NO operation reads NOTHING ***
*** ⛔ THE ACCEPTANCE CASE — she never has to ask what Aroma System is ***
*** ⛔ THE CANARY TURN — three reads consume the budget and it STILL composes ***
*** ⛔ THE CITED SPAN IS THE FOOTNOTE, NOT THE FACT — proved on the captured live payload ***
*** ⛔ THE CLARIFY TURN DECIDES THE SAME WITH LOGGING AND WITHOUT ***
*** ⛔ THE CLASS, not the instance — other subjects that merely carry a token ***
*** ⛔ THE ENUMS ARE CLOSED — an unrecognised value is never echoed ***
*** ⛔ THE FIVE SHAPES ARE DISTINCT, AND ALL FIT THE 20-CHAR LOG FIELD ***
*** ⛔ THE FOUR ENUMS ARE DISTINCT AND CLOSED ***
*** ⛔ THE GOOD ANSWER IS NOT CAUGHT — and it ends in a question too ***
*** ⛔ THE HAZARD WAS REAL: empty strings overwrite real settings ***
*** ⛔ THE KNOWN FALSE POSITIVE IS PINNED, NOT HIDDEN ***
*** ⛔ THE OWNER\
*** ⛔ THE PRODUCTION DEFECT: 「目前最缺的四項」 IN THE WRONG ORDER IS REFUSED ***
*** ⛔ THE READING TURN DECIDES THE SAME WITH LOGGING AND WITHOUT ***
*** ⛔ THE REAL MESSAGE, 2026-08-08 ***
*** ⛔ THE REAL TURN: nothing read, he asked for a look → the record is appended ***
*** ⛔ THE RESERVED CALL IS BOUNDED — it may not read, so it cannot extend the turn ***
*** ⛔ THE RESERVED CALL IS NOT AN ESCAPE HATCH AROUND beforeTerminal ***
*** ⛔ THE RESTAURANT IS NOT THE SYSTEM — no composed answer ***
*** ⛔ THE STOP: planFor returns notAsked, which is checked BEFORE any read ***
*** ⛔ THE TWO ARE TOLD APART ON OTHERWISE IDENTICAL INPUT ***
*** ⛔ THE WORKED EXAMPLE — 「三間門市」 on a turn that read nothing ***
*** ⛔ THE WORKING TURN STILL WORKS — the URL, clean ***
*** ⛔ THE ZERO-SOURCE BYPASS IS CLOSED — a claim with NO proof is refused ***
*** ⛔ TURN 1 — the URL ships and NO internal-vs-public question survives ***
*** ⛔ TURN_ROUTER is NOT the A4 rollback — the flags are independent ***
*** ⛔ WIRED: a contradicting order is WITHHELD by validatePlan, not just judged ***
*** ⛔ WIRED: an ORDINARY question is untouched — no new refusals ***
*** ⛔ WIRED: an undeclared superlative is WITHHELD ***
*** ⛔ ZERO ROWS FROM A HEALTHY READ IS STILL SUCCESS — the fix must not cry wolf ***
*** ⛔ a 30-day count cannot become a source-total claim, end to end ***
*** ⛔ a READ in the world he did NOT mean is refused before the connector ***
*** ⛔ a REAL chat answer is still chat — the fix must not swallow the ordinary case ***
*** ⛔ a TIMEOUT is unavailable, never intent:chat ***
*** ⛔ a capability denial is NOT corrected when the source really was not read ***
*** ⛔ a claim cannot bind to a source that was not read ***
*** ⛔ a claimed scope that CONTRADICTS the evidence scope is unverified ***
*** ⛔ a field that HALVED is reported — a tier-only diff would call it unchanged ***
*** ⛔ a filtered endpoint may not produce an existence claim ***
*** ⛔ a marker with no claim in front of it fails closed ***
*** ⛔ a missing claim structure is UNBOUND — never row_local, never source_wide ***
*** ⛔ a missing content array is an error, not silence ***
*** ⛔ a per-RESULT unavailable marker is not filtered away as 「no rows」 ***
*** ⛔ a plan with nothing required produces no block rather than an empty heading ***
*** ⛔ a real failure is NOT dressed up as patience ***
*** ⛔ a record is included WHOLE or not at all ***
*** ⛔ a reply with no specific claim is silent — most turns must be ***
*** ⛔ a router failure falls back to the MODEL, never to a guess ***
*** ⛔ a row_local claim cannot reference a sourceId that was not retrieved ***
*** ⛔ a row_local claim stays VERIFIED when its source is truncated ***
*** ⛔ a source WITHHELD from the active provider executes ZERO reads ***
*** ⛔ a thinking-only response THROWS, and names the budget as the cause ***
*** ⛔ a timeout is FLAGGED as a timeout, not filed as a network failure ***
*** ⛔ a turn where the tool never ran is UNAVAILABLE, never LIVE-ZERO ***
*** ⛔ a two-model turn reports BOTH calls, not one label ***
*** ⛔ adding answerClaims changes NO owner-visible byte ***
*** ⛔ additionalProperties:false still holds, at every level ***
*** ⛔ admitsLimitation cannot license a claim from a TRUNCATED read ***
*** ⛔ admitsLimitation cannot license a universal claim with sourceTotal unknown ***
*** ⛔ an ENRICHING read is listed and not performed, and cannot sink the plan ***
*** ⛔ an INVALID claim declaration still changes nothing ***
*** ⛔ an absent model is NULL, never backfilled with the provider name ***
*** ⛔ an endpoint that returned no rows makes NO claim about its fields ***
*** ⛔ an endpoint the fresh read could not produce is an ALARM, never a silence ***
*** ⛔ an incomplete tool call is not a completed search ***
*** ⛔ an unknowable value is null, never a default wearing a fact\
*** ⛔ an unknown block type is an ERROR that names what it saw ***
*** ⛔ an unreadable store is LOUD — corruption is never read as "nothing learned" ***
*** ⛔ and a SINGLE-ITEM ranking section is refused when unproven ***
*** ⛔ and no logger interpolates a secret ***
*** ⛔ body.count is never called matchingTotal before it has been qualified ***
*** ⛔ but a REORDERED subsequence is not ***
*** ⛔ candidate, rejected and superseded NEVER appear ***
*** ⛔ clicking Save after a failed read sends NOTHING ***
*** ⛔ complete WITHIN SCOPE does not support a SOURCE-WIDE universal claim ***
*** ⛔ content that is NOT the disambiguation survives the correction ***
*** ⛔ createdBy confers NO validation authority, whatever it says ***
*** ⛔ daily-count items are 50 empty arrays — the count-vs-stock case has no per-item data at all ***
*** ⛔ empty and non-string inputs propose nothing, and never throw ***
*** ⛔ every property is in required, at every node — strict-mode compatibility ***
*** ⛔ every unusable resolver result fails closed to a QUESTION, never a read ***
*** ⛔ facts[] survive a TRUNCATED source — row-local truth is not set-wide ***
*** ⛔ intentFor is UNCHANGED — the first match, exactly as before ***
*** ⛔ intentKeys may only ever contain KEYS from the closed intent table ***
*** ⛔ inventory MAY assert its ranking; orderPlanning MAY NOT ***
*** ⛔ it decides NOTHING — there is no refuse/allow in the surface ***
*** ⛔ it does NOT run when rows were read — claim binding owns that turn ***
*** ⛔ kind is structural — replacing the TEXT changes no verdict ***
*** ⛔ live internal evidence never reaches the resolver ***
*** ⛔ modelCalls IS COUNTED, NOT DERIVED — and 4 is the default worst case ***
*** ⛔ no capability, tool, provider or network vocabulary ***
*** ⛔ no interface string is written in Cantonese oral register ***
*** ⛔ no keyword classifier, no domain rule, no holdout string ***
*** ⛔ no keyword router, and no holdout literal in the production prompt ***
*** ⛔ no logger field carries a turn\
*** ⛔ no network primitive exists on the public path ***
*** ⛔ no production runtime file imports the wisdom domain ***
*** ⛔ no production schema pairs a union type with an enum — use anyOf ***
*** ⛔ no production schema uses maxItems — Anthropic rejects the request ***
*** ⛔ no test NAME has disappeared since the ledger was written ***
*** ⛔ no test in this file can reach the Owner\
*** ⛔ nothing outside src/wisdom requires the wisdom domain at all ***
*** ⛔ on-the-cap is UNKNOWN, never false ***
*** ⛔ one source\
*** ⛔ propose() reports unavailable and creates NO proposal ***
*** ⛔ provider-neutral, network-free, write-free ***
*** ⛔ purchase-order items carry a NAME and no ingredient id — the join is a spelling match ***
*** ⛔ removal emptying the text cannot ship: composition + non-empty compose safely ***
*** ⛔ requiring the domain creates no files anywhere ***
*** ⛔ rowShape and queryScope cannot be confused ***
*** ⛔ servedBy carries the MODEL ID the adapter actually returned ***
*** ⛔ src/wisdom makes no network or model call, in executable code ***
*** ⛔ the API key never appears in a result, error or accounting line ***
*** ⛔ the CONVERSATION fallback narrows: the model decides only where a CHANGE is proposed ***
*** ⛔ the OTHER guard on this boundary reports the same way (routeEvidenceGuard) ***
*** ⛔ the ROUTER outranks the model: a hallucinated develop cannot become a work order ***
*** ⛔ the adapter NEVER invents a model, whatever is missing ***
*** ⛔ the adapter THROWS on unavailable — it never returns zero rows ***
*** ⛔ the bare catch is gone from the loop boundary ***
*** ⛔ the concurrency case does not use spawnSync — that is what made it fake ***
*** ⛔ the defect sentence SAYS it is a defect — it must not read as an answer ***
*** ⛔ the denominator moving with the business is NOT drift ***
*** ⛔ the deterministic step is PRESENT with model:null — not omitted ***
*** ⛔ the disambiguation cannot ship, and the fact does — now by construction ***
*** ⛔ the event ledger holds ids and states, never lesson content ***
*** ⛔ the extractor sees the shapes this suite actually uses ***
*** ⛔ the false positives the measurement rejected stay rejected ***
*** ⛔ the fence can see the shapes it is built for ***
*** ⛔ the model\
*** ⛔ the prompt catalogue carries SHAPES and not one data row ***
*** ⛔ the ratio travels on BOTH sides of every threshold ***
*** ⛔ the resolver is NOT told the proposed world or what is available ***
*** ⛔ the resolver sees Owner words only — and is told nothing about the system ***
*** ⛔ the schema is closed, portable, and carries no reasoning field ***
*** ⛔ the schema is strict, closed, and carries no reasoning field ***
*** ⛔ the success line and the failure line are not the same line ***
*** ⛔ the universal-claim gate fires when completeness is UNKNOWN ***
*** ⛔ there is no WISDOM flag anywhere — not in the launcher, not in flags.js ***
*** ⛔ there is no cost entity and no costing operation to name in the first place ***
*** ⛔ there is no field through which raw evidence could reach the block ***
*** ⛔ there is no reflection worker and no candidate auto-writer ***
*** ⛔ three facts against one endpoint are ONE read ***
*** ⛔ totalCount is GONE — not aliased ***
*** ⛔ two kinds of null are told apart by `deterministic`, never by absence ***
*** ⛔ what she CANNOT answer is named, not left to be discovered ***
*** ⛔ wisdomRecall has no default store reach-through ***
*** ⛔ 「${heading}」 PRESENTS AS A RANKING AND CANNOT SHIP UNDECLARED ***
*** ⛔ 「我哋現有」 — the exact phrasing that leaked — cannot survive ***
*** ⛔ 「給我 Aroma System 的 website」 MUST NOT REGRESS — it asked nothing and read nothing ***
*** ⛔ 「給我 X」 proposes no change, so the guard may reject a develop claim ***
*** 「1」 after a numbered list keeps 香香\
*** 「A 定 B」 is not one question — it becomes no question, not an invented one ***
*** 「Aroma System」 now reaches the Owner AS 「Aroma System」 ***
*** 「今天幾號？」 likewise ***
*** 「今日的安排目前讀不到」 is caught — 安排 is what this turn was about ***
*** 「你可以幫我做什麼？」 is CONVERSATION and asks for no source ***
*** 「幫我改 docs/canary/agent-canary.md」 is ACTION ***
*** 「幫我睇 Calendar」 gets the answer, NOT 「我未有建立提案」 ***
*** 「我目前讀唔到你的日程」 with the calendar live is still corrected ***
*** 「最近有哪些發票？」 is BUSINESS_QUERY on the invoice domain ***
*** 「現在是幾點？」 is UTILITY, and asks for no source at all ***
*** 「現在是幾點？」 reads NOTHING and calls no model ***
*** 「讀唔到嘅直接講讀唔到」 is recognised as an instruction, never searched for ***
*** 三項 is a count claim and is checked — the ASCII case was never the only one ***
*** 下午 4 時 verifies against a stored 16:00 ***
*** 唔該 is politeness and must never read as a refusal ***
*** 早晨 / 午安 / 晚安 by the hour ***
*** 結果摘要 is one sentence of counts — never item detail ***
*** 缺口 is computed by the SERVER from two declared metrics ***
*** 聽日 and 琴日 are computed, not refused ***
.aroma/tasks, .aroma/results and data/* are NEVER touched even when aged (durable evidence safe)
0 with a real run time is healthy
1. lists terminal results — succeeded + failed both present with correct summary
10. filters — ?status= and ?since= narrow the list purely (no state)
2 to 24 hours: struck through, and it says why
2. provenance per item — proposalId, executionId, confirmedBy/At, sourceTaskId all resolve
267009 RUNNING and 267008 READY are also not failures
267011 SCHED_S_TASK_HAS_NOT_RUN → not unhealthy, and says it has not run
3. non-terminal excluded — running (exec, no result) and pending (proposal, no exec) absent
4. ROUTE ORDER — /proposals/results (alias) hits the list, NOT the :id handler; /return-ready works
5 of 6 clean → the blocked one is NOT absorbed into the calm sentence
5. zero dispatch on read — hitting the endpoint calls dispatcher/runner 0 times
6. read-only / no mutation — artifact files + proposal count unchanged after the call
7. allowlist / no over-exposure — items carry sourceTaskId but never task(prompt) or sandbox
8. corrupt-artifact resilience — a malformed result file is skipped + counted, never crashes
9. empty state — no artifacts → { items: [], count: 0, malformed: 0 }
A JUDGEMENT BOUNDARY, recorded rather than hidden
A1/A2 draft_proposal parses recipient/purpose/tone/constraints + signals
A3 classifications FACT/PREFERENCE/TEMPORARY are distinguished
ACCEPTANCE (1) commit + tasks:[] → tasks stays [], no synthetic task
ACCEPTANCE (2) commit + tasks field MISSING → normalizes to [], no synthetic task
ACCEPTANCE (3) commit + blank/invalid-title tasks → filtered to [], NOT backfilled from reply/decision
ACCEPTANCE (4) commit + real tasks → preserved unchanged (title/count identical)
ACCEPTANCE (5) chat routing unchanged — no Decision/Task
AGENT_BRIDGE OFF: a full EXECUTE triple is NOT authorized and no runner exists
AND AT RUNTIME: a data-derived key cannot resolve
ANTHROPIC_API_KEY is deliberately NOT in the allowlist (subscription, not API billing)
APPLYING without a prior PENDING_APPROVAL and approval throws
AUTHORIZED dispatch: DISPATCH_CLAIMED written AFTER auth and BEFORE the spawn, with linkage, durable
Apply returns ok false when the fake stdout carries no backup reference
Apply returns ok true and the parsed backupRef, tolerating a non-English label before it
Apply to dev returns allow with requires_backup true
Apply to dev returns ok and the adapter is called exactly once
Apply with no backup reference in stdout returns ok false
Apply: a non-English label before the backup reference yields the bare reference
Apply: stdout with no backup reference still yields ok false
B1 ask mode: draft null, non-empty restatement + clarifyingQuestion
B2 ask mode with a draft is rejected (mode/draft inconsistent)
B2-9 INVARIANT: constructing/loading the store triggers NO dispatch (spy 0)
B2-9 preserved: flag-off (authorizeDispatch false) → not_authorized, 0 claim, 0 execution
BACKGROUND statements are NON-COMMIT (no Decision/Task), stable across 3 runs
BOTH outcomes are audited — success and failure — through the real store
Bounty resolves to the Bounty button, and it is not the first on the page
C1 draft_proposal: draft object complete
C2 draft_proposal with a clarifyingQuestion is rejected
CAP 3: a workspace that refuses (not under tmpdir / remote survives) stops the run and is audited
CAP 5: a Work Order naming a forbidden file is rejected BEFORE anything runs
CAP 7: an append-only audit record is written for BOTH success and failure
CAPS 1-4 + 8 re-asserted post-wiring: no bypassPermissions, timeout/cost, no remote, fail-stop
CHAT LANE: even a develop-intent turn only PROPOSES — sealing needs a separate Owner act
CHAT LANE: 「批准、立即執行」 produces no Work Order, no seal and no execution
CHAT lane requests maxTokens 8192
CJK numerals are caught too — the numeric half is not ASCII-only
CLI dry-run writes no file (all fs writes disabled)
CRITICAL: Deploy to production without approval is pending, adapter NEVER invoked
CSRF matrix: method, Origin, Host and Sec-Fetch-Site are ALL enforced, fail-closed
Cantonese on explicit request only
Cantonese particles still segment a clause
Case A: WORKER off, DEVELOP off → dispatcher 0, worker 0, not_authorized, proposal confirmed
Case B: WORKER on, DEVELOP off → sandbox worker runs; Develop dispatcher 0
Case C2: WORKER off, DEVELOP on, NO dispatcher injected → nothing dispatched; not_authorized (no productionDispatcher fallback)
Case C: WORKER off, DEVELOP on, dispatcher INJECTED → dispatcher called exactly 1; worker 0
Case D: WORKER on, DEVELOP on → configuration_conflict, both paths 0, no side effects
Case E: DEVELOP wrong-case/misspelled/illegal → treated as off; dispatcher 0
Case F: an injected spy is the ONLY dispatcher reached — production wiring never touched
Change C: ACTION_HONESTY_GUARD reaches the adapter system on the demo path
Chrome signed into Google blocks the session
Claude still receives recall + read-context on its own lane (unchanged)
ClaudeAdapter WITHOUT responseFormat => legacy body (no output_config) + exact legacy headers
ClaudeAdapter maps responseFormat -> output_config.format {type,schema} with NO name, NO beta header
ClaudeAdapter maps stop_reason → stopReason (and null when absent)
ClaudeAdapter rejects malformed responseFormat BEFORE any network access
D1 server-fixed fields appended after successful parse (ask)
D1 server-fixed fields appended after successful parse (draft)
D2 model output containing gmailDraftCreated is rejected
D2 model output containing proposalCreated is rejected
D2 model output containing stage is rejected as authority key
D2 sent/approved/executed/persistentMemoryWritten all rejected
D3 server-fixed values cannot be overridden by model text
D4 model-supplied requestId is rejected as unknown key
D4 server-appended requestId matches the caller requestId
DEFECT (a): no input uses its required value as the placeholder, and submits are gated
DEFECT (b): a new card clears the previous red errors
DEFECT (c): the failure message is not doubled
DEFECT (d): the card renders the server-built sections, never a raw brief
DEMO_HTML: ONE composer — no permanent mode controls, two shortcuts behind "+"
DEMO_HTML: no storage/cookies, no innerHTML/eval/new Function
DEMO_HTML: safety labels + unknown fallback + Enter/Shift+Enter
DEMO_HTML: same-origin fetch target only, no external URLs
DEMO_HTML: the approval card is a viewer + four fields of intent, and holds no authority
DEMO_HTML: the emitted browser script is syntactically valid JS
DISPROVEN and SUPERSEDED are distinct statuses, not one "not current"
DOM.resolveNode failing is refused too — measured: it can also SUCCEED on a removed node
DPI is carried on both the result and the audit, not recorded once per machine
DUE and NEVER_RUN are visually distinct from FRESH, but none of them is an alarm
Deploy to production WITH approval proceeds to the adapter
Deploy to production returns require_approval
Develop request to dev with no registered agent returns no_agent
Develop whose stdout says no files were changed returns ok false
Develop with a cost figure in stdout yields that number in cost
Develop with a valid patch path returns ok true with that exact patchPath
Develop with no cost figure yields cost null and never zero
Develop with no patch path in stdout returns ok false and patchPath null
Develop: a POSIX patches path yields exactly that path
Develop: a non-English label before a Windows patches path yields exactly the bare path
Develop: a patches path surrounded by quotes and trailing whitespace yields the trimmed bare path
Develop: stdout with no patches path still yields ok false with patchPath null
Develop: two candidate patches paths yield the last one
E1 malformed JSON rejected
E2 extra prose / markdown fence rejected
E3 duplicate keys at top level rejected
E3 duplicate keys nested rejected
E3 escaped-equivalent duplicate at top level rejected
E3 escaped-equivalent duplicate nested rejected
E4 unknown key rejected
E5 invalid enum rejected (classification/source/confidence)
E5 invalid enum rejected (mode)
E6 empty required field rejected (no signals)
E6 empty required field rejected (restatement empty)
E7 inconsistent mode/draft combo rejected (ask + draft already covered; draft w/o object)
EARLY failure BEFORE the model call → outcome line STILL produced (the invisibility fix)
EMAIL_DRAFT lane is untouched: U1 early-return, U1_MAX_TOKENS still 1024
EMPTY: read succeeded and there is genuinely nothing
END TO END: a refused run writes a REAL audit file through the REAL store
EVERY approval attempt is audited — accepted, refused and expired alike
F1 real red-line: blocked===true and adapter/model call count === 0
F2 U1 modules contain no direct write/dispatch/persist call sites
F2 U1 modules do not import any Gmail/Gateway/store/dispatch/memory/fs-write seam
F3 adapter failure wrapped in IntakeUpstreamError with requestId
FAIL-CLOSED: any unrecognised value withholds, and never shares
FAILED without a locus is refused
FIELDS is the whole contract — adding one is a deliberate act
FILES_WAITING: counts files nested one level inside the batch folders
FIRST RUN: every ingredient uncomparable, and the calm sentence is absent
FLAG OFF → adapter system is BYTE-IDENTICAL to the pre-contract composition
FLAG ON → contract present, AFTER persona+guard, BEFORE the classifier
FLAG ON → the frozen persona and guards are still verbatim in the system
FLAGS OFF → adapter input BYTE-IDENTICAL to today and ZERO source reads
FOLDER_NOT_FOUND is its own state — a 404 must never read as "nothing to move"
FRESH does not depend on the witness — a recent row is recent either way
G1 flag OFF/non-true (${JSON.stringify(flagVal)}): legacy path, U1 not entered
G2 flag ON: U1 uses runtimePersona().personaText and early-returns SHADOW_ONLY
GATE absent interactionMode → byte-identical (exec1 still → execution_proposal, gate not entered)
GATE chat+commit → talk-only: no Proposal/persist/dispatch (promote never called)
GATE chat+non-commit → normal talk path (not the talk-only intercept)
GATE proposal+non-commit → deterministic clarification (no fabricated task/proposal)
GATE proposal+single-commit → existing proposal-only path unchanged
GET .../attachment returns exactly what would travel
GET /api/v1/context/health
GET /api/v1/demo/enquiries — the mount that silently did not happen
GET /api/v1/demo/enquiries/:id
GET /api/v1/demo/greeting — the backlog line rides here
GET /api/v1/demo/version — the stale-tab guard
GET /api/v1/home/briefing answers through the real app
GET /api/v1/home/section/recall answers with a detail, not a 404
GET /api/v1/runs/:id responds without a token
GET /demo OFF → 403 demo_disabled; adapter/processIntake not called
GET /health
GET /health responds 200 with no Authorization header
GET continues
GPT fails AND Claude fails -> the existing safe error propagates (no loop)
GPT parse failure -> Claude once, and GPT USAGE IS RECORDED (two provider calls billed)
GPT provider error -> Claude called exactly once; turn succeeds
GPT receives persona+guards+contract+classifier AND the same recall/read-context Claude gets
H1 fake adapter returns complete CompletionResult shape
HARD invariant: created claim ONLY when proposalCreated===true AND a real id
HEAD and OPTIONS continue — neither changes anything
HTTP: OFF success unchanged — 200 with requestId, no top-level detail/error
HTTP: malformed output → 500 invalid_llm_output, safe body, no raw/detail leak
HTTP: provider failure → 503 llm_unavailable, no provider text/path/stack
HTTP: unknown provider (throws before service) → 500 internal_error via ctx fallback id
ISOLATION (runtime): a chat turn with recall + read-context NEVER touches the runner
ISOLATION (structural): chat / recall / read-context source files contain NO agent or dispatch reference
ISOLATION: the agent runner has EXACTLY ONE call site, in the ONE shared confirm service
InlineTextBox is dropped — it duplicates StaticText and is pure noise
L1 stopping the click yields STOPPED_FOR_YOU with a complete report
L2 U1-only result shape proves the flag-ON call returned through U1
L3 scoped exact-call reachability: mount wiring inside runIntakePipeline
LAUNCHER names both halves, so a reader can find them
LAYER 1: a forbidden target is rejected and NO work order is produced
LAYER 2: a result is write-once and the route is loopback + same-origin bound
LAYER 2: the result view reports the runner outcome, and is a pure read
LAYER 2: with AGENT_BRIDGE OFF a correct approval produces NO result at all
Louie is called Chef here, and the classifier no longer contradicts it
MockAdapter WITHOUT responseFormat keeps legacy behaviour (returns text string)
MockAdapter with responseFormat returns a deterministic fixture that PARSES
MockAdapter({supportsStructuredOutput:false}) fails closed on responseFormat
NEVER_RUN makes no claim in any of the four fields
NO AMEND PATH: adding a file requires a NEW order (different approvalId AND hash)
NO COORDINATES anywhere in the output
NO DECLARATION MEANS WORKING NOTE — fail-closed, never ACTIVE
NO browser tool is reachable — measured TOOL_NOT_AVAILABLE, and never a default here
NO double-logging on any path
NO expansion / substitution — output contains ONLY the task title+note verbatim
NO write method exists on ANY of the four adapters
NOTHING IS DROPPED — the full entries are always on the object
NOT_CHECKED has no checkedAt and no label
NOT_LOOKED when there is no drive service at all
OFF byte-identical: no persona/honesty frames; reply is the raw model reply
OFF: /api/v1/intake response carries NO demo fields (contract unchanged)
OFF: response contract + adapter args unchanged (no demoOutcome; raw distill prompt/system)
ON execution: 0 task → clarification (no_actionable_task); no promote
ON execution: >1 task → clarification; promoteToProposal NEVER called; no proposals
ON execution: domain-seam failure surfaces the domain error, not a proposal
ON execution: seam not wired → visible promoteErrors, NO fake proposal
ON execution: single task → official Proposal from the domain seam; state only from the record
ON: Speech/Context/Clarification add demoOutcome only; no proposal; no invented state
ON: a conversational turn (chat) gets demoOutcome but creates no Proposal
ON: execution → official pending Proposal in the SAME store; visible in GET /proposals; NO new Run/dispatch
ON: persona guard reaches the adapter system; context card sanitized + warnings surfaced
ONE broken source does not block, fail, or infect the others
ONE source can be withheld from OpenAI while Claude keeps it
OPTION B: a file NOT mentioned in the conversation is rejected
PARSE-FAILURE turn → outcome line present, with parseResult/parseErrorReason
PERSONA_IDENTITY equals the frozen, Owner-signed-off payload verbatim (no drift)
PERSONA_IDENTITY is untouched: no contract text leaked into it
POST /api/v1/demo/intake OFF → 403; adapter/processIntake not called
POST /api/v1/runs with a wrong token responds 401 and creates no Run
POST /api/v1/runs with the correct Bearer token is accepted and returns a run id
POST /api/v1/runs without an Authorization header responds 401 and creates no Run
POST /api/v1/runs/:id/approve without a token responds 401 and leaves the Run unchanged
POST /runs on the unprefixed path with the correct token is also accepted
POST invalid (
PROOF 1 — chat + flag ON → adapter input contains EXACTLY ONE recall block
PROOF 2 — chat + flag OFF → adapter input BYTE-IDENTICAL to baseline (no recall)
PROOF 3 — proposal + flag ON → BYTE-IDENTICAL to proposal baseline (builder never called)
PROOF 4 — proposal + flag OFF → no recall block
PROOF 5 — missing interactionMode + flag ON and OFF → BYTE-IDENTICAL to legacy baseline
PROOF 6 — U1 + flag ON and OFF → recall builder NEVER called; path unchanged (SHADOW_ONLY)
PROOF 7 — B2 chat gate stays talk-only (flag ON): chat+commit → no proposal produced
PROPOSAL lane still requests 1024
PUT, PATCH and DELETE are aborted too
R1 history uses h.text (regression)
R1b history falls back to h.content when text absent
R2 malformed JSON that passes shape guard hits MALFORMED_JSON branch
READ_ACCESS off → nothing built at all; enabledSources empty
READ_FAILED carries a reason and is NOT the empty answer
REAL paid bridge E2E — Task→promote→confirm→worker→Result API, full provenance
REAL paid unattended E2E — Capability Verification Task
Rollback to production returns require_approval
S1 flag OFF: legacy distill path supplies NO responseFormat
S1 flag ON: processIntake -> U1 supplies responseFormat with the U1 schema name
SAFETY_HEADER prose names NO source — the list is data, not text
SENTINEL: no prompt / user content / model output / credential in any outcome line
SUCCESS turn → exactly one outcome line with the expected fields (incl. stopReason)
SYSTEM_PROMPT declares the context/background category + non-commit rule
THE HR-12 CASE: a claim about a set whose FILTER removes the answer
THE REAL composition root builds a runner WITH an audit store
TRUNCATION IS STATED, with what was dropped
TTL resolver: unset → 24; illegal/negative/zero/non-numeric/infinite → 24 (fail-safe); valid → parsed
U1 accepts a clean raw JSON response (draft_proposal) through the unchanged parser
U1 hardening: system prompt gives first-person owner-voice tone guidance
U1 hardening: system prompt states the raw-JSON / no-code-fence output rule
U1 negative lock: markdown-fenced JSON is STILL rejected (parser not loosened)
U1 parser stays strict: a fenced JSON response still throws EXTRA_PROSE
U1 passes responseFormat exactly once with the U1 schema + name
UNAMBIGUOUS nodes get no group — context is earned, never universal
UNAUTHORIZED (gate false): NO DISPATCH_CLAIMED, dispatcher spy 0, no execution (B2-9 preserved)
VALIDATION rejection (before adapter acquisition) → outcome line produced
WYSIWYA: the displayed object IS the hashed object (same serialization)
a BMP private-use glyph is removed from the name
a CLEAR result speaks and proves it looked — silence cannot distinguish clear from broken
a CORRUPT store reports UNREADABLE — never EMPTY
a CRA origin is refused even when the order explicitly names it
a Chinese-numeral count that IS wrong is dropped through the real validator
a Claude-served turn DOES fetch it — the boundary is unchanged
a DIFFERENT path on the allowed origin is not
a DISPROVEN defect is indexed AS disproven — not merely present
a DISPROVEN entry renders as disproven — this is the DEFECT-001 case
a DISPROVEN entry says so in the text that reaches her
a Develop dispatch where every agent fails returns failed carrying agentId, cost, latencyMs, error and an attempts array
a FAILED attempt also burns the nonce — a refusal is not a free retry
a FAILED execution result is readable
a GET that commits is NOT caught, and the fence says so about itself
a GPT-served turn fetches only what it is PERMITTED — and nothing when all is withheld
a LIVE setting says so too, so the two are distinguishable
a NEW recall is named with its ingredient and date
a NON-EXISTENT path REFUSES to seal, with a reason the Owner can read
a NOT_WIRED store makes no freshness claim either
a PARTIAL execute triple never authorizes (all three fields required)
a PARTIALLY shown group states shown-of-total on its own line
a POSITIVE claim supported by NO_RELEVANT_RESULTS is refused
a PROPOSAL needs an instruction to change a file, not a question about changing one
a QUESTION about a setting is not a request to change it
a REAL instruction in the chat lane still gets the "say what to change" notice
a REAL instruction still routes on its own words, whatever came before
a REFUSED action does not spend the read — nothing happened, so nothing changed
a Run can never be created or dispatched with a targetProject of production
a StaticText with no interactive twin is untouched
a WORKING NOTE citation says so in the citation itself
a Work Order cannot be sealed without a real, pending Proposal
a backend Run dispatches a snapshot carrying targetProject backend
a banned key hidden deep inside the input is still caught
a bare imperative fires — 幫我 is politeness, not a requirement
a black capture in a disconnected session reports BOTH reasons, not just the first
a browser-supplied workOrder / allowedFiles / caps is REJECTED, not silently ignored
a caller cannot ask for an unbounded wait
a caller cannot supply windowChanged itself — it is derived, never accepted
a cancelled proposal can never be confirmed and creates no Run
a capability question is a question, never an instruction
a card in the profile means NO BROWSER IS STARTED
a cause asserted with no measurement in the same report is refused
a caveat about the ANSWER and a caveat about the METHOD render in separate sections
a child given an explicit AROMA_DATA_DIR uses it — the pattern atomicStore already uses
a citation ALWAYS carries the status and the date
a claim about the ONE source that failed is not corrected because another was live
a claim from a TRUNCATED read is refused
a claim that ADMITS the limitation is allowed — and NOT because of the flag
a clean turn still reads validated, with both counters at zero
a clean validated turn carries no correction at all
a clear line is SHORTER than a waiting line — lighter when clear, heavier when something waits
a clear result SPEAKS, and proves it looked by naming the time
a complete read supports a claim about its own contents
a completed enquiry reports CONCLUDED and the real cost
a confirmed proposal cannot be confirmed twice — only one Run is ever created
a connector-level unavailable result becomes an UNAVAILABLE line
a context/background classification is NON-COMMIT — no Decision, no Task
a correctly alternating plan passes
a correctly transcribed metric is unchanged
a corrupt file is skipped, not fatal — one bad file may not hide the rest
a cross-site login submission is refused outright
a date-shaped business question is not a date question
a date-shaped business question is still not a date question
a day where something new appeared names it
a declaration buried past the header block does not count
a declared status is read from the document itself
a defect found in passing survives into the report
a development request yields a Proposal whose task is the exact verbatim worker string
a different METHOD on the allowed path is not
a different scheme on an allowed host is refused
a digit-heavy value is classified without being quoted
a directAnswer that loses every sentence does not stand as an answer
a directory that does not exist yet lists as empty rather than throwing
a directory, an unreadable file and a path outside the repo all refuse
a dry-run changes nothing on disk — the approved root is untouched
a due date is never used as the row date
a fact value that is not a real value is dropped
a failed load can be retried
a failed read SPEAKS — silence would read as "nothing waiting"
a failed turn leaves no half-conversation behind
a failing apply appends ROLLED_BACK with the error and never COMPLETED
a failing runner status yields ok false with an error
a failure explains itself using the runner\
a failure reason is SCRUBBED of urls, paths, emails and opaque tokens
a field NAMED like a credential is refused too
a file edited by hand is picked up too — the cache is one second, not the process life
a form label beside its field collapses to the field
a fresh store reports EMPTY with a reason
a frontend Run dispatches a snapshot carrying targetProject frontend (the adapter input the dispatcher builds)
a full EXECUTE triple with the flag ON hands off exactly once, with the approved hash
a full page reports itself as truncated rather than looking complete
a fully shown group carries NO count — a suffix that always fires is ignored
a genuine total read failure still says so plainly
a genuinely empty calendar still reports read-OK, never UNAVAILABLE
a gone element refuses before typing
a greeting yields intent chat and creates no Proposal
a group that ends up with zero members is dropped, not printed as 「0 of N」
a group with NO room for members is dropped whole, never left as a bare header
a halted investigation can never render as a completed one
a hand-edited value outside its range falls back to the default
a happy path sequence of stages is accepted in order
a hash that does not match what was displayed is refused (no execution)
a healthy run records a row and reports what it did
a held lock blocks it, and nothing is deleted
a hint on a NON-chat lane is ignored entirely
a hint sent to a NON-chat lane never even appears in the engine opts
a hostile id cannot escape the store directory
a huge page is bounded
a hung site is bounded — the errand gives up rather than running forever
a key that is not key-shaped is refused before any lookup
a kind that has run is openable
a kind whose only rows are BLOCKED is still openable — the reasons are content
a limitation about something genuinely new still reaches him
a limitation that survives is not counted
a live proposal takes precedence for provenance
a live read with NO fallback is corrected without the out-of-window wording
a lock file is reported as LOCKED and left alone
a logging failure can never break a turn
a login wall is BLOCKED_BY_SITE and says so — she never types a credential
a long message is never treated as a short reply
a long or non-string detail is dropped, not carried
a lookup that also asks for a change is NOT exempt
a made-up path still 404s — otherwise this file proves nothing
a mail item renders a complete date in a multi-source turn
a mail question still reads mail — that is the domain, not a hunch
a malformed id cannot become a file path
a malformed or misdirected frame is refused at the boundary
a malformed timezone makes startOfLocalDay and formatLocal throw too
a matched message still yields the same plan it always did
a met condition and a timed-out one are distinguishable outcomes
a metric the ROW does not carry is still dropped — nothing is invented
a missing Preferences file is unsafe, not fine
a missing approved hash is refused (execution always requires an explicit approval)
a missing mode is coercion too — absent and unrecognised both end as chat
a missing or unreadable Preferences file is unsafe, not fine
a missing order blocks — an absent fence is not an open one
a mix: one comparable and clean, one with no history → BOTH stated, neither hidden
a money value matches the bare number the row carries
a month or an invoice number is not an expression
a name that was ONLY a glyph becomes empty, not a ghost string
a named mechanism turns a refusal into BOUNDED
a non-browser caller gets a plain 401 it can act on, not a redirect
a non-exact interactionMode is NOT treated as chat
a non-integer is refused rather than coerced
a non-parse error still logs the historical shape (no forensic fields invented)
a non-zero exit status returns ok false regardless of stdout
a nonce from a DIFFERENT session cannot be used
a nonce from another order is unknown here
a normal turn reports the counts it actually had
a number from a source that declared itself a sample must be marked
a page that fits is NOT marked truncated
a page with nothing actionable yields NOTHING, not a tree of empty divs
a parsed result carries cost and turns through
a payment-named control is REFUSED by click itself
a phase is one of a fixed set — anything else is refused, never passed through
a plan for a disproven fix does not read as a ruling
a plan that acts twice on one read is refused without running
a policy-denying dispatcher produces DENIED and no AGENT_SELECTED stage
a positive claim from a set the source called a SAMPLE is refused
a predicate cannot be smuggled in — there is no code path for it
a prefix match does not pass — costco.ca.evil.com is blocked
a present-but-malformed Authorization header (missing Bearer prefix) responds 401
a promoted Proposal brief no longer leaks Title:/Details: into the goal
a question about a ruling plans a RECORD read, not a commit list
a question or an offer is not a claim
a read FAILURE speaks — silence there would read as "nothing waiting"
a read that DOES carry its own time keeps it, unchanged
a real change instruction still gets the "say what to change" notice
a real command (commit) still produces a Decision + Task
a real exit code 1 still gets the profile-visibility hint — it is the measured trap
a recall the site returned but whose title lacks the query word is KEPT
a recent run reads FRESH and still says its age
a ref from another source does not validate an aroma_system claim
a ref is reproducible for the same node, so a re-read is cheap and safe
a ref maps back to exactly one node
a ref survives OTHER nodes disappearing — the property an index does not have
a ref that was never read at all is refused too
a ref whose element left the DOM is refused
a refused type is BLOCKED_BY_SITE and carries the refusal reason
a registered, enabled, healthy task → INSTALLED, scheduled true
a rejected Run cannot afterwards be approved
a rejecting dispatcher yields a FAILED stage carrying the error, no unhandled rejection, terminal status
a rejecting runner on Apply also yields ok false with an error
a repeat that quotes only the source\
a reply that claims nothing about reading is untouched
a reply with no Han characters is NO_EVIDENCE, not CLEAN
a report carries outcome, answer, measurements, not-established and cost
a report with NO applied changes says so explicitly rather than staying silent
a report with method caveats but no answer caveats still says so, rather than going quiet
a role cannot send a message that belongs to the other direction
a row carrying its own nextRunAt is judged against THAT, not against a recomputed one
a row that genuinely found nothing is still a clean ANSWERED zero
a row with NO items field reports UNRECORDED, not an empty list
a row with no usable time cannot be judged, and says so instead of reading as ancient
a run still in flight is reported as running, not as "nothing happened"
a runner that rejects yields ok false with an error, not an unhandled rejection
a runner that resolves after a delay does not block: an independent task completes first
a sandbox outside tmpdir refuses BEFORE spawn — so cwd never reaches spawn with a bad value
a schedule is not a clock
a schedule question shows the calendar and nothing else
a second knock inside the window is TOO_SOON
a section is capped at five items and says how many more
a section that DID read carries a time; one that did not, does not
a section that never ran attaches that fact rather than nothing
a sentence about nothing in the registry fires nothing
a sentence with no change verb at all never fires
a session revoked by logout stops working immediately
a settings file with no timezone field → the default
a short page is not truncated
a short reply of shared characters passes
a short reply that the classifier did NOT read as a commit is untouched
a short reply with NO history is not a continuation
a single genuine question survives untouched
a single 更新於 line is rendered at the top
a source that could not be read is still listed as attempted
a source that genuinely WAS unavailable is never corrected
a source that genuinely failed is still never corrected
a source that genuinely fails is still reported as unavailable
a source that never resolves cannot take the turn down (all sources broken)
a source that was not read live asserts nothing
a source with no declared derivations gains no line
a source with no declared derivations gains none
a stage missing its required facts throws
a stamp exists and is short enough to eyeball
a status enum still translates on the unchanged path
a status enum still translates, and a raw enum never survives
a step path must be BOTH inside the root and covered by allowedPaths
a step whose action is hostile text is refused, and the text is not echoed
a stop carrying a typed value is refused by the store and recorded as a defect
a stop is final — there is no resume
a stop is recorded as STOPPED_FOR_YOU, carrying its stop report
a stopped enquiry must carry what it did NOT establish
a stopped supervisor refuses, and still writes the record
a store that CANNOT be read makes no freshness claim at all
a successful Develop leaves the Run pending approval: PATCH_READY then PENDING_APPROVAL
a successful append stays quiet — the line means something went wrong
a successful dispatch increments the agent's runtime health sample_count
a supplier and a product name are never transliterated by code
a throwing apply dispatch also rolls back, carrying the thrown error
a throwing dispatch reports FAILED rather than an empty conclusion
a throwing read → trust:unavailable + plain reason (no guessing)
a throwing worker still records a failed Result (no unhandled rejection)
a timezone that cannot be trusted makes the clock question fall through too
a true claim about aroma_system is still NOT corrected
a truncated excerpt is disclosed on the card
a turn that already has a proposal declines with its own reason
a turn that read NOTHING does not ask for a plan
a turn that read NOTHING is still not asked for a plan at all
a turn with no conversationId still answers, and writes nothing
a unit code with no declared label renders as itself, never guessed
a valid hint chooses the provider for a CHAT turn
a valid service token is accepted as an alternative credential
a visibility wait without a ref is refused rather than widened
a warning is emitted so the redirect is never silent
a well-formed order validates, and is fail-closed on anything malformed
a wider grant must be asked for AT THE CALL SITE, per dispatch
a wildcard origin is NOT honoured — there is no way to say "anywhere"
a write the order named is allowed
a wrong password is refused and tells the caller nothing
a wrong-type error is carried as its own reason
aborts are counted and described
accept: well-formed values
acting before ANY read is refused
adapter called with fixed maxTokens=1024 and temperature=0.2
adapter errors never leak the provider body, and the key is never returned
adapter maps the documented Responses shape to the shared result shape
adapter.invoke returns a Promise and awaiting it yields the same result shape as before
admission is fail-closed on malformed input
agentsProviding returns claude-code for Develop@1 and empty for an unprovided version
agreement is named for each direction, so a disagreement is not a diff exercise
all flags OFF (default) → every source inert; adapter NEVER called
all of it is in the served page
all of it reaches the SERVED page
all of it reaches the SERVED page, not just the asset file
all sources failing + flags ON → reply NOT blocked (fail-soft)
all three bindings are now real, and the register says so
allowed reads are NOT logged — a record of everything is a record of nothing
an ABSENT allowedWrites blocks every write — an absent fence is not an open one
an ABSENT conversation is still null — genuinely not there
an ABSENT file is still legitimately empty — first run must work
an ABSENT or INVALID hint falls back to the flag-driven default, never to an error
an ABSENT order blocks everything — an absent fence is not an open one
an EMAIL request needs an act of writing, not merely the word email
an EMPTY allowlist blocks everything — the default is stop
an EMPTY window falls back to the next scheduled events, honestly labelled
an EXPIRED sealed record + nonce cannot be approved
an EXPLICIT interactionMode still wins — the "+" shortcuts and scripts keep working
an INJECTED workerDeps still wins, so tests and fakes are unaffected
an OPEN defect does not arrive looking handled
an ORDINARY confirm (no EXECUTE triple) can never start the agent, even with the flag on
an ORDINARY confirm never authorizes agent execution (flag ON, runner present)
an UNREADABLE probe is unclean too — blind is not clean (HR-23)
an UNRESOLVABLE CLI fails closed — no spawn, no bare-claude fallback
an abbreviation is refused at save time too, with the reason
an absent order blocks everything
an absent order refuses too — an absent fence is not an open one
an accepted knock is written with its time and caller
an action nobody gated is refused rather than allowed by omission
an adapter with no client fails-closed to unavailable
an aged amount is struck, and an expired one is absent rather than decorated
an ambiguous tag is refused rather than resolved by picking
an answer is recorded as ANSWERED, with the answer
an aroma_system claim is detected and corrected by name
an element behind a modal is STILL LISTED — hiding it would be a judgement
an element scope is reported as such
an email body ordering an action is injected verbatim as reference only
an email composition request is ACTION too — one router, not two
an empty attachment produces NO envelope at all
an empty conversation is not also listed in the sidebar
an empty ingredient list is refused — it would look like a working errand with nothing to do
an empty or non-string reply does not throw
an empty or whitespace first message still yields a usable title
an empty password never matches, even against an empty expected value
an enquiry can be saved and reopened whole
an enquiry gets READ tools only — it asks questions, it does not change files
an errand that throws is still recorded, and ok is false
an existing settings file without the field keeps working untouched
an expired order stops blocking, and stops being live
an expired session is refused
an explicit AROMA_DATA_DIR is used verbatim, test process or not
an honest reply over the same live read is untouched
an incomplete request does NOT offer — it is not the offer\
an inert READ is still allowed to answer emptily — it is not broken, it is empty
an ingredient list is only read when written as one
an injected save still saves
an intent that asks nothing of Aroma System also returns null
an invalid or absent flag value can never open the gate
an invalid order is refused before any step is resolved
an invented session cookie is refused
an invoice question reaches the invoices endpoint end to end
an item is two lines and carries no 來源 segment
an item that was never retrieved is an invention and is dropped
an llm returning a targetProject of production is rejected — no Proposal is created
an llm that returns text agreeing to proceed does not confirm anything
an observation request through the Companion is refused in stage 1
an opaque timeout becomes a named cause, and says what covered it
an openable section renders a control that opens it
an ordinary Traditional reply passes untouched
an ordinary conversational reply is untouched, byte for byte
an ordinary field is not refused
an ordinary file is still allowed — the rule is not just refusing everything
an ordinary repo question still plans the old way — nothing is taken away
an origin the order did not name is blocked
an out-of-range write is refused and NOTHING is stored
an out-of-scope run is STILL flagged — the fix must not blunt the real warning
an over-long list is refused with its cost, not silently truncated
an oversized line is CAPPED, not allowed to price out a source
an unavailable in-scope source is reported, not hidden
an undeclared entry has no date, and says that rather than inventing one
an undeclared source code renders as itself, never guessed
an unfilled slot is VISIBLE, not silently empty
an ungrounded limitation and follow-up are removed on the same rule
an unknown artifact kind throws
an unknown condition is REFUSED, never defaulted
an unknown conversation is 404, not an empty one
an unknown id is 404, not an empty enquiry
an unknown id is refused
an unknown id returns null rather than an empty enquiry
an unknown kind previews nothing, with a 404 rather than an empty attachment
an unknown locale falls back the same way in both
an unknown locale falls back to the default rather than to nothing
an unknown method is aborted — the default is stop, not pass
an unknown or missing kind returns null rather than throwing
an unknown or missing role is attributed to the OWNER, never to her
an unknown outcome from the errand becomes a recorded defect, not a crash
an unknown source name is shared by default (it is not a secret list)
an unknown stage name throws
an unmatched query returns nothing rather than everything
an unnamed origin is BLOCKED, and the reason names the origin
an unnamed 讀唔到 with a genuinely failed source is left alone
an unreadable database is NOT clean — unsafe unless proven safe
an unreadable values file is also defaults, not a crash
an unreadable witness claims NEITHER — 「I could not look」 is its own state
an unrecognised status word is NOT quietly treated as ACTIVE
an unrecognised value is still WARNED about — it is a typo, not a preference
an unresolvable CLI rejects with the resolver\
an unresolvable timezone degrades to the greeting-free line, never to a wrong hour
an unstable element is named unstable, not covered
and a source named in the SAME clause does
and an ordinary control still goes through — the stop is not a blanket refusal
and it is refused even when the order also names a legitimate file
and list() throws rather than returning [] when it cannot read
and the output SAYS they cannot be told apart
and the router hands the same single source through
and the rule is restored: the live matcher still refuses
answered, stopped-for-you and blocked-by-site are distinct states
anything outside the closed set is refused as out of scope, not attempted
anything under src/governance/ is un-allowlistable, including a file nobody has written
appendStage (via dispatcher) → restart → stage persists; deriveStatus unchanged
approveRun can never be steered to dispatch Deploy — only Apply@1
approveRun on a Run that is not pending approval is rejected and dispatches nothing
approveRun on a backend Run is rejected with a clear error and dispatches nothing
approveRun on a frontend Run dispatches Apply@1 (approved:true) and reaches COMPLETED with a backupRef
approveRun on an unknown run id rejects with a 404
approved hash !== executed hash -> REFUSED, audited, ZERO workspace prep and ZERO worker call
approved hash === executed hash -> the run proceeds
approvedBy is always set by the store and can never be supplied by the caller
aroma_system sends no q and has no fallback — the API ignores q, zero rows is an answer
ask → clarification
asking for force THROWS — measured: force:true on a covered button reports success and clicks nothing
asking for force throws
asking for headless THROWS — it is not silently corrected
assertAuthUrlValid: REJECTS a URL truncated at the first & (the reported bug)
assertResponseFormat accepts a valid contract
assertResponseFormat rejects ${label} with MALFORMED_RESPONSE_FORMAT
assertSandboxUnderTmpdir: accepts a real tmp subdir, rejects escapes/symlink-out
at most three sentences survive
attachmentFor takes a kind and rows — there is no message parameter
audit refuses an undeclared field rather than accepting a payload with it
audit: appends one immutable record per run
authorizeExecution: all off → not_authorized (default dormant)
authorizeExecution: any two-of-three on → configuration_conflict → zero
authorizeExecution: single-lane, needs its runner configured
bad input degrades to keeping everything, never to throwing
batch folders are read CONCURRENTLY, not one after another
best-effort: a delete failure on one entry is logged, sweep CONTINUES, summary reflects it
best-effort: sweep NEVER throws (missing/invalid tmpDir → empty summary, no crash)
bind failure → linkState linking_failed, worker NOT started, 500; retry resumes, no duplicate
blank/whitespace note → title-only (no empty Details section)
block carries the verbatim safety header, retrieval time, and cited+dated items
blocked:
bootstrap never routes the URL through a shell (no cmd/start usage)
both entry points reach the runner through the SAME service (one hash check, one gate)
both providers receive the SAME assembled context, from ONE fetch
bounds cannot be raised by the order itself
bridge pending + UNKNOWN linkState value → 409 fail-closed
bridge pending + linkState 'linking' → 409, no worker, no confirm side-effects
bridge pending + linkState 'linking_failed' → 409, no side-effects
bridge pending + linkState 'ready' → confirm succeeds; confirmedBy/At from the real confirm
bridge pending + linkState MISSING (null/undefined) → 409 fail-closed
broken Google creds → those sources skipped, github still registered; NEVER throws
buildApplyArgs produces exactly three elements: the script path, then --yes, then the patch path
buildArgs is byte-for-byte the B2-1 spike command
buildAuthUrl: refuses to build with any non-readonly scope
buildAuthUrl: response_type=code, access_type=offline, prompt=consent, exactly the 3 readonly scopes
buildDevelopArgs defaults to backend and includes --backend when targetProject is not given
buildDevelopArgs omits --backend when targetProject is frontend
buildDevelopArgs places --backend as its own element immediately before the task when targetProject is backend
buildResultView is an ALLOWLIST — poisoned artifact fields never appear
buildSafetyHeader lists exactly the sources given, and nothing else
buildU1DraftPrompt embeds persona text read-only
buildU1DraftPrompt is pure and returns { system, prompt }
builder: NO conflict resolution / dedup / supersession — duplicate statements both kept
builder: NO_RECORDS when zero decisions → block null
builder: RATIONALE is the only truncatable field (ellipsis, record stays ≤ perRecordCap)
builder: SELECT limit=5 (7 decisions → 5 records, TRUNCATED)
builder: SOURCE_UNAVAILABLE when a read throws → block null (fail-soft signal)
builder: STATEMENT never truncated — whole record omitted when it cannot fit, TRUNCATED
builder: charCap on the FULL serialized block; whole-record boundary; TRUNCATED
builder: deterministic sort — decided_at newest-first, undated last, id tie-break
builder: dry-run dirty -> generatorProvenance INCOMPLETE (no throw)
builder: explicit small charCap forces TRUNCATED
builder: listDecisionsFn + listTasksFn each called at most once
builder: proposalId → "linked to a Proposal" only; never a lifecycle string
builder: provenance real fields (no actor) + approval qualifier; approved_by never phrased as approved
builder: real + builder NOT in commit -> PROVENANCE_INCOMPLETE
builder: real + clean + builderInCommit + 40hex -> generatorProvenance COMPLETE
builder: real + dirty tree -> PROVENANCE_INCOMPLETE
builder: relationship != REFERENCE_ONLY -> RELATIONSHIP_INVALID
builder: short/invalid release commit -> RELEASE_COMMIT_INVALID
builder: supersedes valid path+hash -> recorded; prior artifact unchanged
builder: supersedes wrong hash -> SUPERSEDES_PRIOR_MISMATCH
builder: supersedes wrong reason -> SUPERSEDES_REASON_INVALID
builder: tasks max 3 by id asc + omission count + TRUNCATED
builder: valid productionReleaseReference recorded (REFERENCE_ONLY)
builder: verbatim safety header is the first line inside the wrapper tags
builds the EXACT spike command: -p <task> --add-dir <sandbox> --permission-mode bypassPermissions --output-format json
but naming the LIVE source while another failed is still caught
by default every source is shared with both providers — the Owner said so
calendar items carry the weekday from data
calendar: BOUNDED window — timeMax present and exactly windowDays after timeMin
cancel → restart → status/cancelledBy/cancelledAt persist
caps: items per source enforced + TRUNCATED
caps: per-item content truncated with a flag
caps: total char cap bounds the whole block at a line boundary
changing it changes what the server renders, at use time
chat → 4-arg processIntake with interactionMode:chat, demo:true, no u1DraftShadow
citesEvidence:true is unaffected by rule 7 — naming a row it cites is the point
citesEvidence:true with nothing provable still falls back exactly as before
claim FLUSH failure → dispatch_claim_failed (fail-closed)
claim FLUSH failure → dispatch_claim_failed (fail-closed); NO spawn
claimDispatch back-to-back (no event-loop yield) → one dispatched, one already_dispatched (no interleave); count stays 1
clarification / multiple_tasks_narrow_to_one → ask to narrow; NO proposal claim
clarification / no_actionable_task → NO proposal claim
classification + normalization unchanged (chat/ask/recommend/commit/context)
classify: DistillParseError → invalid_llm_output (instanceof, not name)
classify: IntakeUpstreamError → llm_unavailable
classify: unknown error → internal_error; a same-name impostor is NOT misread
claudeWorker re-exports the SAME brake function (back-compat)
cleanup is a NO-OP — returns undefined and does not remove the dir (B2-1 preserved sandboxes)
client body never carries detail / raw / message / stack
closing the live order lets the next one in
collapsing never hides the ANSWER or the measurements — only the two long sections
comparison is over fixed-length digests, so a length mismatch cannot throw
completed-terminal timeline (COMPLETED) → already_completed
completed-terminal timeline → already_completed
composition order is Persona → Guard → frozen Distill; classifier preserved at the END
computeWritePath: base for first; unique supersede path under <gen>/supersedes
config is fail-closed: no model or no key -> adapter unavailable, never a default id
confirm remains TOKEN-GATED: no token -> 401 and no hand-off
confirm → restart → status/confirmedBy/confirmedAt/runId persist
confirmProposal is the only path that calls runStore.startRun, passing the conversationId through
confirmProposal rejects a proposal that is not pending
confirmedBy is always set by the server and can never be supplied by the caller/model
connector REFUSES a write-shaped method at registration
console.log('[X]', JSON.stringify({ reply: r.text }))
containment: a prefixed symlink/junction escaping tmpDir is SKIPPED — target NEVER deleted
containmentCheck === assertSandboxUnderTmpdir (canonical) and rejects a non-tmp path
content that says "香香 do X" is returned as data; no write path to act on it
content-hash mismatch (tampered revision) -> rejected (fail-closed)
context carve-out anchors preserved
context intent → context (distinct from generic speech), in chat mode
contract governs HOW she speaks: no title reference, no schema change, no citation restatement
contract is a static constant within the agreed size budget
conversions still work and density is still refused
correlationId: error-carried id wins; ctx is fallback
corrupt evidence (safe-load null/throw) → needs_review; NO claim
corrupt file → RunStoreCorruptError at construct; file NOT overwritten, nothing fabricated
corrupt/absent result (safe-load passed null) → INTERRUPTED, never guessed success
corrupt/inconsistent evidence (safe-load null/throw) → needs_review; NOT guessed, NO claim, NO spawn
create → restart → the proposal is present with identical fields
createAgentRunner passes a resolved absolute path down to the worker
createResult fills safe defaults
createResult preserves provided values
createRun rejects a targetProject of production
createRun requires an owner
createRun → restart → run present, timeline intact, derived status identical
createdTime is requested — "how old is the oldest" cannot be answered from modifiedTime
createdTime reaches the caller, not just the request
creating a Proposal never calls the runStore
creation blocks browser sign-in and sync
cwd passed to the runner is always the brake-validated sandbox (never outside tmpdir)
dark mode changes COLOUR only — type is shared between the themes
dark mode keeps the text levels in order and inverted
data_domains including banking returns deny
default drops echoes
defaults written at creation disable card, address and password saving
deletes ONLY aged aroma-sandbox-* dirs; keeps fresh; never touches .aroma / data / other names
deletion is by file age, so a lost index cannot retain evidence forever
demoOutcome mapping unchanged
deny beats a would-be allow (banking + Apply to dev is denied first)
deriveStatus folds the timeline for each shape
description mentioning SIN returns deny
deterministic and free — the same words always give the same answer
deterministic output — same seed + same generatedAt -> identical hash and gen
deterministic — same input yields identical output across calls
diagnostic entry is metadata-only — no raw text, no err.message
different ids still coexist
direct file evidence: createRun writes { order, runs } with the timeline
disabled is named
disabled is named disabled
document order is PRESERVED — it is what disambiguates, and it is load-bearing now
double claimWorker → one dispatched, one already_dispatched; WORKER_CLAIMED IMMUTABLE (count stays 1)
double re-dispatch of the SAME run → ONE spawn; 2nd already_dispatched; claim IMMUTABLE (count stays 1)
drive: OR-ed fullText AND name search, trashed excluded, recent fallback
duplicate_keys: decoded-key equality (unicode-escaped key equals literal)
duplicate_keys: nested object (depth 2)
duplicate_keys: object inside array (depth 3)
duplicate_keys: surrogate-pair escape decodes to the same char as a literal emoji key
duplicate_keys: top-level
durable claim protects across RESTART: re-claim after restart → already_dispatched
durable claim protects across RESTART: re-dispatch after restart → already_dispatched, no spawn
durable terminal result (resultEvidence ok) → already_completed; NO claim
durable terminal result (resultEvidence ok) → already_completed; NO claim, NO spawn
each action requires its own declared parameters
each entry is SMALL — four must fit a source budget of 6000 chars shared five ways
each errand keeps its own outcome
each ingredient appears once, with what the site returned
each line carries ref, role and name — the audit vocabulary
each reply is labelled with the provider that ACTUALLY answered
each round-trips through the store unchanged
each source has its own switch, and they compose
each source returns well-formed, sourced, dated context (trust:live)
effectiveFlags reports a state for every switch the page can show
elapsed time is shown against the cap the Owner approved
elapsedMs is finish - start; provenance falls back to the execution snapshot
email_draft → 4-arg with u1DraftShadow:true, NO demo, NO promoteToProposal
empty and non-string inputs refuse
empty_response: empty / JSON-whitespace-only
enforceReadState leaves a correct answer completely untouched
entries arrive already rendered as citations, with status and date
entry() is the one lookup, and an unknown id returns null rather than throwing
envelope edge cases: whitespace / same-line / empty-fence / array-primitive-inside / external junk
envelope fixtures: which shapes the existing strict parser accepts / rejects
errands and waiting keep a time only because the store was ACTUALLY read
evaluate throws for an unknown capability
every Add to Cart is reachable by the product it belongs to
every B-10 promise still stands after the rewrite
every B-11 refusal still states that NO work order was created
every captured shape maps to a usable id — snake_case included
every class used is a real one
every displayed value is a projection of the sealed canonical object
every entry declares HOW a change takes effect
every entry names the file it came from, so a claim can be checked
every fence marked moved:true is inside the protected path AND forbidden
every file inside the path is un-allowlistable
every ingredient checked, nothing new → NOTHING_NEW with a count
every key is key-shaped, so data can never collide with one
every mapped status renders as its Owner-facing word, and needs_review never leaks
every message is bound to one order and one step, so nothing can be replayed
every not-found refusal is treated the same way, not just the one we hit
every path passes a timeout to the library
every raw-content and record rule is reachable and none overlaps
every reason is a named constant, never a raw library string
every ref in one read is unique — an ambiguous ref would click the wrong element
every refusal is a returned value, never a thrown error
every round is recorded with its goal, result, cost and session
every section renders its timestamp
every short confirmation behaves the same way
every source alias still anchors a claim
every turn keeps its cost, so a surprising report can be priced
everything withheld leaves the note standing alone, never an empty reply
exceeding the bound is STATED, not silently accepted
execution_proposal + promote failed (no id) → NO id, NO created claim
execution_proposal + real id → references THAT id, marks pending/not-executed
explicit injected token → privileged route ACCEPTED (201)
extension-less requires are resolved — the case that broke the first walker
extractKeywords: CJK clauses are SEGMENTED into terms, not clause-blobs
extractKeywords: instruction phrases and source/tool names are dropped
extractKeywords: latin terms still work; empty input yields nothing
facts may never carry owner (provenance guard)
fail-safe: missing / malformed input degrades to speech, never execution_proposal
fence policy: bare / json / JSON / no-lang accepted; other lang + malformed rejected
fence_malformed from a TRUNCATED reply logs exactly the 8 forensic fields
fieldOf reads the adapter\
fields outside the whitelist are dropped, and reported
filename is timestamped (sanitized createdAt) and id-suffixed
finders scan robustly: match by field, count malformed, never throw
first dispatch → claim written once + spawn once
first worker claim → WORKER_CLAIMED written once; DISPATCH_CLAIMED untouched (distinct track)
fix (c): NO sampling parameters are sent — reasoning models reject them with HTTP 400
flag OFF -> agentRunnerConfigured false, NO runner constructed, lane unauthorized
flag OFF -> claude for EVERY lane
flag OFF for every invalid spelling -> still nothing constructed
flag OFF: B2-9 auth gate first → 0 claim, 0 worker
flag OFF: GPT adapter is NEVER called; Claude handles the chat lane
flag OFF: a READY bridge proposal confirms → {runId} only, NO worker artifact
flag OFF: confirm reports not_authorized and NO worker artifact is written
flag ON + injected runner -> constructed AND agentBridgeAuthorized
flag ON -> openai ONLY for the exact chat lane
flag ON but OPENAI unconfigured -> no GPT call attempted, Claude serves the turn
flag ON but runner assembly impossible -> fail-closed, NOT authorized
flag ON: a READY bridge proposal confirms → worker fires; chain resolves; proposal is the bridge one
flag ON: chat -> GPT primary (Claude untouched); proposal/legacy -> Claude only
flag ON: worker fires ONCE, WORKER_CLAIMED is immutable, a re-claim is refused
flag ON: worker fires after response; chain Result->Execution->proposalId->approval resolves
flag is fail-closed: unset/empty/invalid -> off
flags ON + chat → exactly ONE bounded context block in the adapter input
flags resolve fail-closed (invalid/wrong-case → off)
formatLocal renders in the configured zone
forward slashes and mixed separators are handled, not a way around the check
freshness comes from the NEWEST matching row, not the first in the file
full chain (injected): enriched result returns; sandbox/prompt NEVER projected
full-width digits are ordinary typing and must work
gap and alert coexist — a new finding does not hide an unchecked ingredient
garbage from the shell is UNREADABLE, not a silently-parsed truth
genuinely nothing waiting in Drive is its own state
getCapability returns the typed contract
getEvents records an Event for every dispatch, including denied and pending_approval
getFile asks for shared-drive support
getRun exposes the full timeline and the caller cannot mutate it
github with everything merged: state=all fixture returns merged PRs
github: state=all (not open-only) with a recent-commits fallback
gmail: terms are OR-ed (not the whole question AND-ed), capped, with newer_than fallback
goal and conversationId may be null, and defaults are applied
google scopes are ALL read-only; bootstrap refuses a write scope
governance wording v2 present (Proposal-first, stage-honest)
grounding: clarification (>1 task) reply claims NO proposal + no id
grounding: execution with a REAL proposal id → reply references THAT id
grounding: promote failed → reply claims NO proposal + no id
grounding: speech/context pass the model reply through (no injected action claim)
groups dropped entirely are counted in the global notice
handleIntakeError: status + stable safe body per code
he names the setting and the language, in his own words
headings scale with the body and can never end up smaller than it
headless: false passed explicitly is fine — agreeing is not violating
health returns up when the runner is callable
her section is bounded by the next heading, not by the end of the reply
hiding uses visibility, so the row keeps its height and nothing jumps
high-risk capability with no other matching rule returns require_approval
history is newest first
holding an Owner session grants nothing on the approval path
humanDuration renders caps the way a person reads them
idempotent — a second promote returns the same proposal, no duplicate created
identical siblings sharing an ancestor stay as separate entries
ignored nodes never survive
image + link — the class that is NOT StaticText
in-memory mode (persistence:false) writes no file — pre-B2-10 behaviour
in-memory mode (persistence:false) writes no file — pre-B2-6 behaviour
inactive/mismatched revision -> rejected, no file
index.js calls the check at startup
input[type=password] is refused before anything is typed
integrity — task.proposalId set but Proposal missing → 409 + evidence, no recreate
invalid event (tampered eventHash) -> rejected (fail-closed)
invalid_json: BOM is not stripped and is rejected
invalid_json: leading/trailing/multiple/truncated fold into invalid_json
invalid_json: syntax errors
invoke Develop with input.targetProject backend produces --backend as its own element before the task, even on a frontend-constructed adapter
invoke Develop with input.targetProject frontend produces no --backend element, even on a backend-constructed adapter
invoke rejects for an unknown capability id
invoke rejects for version 2
invoke rejects when target is 'production'
invoke('Apply', 1, ...) returns ok true and the parsed backupRef
invoke('Develop', 1, ...) returns ok true and the parsed patchPath
is allowed
isFileAllowed refuses it even if it somehow got into an approved allowlist
isRoutable is true for active, false for retired, and warns for deprecated
is_error:true or a non-zero exit yields ok:false with an error
it appears as a section, not attached to a greeting
it carries whether the change applies live
it clicks via the tagged locator, never via a raw node handle
it does not detect an unmeasured assumption with no marker in the record
it does not fire on a plain request
it does not inspect bodies — it is a method fence, not a content filter
it does not resolve as though the condition were met
it finds the real rules file and splits it into one entry per rule
it is OFF by default
it is a fingerprint, not a timestamp — the same inputs give the same stamp
it is bounded in characters too — a 300-node tree is still a prompt
it is built by reading docs/, not from a list in this file
it is cheap — no Drive read, no store read, just the stamp
it is exactly what he named on 2026-08-07
it is guarded exactly like the page it describes
it is not a transcript — the rounds are referenced, never inlined
it is the INTERACTIVE node that survives, never the echo
it leads, it does not trail
it leaks nothing — one boolean-or-null, and no path
it looks for the WINDOWS name too — measured: Chrome uses `lockfile`, not `SingletonLock`
it names WHICH read the ref came from and how many acts have happened since
it names the characters it found, capped
it needs no repo configured — the record is read from this build, not fetched
it never stores an authorization header or any token-shaped value
it reads the field the client actually sends
it refuses like its siblings — POST only, loopback, session
it reports how many runs are behind the conclusion, without listing them
it returns every entry, with stored values winning over defaults
it says so about itself
it says what it cannot see
it says what it does not cover
it sits under /api/v1/demo so it inherits the owner gate
it stays under one screen — R1.3
it stores and returns all five
it verifies the element is the one that was READ, by name
keyword miss → recent-items FALLBACK, labelled "(recent items)"
killing a writer mid-append leaves a complete, valid conversation
killing a writer mid-write leaves a complete, valid file
later rounds RESUME and do not re-open
legacy / unset interactionMode still requests 1024 (unchanged behaviour)
length cap is observable: over-length field truncated AND warned (not silent)
length rules are bounded by the server, not by the model behaving
list returns every record oldest-first
list() still skips a corrupt file rather than hiding every other conversation
list/get identical across restart — order preserved (most-recent-first)
list: skips the malformed entry and returns the valid ones (never crashes, never misreads)
listActive returns only active capabilities and all seeds are active
listApprovalEvents returns ALL of them, oldest first — not a tail
listFiles asks for shared-drive items — this is the RECENT fallback, 24 of 25 files were invisible
listRules returns the ordered rule table, deny first
listRuns returns the most recent runs, most-recent-first
load: a run record without a timeline array is corruption (throws)
load: malformed JSON throws ProposalStoreCorruptError and does NOT overwrite the file
load: malformed JSON throws RunStoreCorruptError and does NOT overwrite the file
load: missing file returns an empty shape, NEVER throws
load: missing file returns empty shape, never throws, does not create the file
load: non-array order / non-object proposals throw
load: order id with no matching record throws
load: order id with no matching record throws (structural integrity)
load: valid JSON but wrong top-level shape throws ProposalStoreCorruptError
load: wrong envelope shapes throw RunStoreCorruptError
logIntakeOutcome never throws, even on a hostile sink or input
logout clears with the SAME attributes, or it would not actually clear
losing the audit store is visible, not silent — AND the dry-run fails
malformed file → construction throws a controlled error; file NOT overwritten, nothing fabricated
malformed result artifact → controlled 500, no crash, no path leak
master on but per-source off → still inert
matches on ORIGIN, not on a prefix — a lookalike host is refused
mentionedFilesFrom extracts only path-shaped tokens (never invents)
missing GITHUB_READ_TOKEN → github skipped, Google sources still registered
missing dir → read null, list [] (controlled, as today)
missing evidence (store absent) -> rejected with explicit reason
missing facts stay honestly unknown rather than becoming a false accusation
missing file → safe empty init, no throw, file not created on read
missing file → safe empty init; first proposal creates the file
missing result file (deleted) is handled safely — running, not a crash
mixed script is the normal case, not an edge case
mutating ANY displayed canonical value changes both the hash and the card
names what was counted, how old, AND what was not looked at
never claims a number of invoices
newest first, so the one he just ran is the one he opens
no CORS header is ever emitted, and the session cookie is HttpOnly + SameSite=Strict
no EvidenceSet, no Answer Plan, no responseFormat on a utility turn
no Memory mutation — storeIdentityHash identical before/after a build
no assertion greps a page or source blob for catalogue wording
no click call ever carries force
no database is NOT the same claim as clean
no desktop application is approved yet, so targetApp must be null
no entry carries anything an executor could read as authorisation
no evidence at all is refused — not passed
no false positive: escaped backslash — "\\u0061" decodes to literal \u0061, NOT "a"
no false positives: fake keys in strings, case-different keys, separate objects
no fill call ever carries force
no intent match → the reply passes through completely untouched
no lookup by name exists — a rename must not silently change what is read
no mode uses the legacy 3-arg processIntake (every call has 4 args)
no per-step execution trace anywhere in the payload
no persona text leakage — serialized closure contains no persona payload text
no phase label leaks a path, a prompt, a credential or agent output
no profile is its own state
no question is invented when the model offered none
no read happened at all this turn ⇒ nothing to enforce
no ref is a bare number — so an item number can never BE a ref
no row carries an approval or authorisation field
no search box is BLOCKED_BY_SITE, never a quiet 「冇回收」
no session cookie at all ⇒ refused
no such task → NOT_INSTALLED, and that is a fact not a failure
no t() call sits in a MODEL or MATCHING position
no task installed → DUE still reads as manual, because it IS manual
no translating file declares a local named t
no value named → no offer, with its reason
no write handle — build succeeds with all fs write ops disabled (hermetic no-write proof)
nodeReadFailures is a COUNT and travels on both the result and the audit
non-bridge pending proposal (no sourceTaskId) → confirm works exactly as today
non-chat lanes still read nothing at all
non-context chat/recommend intents → speech
non-demo path is untouched by the flag (no persona, no contract)
non-http schemes are refused outright
nonce is single-use: replay, double-click and reload all fail after the first use
nonces are unguessable and distinct per step
normalization unchanged: chat/commit shapes still produced
not_single_object: valid JSON that is not an object
nothing can be appended after any terminal stage
nothing ever run at all → NEVER_RUN, and no calm sentence exists
nothing in the reply path calls the resolver
nothing is cut silently — every dropped member is in some stated count
nothing is unclassified — a new one fails the suite until someone decides
nothing stripped means the count is zero, not absent
nothing waiting says so, and says when it looked
nothing wired → ran 0 and ok false, so the task records a failure
nothing worth saying → the section is omitted, never padded
null / non-object card → empty preamble, no warnings
null or empty clears the field back to the default
numbers stay numbers and junk becomes null — no smuggling through a field
offerFor is unchanged — the thin wrapper the callers already use
old boundary-violating wording is gone and must not return
old record missing status → normalized to controlled "unknown", never "pending" (not confirmable)
one call clicks at most once — no retry, no fallback
one entry renders inline too
one final correlationId is shared by client body, Error, and diagnostic entry
one source failing → UNAVAILABLE line; other sources still injected
one unfittable line does not end the block — later sources still land
only Buffers are accepted — a string is not evidence
only STOPPED_FOR_YOU is waiting
only an explicit false disables it, and that is the A/B arm
only an explicit group:true turns it on
only files this store wrote are listed — a stray file is not a conversation
only the two exact strings survive; everything else is discarded
opening ONE enquiry by id returns the turns
opening a session routes every request through the fence
order is preserved when filtering
order planning maps snake_case to id and title — the captured defect
ordinary Cantonese prose is untouched — the check is about names, not language
ordinary read instructions and questions are all exempt
other options pass through
out of range → no offer, and the fence speaks
output is byte-identical to the sequential version: order, caps, three-state rendering
output nesting is at most ONE level, whatever the DOM depth
outside the window it may run
owner cannot be supplied by the caller — the store always sets it
parse failure propagates as DistillParseError (not upstream)
parsePlan names why it failed, for every way a model can miss
parses claude success JSON into a clean Result (ok, result text, cost, relay 0/0/0)
partly answered → ok TRUE. The rows exist; the briefing tells the story.
past the cadence → DUE, saying both the age AND what it should be
path escapes / absolute paths are rejected
pending proposal → restart → still pending (no confirm fields fabricated)
per-item size cap truncates content + flags it
permissionMode + addDirs match B2-1
permitted when it should not be is a VIOLATION
persona carries the trusted data-boundary guard (context_card is data, not instructions)
pickRedirectUri: loopback from client, desktop default, OOB and --manual
picking a provider never changes the lane or reaches execution
plane-15 and plane-16 private-use glyphs are removed too
precedence: commit → execution_proposal (execute signal never masked)
prepare mints a sandbox under tmpdir and runs the injected init
previousLane is a LANE NAME, and junk in it is ignored
produces a SEALED work order with system-owned fields and a system hash
production never emits stackSample even with INTAKE_DEBUG_STACK=1
projection is an ALLOWLIST: unknown keys dropped, long strings dropped (never truncated in)
promote does not confirm and does not schedule — proposal stays pending, no Run
promote endpoint: token required, 404 unknown task, promotes+binds, idempotent
promotion failure → no fabricated Proposal; error surfaced from official result
promotion success → exactly the official pending Proposal; no run/dispatch
prompt-injection: delimiter breakout neutralized; instruction text kept as DATA; warned
prompt: p,
proposal confirmed but no execution → 200 pending; execution but no result → 200 running
proposal lane + flags ON → NO context injected, ZERO reads (chat-lane only)
proposal → 4-arg with interactionMode:proposal, demo:true, promoteToProposal fn
provenance / extra fields present at create survive restart intact
rankByHealth uses seed values when sample_count is zero and live quality once events exist
rawMeta: present sample → deterministic 12-hex hash; redactionHit reflects secret shapes
rawMeta: undefined/empty rawSample is safe (rawPresent=false, length 0, hash null, no throw)
read -> act -> read -> act is allowed
read endpoints unaffected: /health and /proposals need no token even with HUB_TOKEN unset
read returns null for a missing id or an empty store
read: valid → record; missing → null; malformed → ArtifactCorruptError (file untouched)
readability anchors: each frozen section is present, incl. governance-supremacy language
readonly is named
real COMMANDS still COMMIT with a Decision/Task
reason is a closed enum, never free text
reasoning effort is overridable per call and via OPENAI_REASONING_EFFORT; typos ignored
reasoning effort is pinned to the cheapest setting by default (budget guard)
reasoning tokens are surfaced from the documented usage path (provable budget burn)
recalls found → ANSWERED, carrying the title and the date
reconcile is durable + persists the mark (survives restart)
reconcile skips already-terminal runs (no mark on a COMPLETED run)
recording the same id twice REPLACES, it does not append
recovery narrow window: WORKER_CLAIMED-only, no Execution + no Result → INTERRUPTED (mirrors DISPATCH_CLAIMED)
recovery: interrupted worker (WORKER_CLAIMED + Execution artifact, no result) → INTERRUPTED (branch c)
refs are not ordered, so the sequence carries no information to extrapolate from
refuses any target not under os.tmpdir() and NEVER calls the runner
refuses:
registerAgent accepts the seeded claude-code manifest
registerAgent throws for an unknown capability
rejectRun accepts no reason and still records the rejection
rejectRun appends REJECTED, dispatches nothing, and the status is terminal
rejects an unsupported capability or version, and an empty task
rejects — unknown task 404, blank title 422, non-promotable state 409
request touching the 'banking' data domain is denied and no adapter is called
requestId is server-generated; browser-supplied requestId ignored
requires a non-empty baseDir
requires a worker with invoke() and an artifactStore with write()
resolveAgentBridge: strict on only, fail-closed
resolveAgentCliCommand finds the real install on THIS machine
resolveAgentCliCommand: explicit override wins, must be absolute and must exist
resolveConversationContract: strict on only; unset/empty/invalid → off
resolveDevelopDispatch: strict on only — unset/empty/wrong-case/misspelled → off
resolveExecutionAuthorization: fail-closed matrix, no implicit priority
resolveRef REFUSES a ref that does not belong to this page
resolveRef maps a ref back to the DOM node — statelessly, by recomputation
resolveReleaseRef: all three flags present + valid -> structured value
resolveReleaseRef: no flags -> null
resolveReleaseRef: partial flags -> RELEASE_REF_PARTIAL
resolveSupersede: partial -> SUPERSEDE_PARTIAL; none -> null; both -> value
resolving one removes it from waiting but NOT from history
response passthrough preserved:
result-count cap truncates + reports truncatedCount
retry endpoint: token required, interrupted → 201 inert attempt, missing reason → 422, duplicate → 409
retry guards: non-interrupted → 409; missing reason → 422; duplicate retry → 409 (no second attempt)
retry: INTERRUPTED → new attempt (linkage), original preserved, NO auto-dispatch, still gated
round 1 opens a session id; later rounds RESUME it rather than starting fresh
round 1 opens a session with --session-id
round-robin: no source gets a second line while another has none
routing reads the RAW MESSAGE — the extractor never emits the term itself
rows are matched to their kind by id, not by title
rows belonging to another kind are not included
rows belonging to no declared kind keep their timestamp and gain NO freshness claim
runContext: AGENT_FINISHED carries cost null rather than zero when the adapter reports an unprovable cost
runContext: Deploy to production without approval appends POLICY_EVALUATED then PENDING_APPROVAL, never selects or runs an agent
runContext: Develop where the top-ranked agent throws and the next succeeds appends two AGENT_RUNNING and two AGENT_FINISHED, the first with success false
runContext: a 'banking' data domain appends POLICY_EVALUATED then DENIED, never selects an agent
runContext: a Develop where every agent fails appends FAILED carrying error, agentId, cost, latencyMs and an attempts array
runContext: a successful Develop to dev appends POLICY_EVALUATED, AGENT_SELECTED, AGENT_RUNNING, AGENT_FINISHED, PATCH_READY in order with the patchPath
sanity: the machine really is America/Winnipeg, so the tests above prove a difference
save is atomic: a pre-existing good file is replaced in place
save is atomic: a pre-existing good file survives (rename replaces in place)
save then load round-trips the envelope byte-identically
save uses temp+rename: no .tmp remains and the file is valid JSON
screenshot between two acts does not rescue a plan
screenshot does NOT spend it — it changes nothing
screenshots are retained for 7 days on the Companion account only
search
searchFiles asks for shared-drive items
send is disabled until there is real input, and while a reply is in flight
service: demo opts (contextCard, promoteToProposal) survive alongside requestId
service: invalid supplied requestId is NEVER used — a fresh UUID is generated
service: malformed output → DistillParseError, carries the supplied requestId
service: non-HTTP caller with no requestId still gets a generated correlationId
service: provider failure → IntakeUpstreamError (typed, carries id + cause)
set() then get() in the same process returns the new value
shell:false is preserved everywhere — no shell:true anywhere in the agent
signature matching honours the declared tolerance in both directions
silence is reserved for "the feature is off" and nothing else
sink failure is swallowed — handleIntakeError still returns a safe response
stackSample: off by default; on only when flag set AND non-production
startRun rejects a targetProject of production and never dispatches
startRun returns a run id immediately, before the dispatch has finished
startup fail-fast: index.js refuses to start (exit 1) when HUB_TOKEN is unset
startup reconcile: crash-boundary matrix marks correctly + dispatcher spy 0 + idempotent
state-distinguishability after restart: claimed vs never-claimed (evidence only, no action)
static safety: no markup-from-strings, no code-from-strings, no persistence, no token
status: succeeded / failed / running / pending
steps cannot be consumed against the wrong order, or with no order live
stopReason is propagated when the provider supplies it, and null-safe when not
stopReason normalization is faithful and null-safe
stopping on budget can never render as finishing
substring matching is still refused
success + full chain resolves + real confirmedBy/At + NO leak
sweeping an empty or absent directory is safe
the "not configured" page says what to set, but never a value
the 1999-11-30 never-ran epoch is not reported as a real last-run time
the ASCII forms still work
the Apply arguments array contains no --patch element and no --target element
the Bearer confirm route has NO confirm logic of its own — it only calls the service
the CFIA recall register is NOT blocked — reading is not filing
the CSS uses tokens, not raw px, in the new rules
the Claude FALLBACK after a GPT failure still gets the full context
the Claude fallback after a GPT failure still gets the full context
the Claude path is not gated here at all
the ClaudeAdapter default limit is untouched (still 1024 when no maxTokens is given)
the Companion refuses rather than partially complying
the DOM id never appears in the text the model reads
the Decision Recall block is gated the same way
the Decision Recall block is withholdable from GPT on its own too
the Franco line is its own row, not glued to the greeting
the ITEM omission note is rewritten too — same sentence family
the LAST source survives even when the earlier ones would fill the budget
the LIST carries reports and NOT turns
the LIST is reports only — opening the turns is a second, deliberate step
the MODEL cannot set system-owned fields (they are overwritten, not trusted)
the MockAdapter still recognises Cantonese greetings
the OAuth credentials file the agent will use is reachable via the allowlisted vars
the ONE difference is additive: a fifth field, always present, false by default
the Owner's four examples land exactly where he specified
the Owner-facing message names his PAYMENT, not a table
the Owner-forbidden file operations are all in the required list
the REAL process env produces a child env with no credential from this machine
the ROW SHAPE travels with the evidence, so an answer can state it
the SAME claim is allowed once something was actually applied
the SAME path on another origin is not
the SPAWNED child really receives the built env, not the parent env
the StaticText label of a button does not survive beside the button
the Supervisor reaches a disk ONLY through the artifact store
the Supervisor\
the UIA rules do not fire on the other two actions
the URL is recorded without its query string — a query can carry a secret
the `next` parameter cannot be turned into an off-site redirect
the absent sentence still reads as the calm, normal, manual state
the accent survives everywhere it was NOT asked to change
the adapter builds an arguments ARRAY, never a concatenated shell string
the added block introduces no other literal colour or size
the aligned technical dump keeps its ASCII separators — alignment is not prose
the allowlist itself contains no secret-shaped name
the app icon is a square canvas holding one centred dot
the appended intents cannot steal a match from the Aroma six
the approval payload is still EXACTLY four fields
the approved TEST command also runs with the stripped env
the attachment carries WHEN it was captured
the attachment names the section and carries its conclusion lines
the audit record now carries duration
the author\
the bar is suppressed on 首頁, because the briefing is right there
the bare row id keeps working — the ref is an addition, not a replacement
the block stays small — the whole point of an index
the body actually uses them — a token nothing reads is decoration
the body exists at the pinned path, in the repo
the boundaries are where they are declared, not one hour out
the boundary policy applies however GPT was selected — hint or flag
the bounded read truncates by lines AND by chars, and says so
the briefing renders on 首頁 instead
the briefing tells the client which sections have a door
the bubble uses its own token, not the generic card surface
the business date beats the row insert timestamp
the button reports the lock refusal instead of retrying
the by-name entries were NOT removed during the move
the calm summary counts only what was ACTUALLY checked
the caps and the untrusted-data framing are unchanged for GPT
the card is deterministic and model-free — same order in, byte-identical card out
the card is still a viewer: server-built sections, exact typed confirmation
the carve-out is not a blanket pass — an UNMENTIONED row is still barred
the catalogue is NOT — he must be able to reword without a work order
the character budget cuts inside a group the same way the count budget does
the chat lane cannot promote even when the envelope is a full commit
the chat response reports the lane; other lanes stay byte-identical
the checker itself is un-allowlistable
the child env is BUILT from the allowlist — every secret-shaped var is absent
the classification does not keep entries for files that carry nothing and translate nothing
the claude-code adapter passes validateAdapter
the clean sentences survive; only the claim is removed
the client asks the briefing endpoint, not the greeting alone
the client asks the settings endpoint and re-points its resolver
the client fetches the version and warns when it differs
the collapsed sections keep every promise they had
the common phrasings are all caught — where the claim NAMES the source
the composer and the message column share ONE centre axis
the composer bar holds the shortcuts and the picker — and no lane switcher at all
the composer comes back when a conversation is shown
the composer is PRESENT on the section view, unlike 首頁
the composer is sticky with room beneath, and the send button lives inside it
the computer-audit artifact kind is registered BEFORE anything can act
the conditions that must stop a run are all defined
the configured check requires a real password, not a stub
the control is in the SERVED page, not only in the asset file
the cookie is httpOnly, SameSite=Strict and long-lived
the corpus covers each refusal reason at least once
the corpus-wide echo count drops to zero
the correction is appended, and states what was actually read
the cost cap is checked BEFORE a round, not after it is already spent
the count is REPORTED — a transformation that changes data must state it
the count is on the GROUP line, not only in a global footer
the count reaches the log line
the daily hour reports that the task must be re-registered
the date on the row still verifies
the decision is computed from the result the turn actually produced
the declared folder ids are the ones measured live
the default keeps the coverage grouping was costing
the default output has no group lines
the default runner is built with child_process.spawn, shell false, and an arguments ARRAY — no real process is spawned
the demo boundary validates previousLane and only continues short replies
the demo boundary validates the hint BEFORE it reaches the engine
the destination has styles and does not reuse the thread layout
the detail renders ingredients and history, not a step log
the develop dispatch always carries the Run targetProject
the disabled sentence says it was set up AND switched off
the display states the consequence in plain language (diff only, worst case, no amend)
the distill schema descriptions are rewritten too
the drop record cannot be turned into a payload by a long or repetitive plan
the earliest day has nothing to compare against and says so
the email lane fetches NO context and makes no second call
the empty state is a class on the pane, so CSS owns the layout
the entity anchor needs the turn to be about that entity
the entry exists and offers exactly the locales the resolver knows
the env override wins over the stored value — a test must not read his settings
the envelope comes BEFORE the message — it is background, not the turn
the envelope is opened and closed exactly once, whatever the content says
the envelope validator is fail-closed on anything malformed
the errand list distinguishes 「none ran」 from 「cannot read」
the errands section carries a freshness entry per DECLARED KIND
the error names the bad value so it can be fixed without a debugger
the evidence records aliveness per binding, so a dead target is always visible
the existing event members are untouched
the expanded form contains everything the collapsed form hid
the expected hash is recomputable by hand, and the command is in the file
the fail-closed rule is in the source, not just in behaviour
the failure entry NEVER contains the prompt, the user message, or the full output
the fallback direction is CHAT — the lane that can talk but not act
the favicon is the padded square, shared with the installed app icon
the file must come from THIS message, not from anywhere in the conversation
the file really is written, under the Companion-local base directory
the file-scope actions stay in 詳細 — saying them twice is what made the card unreadable
the first fix connected the wire and the section still said nothing
the first knock ever is always allowed
the font stack is local — no webfont, no CDN, nothing fetched
the four canonical demo turns map to the four distinct outcomes
the four failure reasons are reported separately, never as one boolean
the four outcomes are not collapsible into ok/not-ok
the freshness entry is carried whole
the gate baseline and the same-round baseline are different obligations
the generated source really is the server function, not a copy of it
the generic kind no longer exists at all
the goal is an argument, never concatenated into a shell string
the good UIA context adjudicates ACCEPTED — the control for the three cases below
the good context adjudicates ACCEPTED — the control for every case below
the grace period stops a run a few minutes late from reading as overdue
the greeting still renders when the backlog read HANGS past its budget
the greeting still renders when the backlog read THROWS
the guard passes while running and refuses once stopped
the harness requires from src/, because it runs as the Owner and is never staged
the hash is stable against key order and array order, but not against content
the header returns with the first message — it is the same class and nothing else
the hint cannot change the lane, the mode, or anything executable
the in-memory array is kept — it is what /approvalAuditLog serves
the ingredients behind the conclusion are named, so a drill-down has a key
the inline script is valid JavaScript (a broken page used to fail silently)
the input is a designed container, not a raw textarea
the interception path is guarded too — a denial there is corrected as well
the kill-switch register does not claim more than has been shown
the labels are Traditional Chinese, and the control is reachable without sight
the launcher body IS in the monthly backup sources
the launcher is outside the repo and no rule here reaches it
the limits are inside the hash, so they cannot be edited after approval
the line carries exactly the allowlisted fields — nothing else survives
the line is attached when there is something waiting
the link works at ANY age — 過期嘅係主張，唔係 access
the list is trimmed and blanks dropped, and the result is what gets stored
the list stays short enough to stay inside the pacing budget
the live shim matches its pin
the locus is ONE line — the stack lives in the turns, not here
the locus names the two sides and what was wrong between them
the log carries counts, enums and drop IDENTITY — never a value
the log is capped and keeps the newest
the log line carries the same projection, explicitly
the log line carries the score and never the withheld value
the logger never throws, whatever it is handed
the login form keeps both inputs and the submit button
the login page is self-contained — no external anything
the manifest route serves the right content type and parses
the maps are DERIVED — a new source cannot be silently unrepresented
the message column and the composer share ONE width token
the method caveat does NOT appear inside the answer caveats
the model reply splits at 下一步, and its prose above is discarded
the module is pure — building a record touches nothing
the module requires no model client
the mushroom line: data survives the frame changing language
the negative threshold is low on purpose — a partly-visible owner window still trips it
the never-list and the restricted list do not overlap
the note is never added twice, and never to a clean reply
the note is short enough to read as one line
the number gate still holds for written-Chinese lines
the offer carries RELOAD_PAGE, so the screen says what is needed
the offer shows before → after, and the button posts the MESSAGE not the value
the old path still serves a turn with no plan, and says so in the log
the open endpoint is mounted and refuses an unknown errand rather than 404ing the ROUTE
the options are headed
the original text survives in full; only a note is appended
the other two-option spellings fire the same rule
the pacing floor cannot be removed from a settings screen
the page POLLS the result endpoint instead of asking once
the page is built from asset files and inlines them — one same-origin document
the page is deterministic, and the dot appears in both places from one asset
the page links the manifest and declares a theme colour for both schemes
the page still assembles no markup from strings, and stores nothing in the browser
the page still builds no markup from strings
the page still builds no markup from strings and stores nothing in the browser
the phrase is sent to the site in quotes
the picker offers exactly the two providers the server allows
the pin is a full sha256, not a prefix
the pipe is local by construction — no port, no host, no network
the placeholder is always present so titles stay aligned
the placeholder is short enough not to be clipped
the prior result is NOT re-pasted into the next goal — that is the relay moving down a level
the public stub is NOT special: a different configured token rejects Bearer svc-token-aroma-os
the pure modules import nothing but node:crypto and each other
the query reaches the page — an answer about the wrong ingredient is worse than none
the query still reaches the page unmangled inside the quotes
the real Blender capture: the donate control now reads as its word
the real Simplified sentence from 2026-08-07
the real Wikipedia case is reported, not silently resolved
the real page shrinks by an order of magnitude, and the number is stated
the real store writes where the other audits write, under the artifact root
the reason distinguishes the failure modes
the reason is a short enum — never the message, never a path
the reason record is frozen, so nothing can rewrite why it stopped
the recall check is declared, daily, and NOT scheduled
the recall header itself carries the rule, so it cannot drift out of the block
the record carries length and a shape class, not the text
the record carries role and name and NO coordinates
the record has the agreed shape and rejects malformed identifiers
the record plan does NOT fall back to a full file read
the record\
the recorded time is when the run FINISHED, supplied by the caller\
the redirect directory actually exists and is writable
the redirect is stable within a process — four modules must agree
the refusal happens BEFORE any browser call — not after a failed attempt
the refusal names the evidence, so the next person does not re-derive it
the register never claims more than the three bindings it lists
the registry file is inside the protected path
the rendered reply carries exactly one 下一步 and one question mark line
the reply reports the provider that REALLY served it, from telemetry
the report references the enquiry by id and does NOT inline the turns
the report separates writes-the-order-permitted from writes-refused
the reported duration STOPS at completion instead of growing
the request cannot name an action — runScheduledErrands takes no action parameter
the resolver is reachable ONLY through the two named entrances
the rest of the scale is unchanged and still declared
the restatement and understanding survive exactly
the result is cached, and the cache is per-TTL not forever
the result records WHEN it looked — a number with no time cannot prove it is fresh
the result view flags an OUT-OF-SCOPE run instead of reporting success
the result view never invents evidence it was not given
the result view reports what the runner returned, and nothing more
the retention boundary is exactly 7 days
the retired names that remain are comments only, and are counted
the retired-name list is intact and did not get rewritten by the rename
the roles are exactly two, and both are named
the round cap stops the enquiry and it reports STOPPED_ON_BUDGET
the router is PURE, FREE and ZERO-CONTEXT
the router returns a provider NAME and nothing else — it cannot carry a capability
the rule is written where a future reader would ask
the rules are in the protected path
the run count is a number, not a list of runs
the run count is shown as a number, not as thirty lines
the run interval cannot be set to zero either
the run still returns its outcome, and says the record failed
the runner and the sandbox worker share ONE artifact store instance
the same dry-run can be requested again only as a NEW order admission
the same hostile content is inert on the CLAUDE path too
the same node keeps its ref across two independent reads
the same slots appear in both languages
the same tree read twice gives the same refs — a ref is not a position
the sampling grid and its detection floor are declared values, not prose
the scanner reports what it does NOT cover
the schema closes every object so nothing can be smuggled past the validator
the schema still closes every object after the minItems change
the seal endpoint is POST-only too
the seal endpoint refuses a forbidden file and a multi-file request — no card is issued
the sealed record is WRITE-ONCE — it cannot be amended after the Owner has seen it
the seam being absent is reported, never silently skipped
the second-vendor disclosure survives the rewrite
the send path captures its conversation BEFORE anything renders
the serialized form carries the truncation notice, not just the flag
the serialized form nests members under their group
the served page carries the stamp it was built from
the server never self-HTTPs and HUB_TOKEN never reaches the browser
the session can say which layers are live, from itself
the settings endpoint ships oneOf, so the screen can offer the choices
the shadow line carries no message content
the shipped block lists a source that was read, including aroma_system
the site explicitly reporting ZERO is a real, clean answer
the site says 89 items but nothing parses → BLOCKED, not a clean 「冇回收」
the sources are never read twice in one turn
the sources are read CONCURRENTLY — the wait is the slowest one, not the total
the spy connector actually records calls (so a zero below means something)
the spy records calls — every zero below is measured, not vacuous
the square icon reads its colour from dot.svg, so the two cannot drift
the staged copy reaches only three node builtins, each named
the staged graph pulls in NOTHING beyond the Companion
the stamp changes when a stale-able asset changes — otherwise it detects nothing
the stamp covers app.js, app.css AND index.html
the stated narrowing appears even when the answer is 「冇搵到」
the static schema agrees with what goes out
the step ceiling ruling is in force: maxSteps <= 10
the stop report is INLINE — the five fields, not a link to a report
the store answers a request while a run is still in flight
the store refuses to guess a location
the store refuses to record anything outside the vocabulary
the store snapshots scope + caps at hand-off and measures duration once
the store writes no approval or authorisation field
the substitution is the label map, not a guess
the suite is free of the fall-through and empty-coercion shapes
the sweep is driven by the injected clock, so the deletion path is reachable in a test
the switch list is DERIVED, so a new source cannot be forgotten
the synthetic marker note "由對話蒸餾" no longer appears for empty-task commits
the tab and installed-app titles are the current name
the three capability states are distinct and named
the three outcomes reach the DOM as distinct classes, never merged
the three states that were already named still are
the timeline gains stages over time and the status becomes terminal once dispatch completes
the timeline returned by getRun cannot be mutated by the caller
the timestamp property and the sample property are each tracked on their own
the title comes from the row, not from the model
the token value never appears in any response body
the topic is not restricted — there is no relevance check anywhere
the truncation note stays, and is consistent with the counts rather than a substitute
the two classes are kept apart and do not overlap
the two known inert implementations agree with each other
the two lines are verbatim distinct, and the header explains both
the two produce DIFFERENT sentences
the two render as different things, which is the whole point
the two signatures are far apart and far from Windows chrome
the two timeout bounds are declared and the wall clock is the larger
the typed confirmation is checked on the SERVER — every near-miss is refused
the untouched defects are NOT marked as anything resembling handled
the v2 card is a viewer: collapsed section is presentation only, payload unchanged
the version route reports the stamp this process is serving
the view reads output.filesChanged / output.diffSummary / cost / latencyMs
the view uses the SNAPSHOT and never needs a Work Order
the visible face follows the Owner-specified structure, in plain Chinese
the waiting card carries all five fields
the warning names the fix, because the fix is not obvious
the whole of src/ is clean
the window is still BOUNDED — it does not return events years out
the worker spawns the ABSOLUTE path it was given — never the bare string claude
the written and the Cantonese forms of the same question both route the same way
the written-Chinese policy holds for the server line
there is NO wildcard — an allowlist with an escape hatch is a denylist in costume
there is a way back to 首頁
there is no exported way to render an entry WITHOUT its status
there is no status setter and no stored status field
three or four on is also a conflict, not a majority vote
three or more entries render as a COUNT, not as the entries
timeout → unavailable
title + note → "Title: <t>\n\nDetails: <n>"
title only (missing note) → title-only
titles and dates come from the row, and a missing date stays missing
tokens: r.tokens.slice(0, 8),
traversal / malformed id → 400 and the store is NEVER touched
two different DOM nodes never share a ref, however alike they look
two entries render INLINE — collapsing three lines is worse than reading them
two genuinely distinct INTERACTIVE elements with one name both survive
two names that differed ONLY by a glyph become a real ambiguity, and are flagged
two numbers is ambiguous and fires nothing
two options collapse to the first question; prose with none becomes null
two paths, a wildcard, or a directory are rejected
two-of-three flags + a full EXECUTE triple -> conflict, ZERO execution
two-of-three flags on -> configuration_conflict -> ZERO execution
two-of-three through the real app: agent + worker on -> conflict, runner never authorizes
undeclared documents appear as WORKING NOTE rather than being hidden
under 2 hours: shown plainly
ungrouped nodes still work and are not forced into a group
unknown proposalId → 404
unknown version is rejected
unmatched low-risk capability (Report) returns allow
unparsable CLI output rejects rather than resolving with nothing
unparseable worker output yields ok:false, not a throw
unscheduled + DUE reads as a standing fact, not a fault
unset HUB_TOKEN + no injected token → privileged route 401 (NO stub fallback)
updateHealthFromEvent creates then updates a record and increments sample_count
updatedAt moves with each turn; createdAt does not
upstream error → safe mapped response (no provider/stack leak)
v1.1: capability limits must be disclosed BEFORE asking for details
v1.1: the amendment did not disturb the existing groups or their order
v2: a non-existent file is REFUSED at seal — no card, no approvalId
v2: approving still carries only four fields, and the card adds no new authority
v2: the sealed response carries the Owner card, and it matches the sealed order
valid task → Proposal(status pending, linkState ready), task bound, provenance complete
valid three-domain closure -> VERIFIED, productionPersonaMode NOT_VERIFIED
validateAdapter passes for a well-formed adapter and returns it
validateAdapter throws when a method is not a function
validateAdapter throws when health is missing
validateAdapter throws when invoke is missing
validateProposalId accepts safe ids and rejects traversal/malformed
verifier round-trip — PASS on good closure; FAIL on any single mutated field
verifySeal is pure — it neither mutates the order nor touches anything
version constant is v1
wait_for spends it — waiting for a change means the DOM changed
weekday comes from the DATA (event local date), not the model
well-formed card is wrapped in EXACTLY one data block
what EXECUTES is the sealed record — the runner receives the server order, byte-for-byte
when Drive did not answer it says so — silence is not 「nothing waiting」
when items are dropped the header says so, and not otherwise
when nothing was read, saying so is still allowed and still true
when the probe cannot say, the reason is UNKNOWN — not a guess
when the probe itself cannot say, the reason is UNKNOWN — not a guess
when the top-ranked agent's adapter throws, the dispatcher falls back to the next and succeeds
white-list enforced: keys outside the schema are dropped (observable)
with AGENT_BRIDGE OFF a fully correct approval confirms but executes NOTHING
with AGENT_BRIDGE OFF nothing is constructed and nothing is audited
with NO interactionMode the server routes, and builds the matching locked opts
with NO rows surviving, the fallback still fires
with READ_ACCESS off the guard is inert and the reply is byte-identical
with a free profile it launches Chrome AT HER PROFILE and the recorded url
with looseFirst, a large loose set starves the groups entirely
with no backlog reader injected at all the route behaves exactly as before
with no history nothing is suppressed — a fresh conversation loses nothing
with no runContext the result shape is unchanged and nothing is appended
with no stored nextRunAt it falls back to lastAt + cadence, and says which it used
with no witness supplied it says nobody looked, rather than claiming a state
with nothing relevant the summary says so, and the section list is empty
with the by-location rule REMOVED, the same work order is accepted
withholding everything from GPT leaves it working, just uninformed
withholding is FAIL-CLOSED: an unrecognised value withholds, never shares
within the bound there is no warning — one that always fires is ignored
within the cadence → FRESH
without the section, anything outside the question asked is discarded silently
workOrder: a well-formed order validates
workOrder: absolute / traversing / forbidden files are un-allowlistable
workOrder: isFileAllowed + deterministic hash
workOrder: must declare commit/push/PR/merge/deploy forbidden
workOrder: rejects missing/empty required fields
worker.buildArgs: NO bypassPermissions; allowedTools = Read Edit Write; no Bash
worker: a surviving remote → ok:false + risk (no push target check)
worker: bypassPermissions provider → refuse, runner NEVER called
worker: cost over cap → ok:false + risk
worker: file changed outside allowlist → ok:false + risk
worker: happy path → ok, enriched output, zero risks
worker: invalid work order → refuse, runner NEVER called (fail-closed)
worker: timeout kill → ok:false + risk
workspace.prepare: isolated clone, agent branch, remotes removed
workspace.prepare: refuse a workspace not under tmpdir (containment)
workspace.prepare: refuse an unsafe approvalId
workspace.prepare: refuse when a remote survives removal
write rejects a record without id or createdAt
write then read round-trips a task and a result
write_file stays NEVER even though save is unlockable
writes Execution + linked Result artifacts; sandbox is under os.tmpdir()
zero results renders "read OK — no matching results", NEVER unavailable
zoneLabel takes the city, never the whole IANA string
⛔ ALL blocked → ok FALSE. Nothing usable was produced, and that IS the schedule failing.
⛔ AND IT DOES NOT FIRE ON HONEST ASSERTIONS
⛔ AND IT DOES NOT FIRE ON HONEST CODE
⛔ AND IT DOES NOT FIRE ON HONEST RENDERING
⛔ AND IT DOES NOT FIRE ON THE IMPORT, OR ON OTHER NAMES
⛔ AND THE CASES A SECOND IMPLEMENTATION WOULD GET WRONG
⛔ AND THE CATALOGUE — rewording is a page change, so it must move the stamp
⛔ BOTH SPELLINGS, ALWAYS — 英文 and en, 中文 and zh
⛔ EVERY key, BOTH locales, identical output
⛔ Franco has no door either — it would open onto the same four numbers
⛔ Franco is LIGHTENED, not shortened — the caveat survives
⛔ NEVER_RUN is NOT openable — no door rather than an empty room
⛔ NOT_WIRED is its own state, distinct from NOT_CHECKED, and also timeless
⛔ NOT_WIRED renders as a DEFECT, never as 「未有差事紀錄」
⛔ POST is ABORTED by default
⛔ SEEN TO FAIL — a fence at its OLD location would be caught
⛔ SEEN TO FAIL — a modified shim is DETECTED, byte for byte
⛔ SEEN TO FAIL — an unsupported locale is REFUSED, not silently ignored
⛔ SEEN TO FAIL — each shape is really caught
⛔ SEEN TO FAIL — on both real instances, verbatim
⛔ SEEN TO FAIL — on the real case, verbatim
⛔ SEEN TO FAIL — on the real instances, verbatim
⛔ SEEN TO FAIL — on the shapes that would actually appear
⛔ SEEN TO FAIL — the WHOLE scan catches a dynamic key in a real file
⛔ SEEN TO FAIL — the guard is capable of missing, and this proves it
⛔ SEEN TO FAIL — the rule really rejects each non-interface class
⛔ SEEN TO FAIL — two locales in one sentence fires NOTHING
⛔ SEEN TO FAIL: the same input WITHOUT escaping does break out
⛔ THE LINK STAYS OPEN AT EVERY AGE
⛔ THE `|| t` FALLBACK IS GONE — a read with no time of its own gets no time
⛔ UNREADABLE is not NOT_INSTALLED — 「I could not look」 ≠ 「it is not there」
⛔ WAITING is rendered FIRST, and the Drive line LAST
⛔ Windows healthy + registry gap is the DANGEROUS case and must SAY it is dangerous
⛔ a BLOCKED ingredient shows its reason, not a zero
⛔ a BLOCKED ingredient travels as a gap, not as a silence
⛔ a DISABLED task is not a scheduled task
⛔ a MISSING shim is UNKNOWN, not MATCH and not CHANGED
⛔ a MISSING store must NOT read as 「I cannot read the record」
⛔ a REFUSED knock is recorded too — that is the interesting one
⛔ a THROWN errand is still recorded — it must not vanish
⛔ a blocked ingredient carries WHY, and is not shown as a zero
⛔ a day with nothing new says 冇變 — it does not repeat the same list
⛔ a declared kind with readOnly:false is REFUSED even with a runner wired
⛔ a flagged reply is distinguishable from a clean one by a FIELD, not by reading it
⛔ a kind that has NEVER run still appears — absence is the signal
⛔ a mismatch NEVER throws — it reports
⛔ a missing values file is DEFAULTS, never an error and never empty
⛔ a non-live change says so BEFORE he presses
⛔ a non-zero last result is reported as UNHEALTHY, carrying the code
⛔ a per-section time renders only when the server says it earns its place
⛔ a refusal is not a request
⛔ a replace cannot resurrect a resolved stop as unresolved
⛔ a row belonging to no kind is not silently called fresh
⛔ a row records the DOOR it came through, never a cause it cannot know
⛔ a runner wired for a kind that is NOT declared read-only is REFUSED, not run
⛔ a section that did NOT read carries no time — the DEFECT-011 invariant
⛔ a slot value that looks like a key is STILL data
⛔ a stop with NO stop report is refused, and recorded as a defect rather than lost
⛔ a stopped errand WITHOUT a stop report is refused — it is the whole point of the state
⛔ an account appearing is caught — seen to fail live
⛔ an empty errand list SAYS WHY it is empty
⛔ an order that NAMES a blocked origin is refused before any browser exists
⛔ an overdue UNSCHEDULED run names its cause and does not read as a fault
⛔ an uncomparable ingredient is never counted in the clean total
⛔ an unknown kind is REFUSED, not answered with an empty detail
⛔ an unknown outcome is REFUSED, not coerced to a default
⛔ an unknown setting is REFUSED, never stored
⛔ an unreadable log REFUSES rather than assuming it is safe to run
⛔ an unreadable record is NOT 「nothing waiting」
⛔ an unreadable settings file falls back to the default and never throws
⛔ an unsupported language is refused with the list, not offered
⛔ and it is checked BEFORE the allowlist, so naming it changes nothing
⛔ and it round-trips through the REAL store as a stop, not a downgrade
⛔ and the CONSUMER sees it — the cadence used by freshness changes live
⛔ angle brackets are stripped, and the stripping is REPORTED not silent
⛔ anything not LIVE says how to apply it
⛔ but 267014 TERMINATED and 267015 NO_VALID_TRIGGERS ARE trouble, and say which
⛔ every entry has zh AND en — a half-written entry is a future second pass
⛔ every entry says what he would SAY, not what the variable is called
⛔ every row for the kind is returned — the detail is NOT capped at six
⛔ every row it writes carries nextRunAt — witness #2 has nothing to measure without it
⛔ every section reaches a state ONLY A REAL READ CAN PRODUCE
⛔ every stated exception carries its reason
⛔ every t() call site in the source passes a STRING LITERAL
⛔ freshness is present even when the store is EMPTY — that is when it matters most
⛔ freshness renders on the EMPTY store path too — that is when it matters most
⛔ if the setting is flipped back on, the probe REFUSES — seen to fail live
⛔ index.js does NOT exit or throw on a mismatch
⛔ it NEVER presses Enter — typing and submitting are different acts
⛔ it NEVER returns a blank body, even with no store on disk
⛔ it REFUSES while she holds the profile, and says it will not clear the lock
⛔ it declares RELOAD_PAGE, not LIVE — because the page is built once
⛔ it detects; it does not prevent
⛔ it renders BEFORE the clarification branch, which is what ate the last one
⛔ it says FOUND vs SHOWN, taking the total from the site\
⛔ never run at all → NEVER_RUN, which no row could ever have said
⛔ no English entry is punctuated in Chinese
⛔ no MODEL, MATCHING or FROZEN file calls t()
⛔ no attachment means no envelope — an empty block would still frame the turn
⛔ no catalogue entry contains a sentence that should have been slots
⛔ no character of the original is substituted
⛔ no count line AND nothing parsed → refuses to call it 「no recalls」
⛔ no id is defined twice — the lookup can only ever reach the first
⛔ no key is defined twice — the object cannot show you this, so the SOURCE is scanned
⛔ no typed VALUE may be stored — filled carries field names, never contents
⛔ no typed value ever reaches the surface
⛔ nothing found is an ANSWER, not a blank — but only after a real search
⛔ only ACCEPTED knocks start the clock — a refusal must not extend the ban
⛔ over 24 hours: the amount is GONE, not struck
⛔ re-running is still VISIBLE — runCount and firstAt survive the replace
⛔ read -> act -> act is REFUSED — the second ref is from a spent read
⛔ real text is NEVER touched — Chinese, accents, emoji, symbols
⛔ scheduled + DUE is a DIFFERENT sentence — the scheduler may have stopped
⛔ sending from a section opens an ORDINARY conversation that appears in the list
⛔ setLocale re-points the SAME resolver — it does not write a second one
⛔ submit() REFUSES when there is no conversation, instead of throwing
⛔ sync turned on with NO account listed is caught too — the second route
⛔ task installed + overdue → the trigger NEVER FIRED. Windows is silent; only this notices.
⛔ the CONCLUSION is rendered, and the per-run history is not
⛔ the INVENTORY above is still maintained by hand, and that is the remaining gap
⛔ the SITE\
⛔ the WAITING bar is rendered OUTSIDE the empty-screen guard
⛔ the attachment rides the FIRST turn only — no lingering scope
⛔ the bar is the briefing\
⛔ the briefing NO LONGER renders on the empty conversation screen
⛔ the browser sends the section ID, never the lines
⛔ the by-name entries still refuse what they always refused
⛔ the client never collapses a missing item list into an empty one
⛔ the defaults are the CURRENT constants, so introducing this changes no behaviour
⛔ the envelope FRAMES it as a record, in words
⛔ the errand never asks for the profile directory
⛔ the four conclusion fields render SEPARATELY — gap can never be folded into calm
⛔ the freshness lines are RENDERED — not merely delivered
⛔ the grace period applies to a STORED nextRunAt too — and that is deliberate
⛔ the ingredient list is capped, with the cost as the reason
⛔ the inlined catalogue cannot close the inline script
⛔ the module never imports the value store
⛔ the name the code looks for is the name the script registers
⛔ the narrowing is stated in the ANSWER, every time — not left in a config file
⛔ the offer shows BEFORE → AFTER in one line
⛔ the open button is a POST, never an <a href>
⛔ the preview and the prompt come from ONE function, so they cannot diverge
⛔ the preview is what the SERVER says will travel, not a client-side rendering
⛔ the recall kind is declared READ-ONLY — the timer may run nothing else
⛔ the registry is FULL, and the next one is a conversation, not a nudge
⛔ the row list is CAPPED and says how many are hidden
⛔ the section view is NEVER BLANK when the read fails
⛔ the section view shows what would travel BEFORE anything is typed
⛔ the settings offer is RENDERED, not merely delivered
⛔ the shapes data would arrive in are each rejected
⛔ the shim carries no configuration
⛔ the state is PARTIAL, not NOTHING_NEW — the summary word itself must not read clean
⛔ the things he would never say are ABSENT, and named as absent
⛔ the three sections have DISTINCT weights, not just distinct positions
⛔ the two witnesses are BOTH reported, and they are distinguishable
⛔ the waiting bar still persists on the CONVERSATION screen
⛔ the waiting section is not openable — a queue is not standing state
⛔ two settings named at once fires NOTHING — picking one would be a guess
⛔ what was attached appears in the prompt the adapter is handed
⛔ 「cannot read」 and 「nothing waiting」 render as different lines
⛔ 「it ran」 is not 「it was recorded」 — an unwritable store makes ok false
⛔ 冇門好過一道假門 — a non-openable section renders no affordance at all
⛔ 營運 / 財務 / 行政 are NOT in the sidebar, and this test is why
⛔ 首頁 has no composer — it is a report, not a conversation
⛔ 首頁 is NEVER BLANK when the read fails
⛔ 首頁 still has no composer — the section view is the ONLY place one appears
「verified」 is refused when nothing was executed — reading a file is not running it
「幫我改，每樣食材顯示 10 條回收」
今天沒有新的發票。權限設定我已經看過了
打算改成 is labelled INTENT — the card never claims it as a result
每個意圖去啱嘅 endpoint — Cantonese and English both route
現時內容 is read from the REAL file, bounded to 20 lines / 800 chars
首頁 is a sidebar destination, beside 設定
香香（Claude）receives the read-context and decision-recall blocks
香香（GPT）NOW receives them too — the v0 exclusion is removed
